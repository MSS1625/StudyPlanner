# مستند فنی پروژه — بخش ۲ از ۵: منطق سرور و API

**پروژه: برنامه‌ریز هوشمند مطالعه (Smart Study Planner)**

> ادامه‌ی مستندسازیِ فایل‌به‌فایلِ پروژه. بخش ۱ پیکربندی و مدل‌های داده را پوشش داد؛
> این بخش به لایه‌ای می‌پردازد که داده‌های خام را به API قابل‌استفاده برای فرانت‌اند تبدیل می‌کند.

---

## فهرست فایل‌های این بخش

| فایل | مسیر |
|---|---|
| `serializers.py` | `backend/planner/serializers.py` |
| `views.py` | `backend/planner/views.py` |
| `urls.py` (اپلیکیشن) | `backend/planner/urls.py` |
| `throttles.py` | `backend/planner/throttles.py` |
| `utils.py` | `backend/planner/utils.py` |
| `ml.py` | `backend/planner/ml.py` |
| `seed_demo_data.py` | `backend/planner/management/commands/seed_demo_data.py` |

---

## ۲.۱ `backend/planner/serializers.py`

سریالایزرها در Django REST Framework دو کارِ اصلی دارند: **تبدیل** مدل‌های جنگو به JSON (برای ارسال به فرانت‌اند) و **اعتبارسنجیِ** JSON دریافتی از فرانت‌اند پیش از ذخیره در دیتابیس. این فایل یک سریالایزر برای هرکدام از مدل‌های اصلی دارد.

### `UserSerializer`

سریالایزرِ ثبت‌نام. دو نکته‌ی فنی مهم دارد:

- به‌صورت پیش‌فرض، `ModelSerializer` یکتا بودنِ `username` را چک نمی‌کند؛ برای همین یک `UniqueValidator` صریح با پیامِ خطای فارسی اضافه شده است (همین کار برای `email` هم تکرار شده).
- متد `create()` بازنویسی شده تا به‌جای ساختِ سطحیِ یک رکورد، از `User.objects.create_user(**validated_data)` استفاده کند — این متدِ ویژه‌ی جنگو، رمزِ عبور را پیش از ذخیره به‌صورت خودکار Hash می‌کند (هرگز متن‌سادهٔ رمز در دیتابیس ذخیره نمی‌شود). همچنین اگر ایمیل خالی فرستاده شده باشد، از دیکشنری حذف می‌شود تا اعتبارسنجیِ «یکتا بودنِ رشته‌های خالی» به‌اشتباه رد نشود.
- فیلد `password` با `extra_kwargs = {'password': {'write_only': True}}` مشخص شده تا **هرگز** در پاسخِ API برگردانده نشود (فقط برای ورودی قابل‌استفاده است).

### `SubjectSerializer`

جالب‌ترین سریالایزرِ این فایل، چون فیلدهای محاسباتی دارد:

- `target_score`: یک نگاشتِ نام‌گذاری است — روی مدل، فیلد `target_grade` نام دارد، ولی فرانت‌اند از نامِ `target_score` استفاده می‌کند. با `serializers.FloatField(source='target_grade', ...)` این دو نام به هم وصل شده‌اند (هم برای خواندن، هم برای نوشتن).
- `notes`: فیلدِ واقعیِ مدل (از 2026-08-25 و Migrationِ `0005`)؛ هم نوشتنی است هم خواندنی و در فرمِ درس جمع‌آوری می‌شود.
- متد `to_representation` بازنویسی شده: پیش از برگرداندنِ خروجیِ استاندارد، تابعِ `compute_subject_progress` (از `utils.py`) را **یک‌بار** صدا می‌زند و هر چهار مقدارِ مرتبط با پیشرفت (`completed_hours`, `total_hours`, `remaining_hours`, `progress_percent`) را همزمان محاسبه و به خروجی اضافه می‌کند. این طراحی عمداً به‌جای چهار `SerializerMethodField` جدا انتخاب شده تا تابعِ پرهزینه چهار بار به‌جای یک بار اجرا نشود.

### `ExamSerializer`

علاوه بر فیلدهای مستقیمِ مدل (`subject`, `exam_date`, `chapters_remaining`, `study_hours_remaining`)، یک فیلدِ فقط-خواندنی به نام `subject_name` دارد که مستقیماً از رابطه‌ی `exam.subject.name` خوانده می‌شود — این کار باعث می‌شود فرانت‌اند برای نمایشِ نامِ درس، مجبور به زدنِ یک درخواستِ جداگانه نباشد.

### `StudyPlanSerializer`

یک سریالایزرِ ساده و خامِ مدلِ `StudyPlan`. نکته‌ی مهم: این سریالایزر فقط برای مسیرهای پیش‌فرضِ `create`/`retrieve` استفاده می‌شود؛ خروجیِ اصلیِ صفحه‌ی «برنامه‌ی مطالعه» (که ساختارِ کاملاً متفاوتی دارد) از طریقِ این سریالایزر ساخته نمی‌شود، بلکه در `views.py` با تابعِ `format_plan_for_frontend` (از `utils.py`) ساخته می‌شود.

### `StudyLogSerializer`

از 2026-08-29 فیلدِ `notes` (یادداشتِ اختیاری، Migrationِ `0006`) هم جزوِ فیلدهای خواندنی/نوشتنیِ `ExamSerializer` است.

دو فیلدِ فقط-خواندنی برای راحتیِ نمایش دارد: `user` (نامِ کاربری، نه شناسه) و `exam_name` که با `source='exam.subject.name'` از **دو پله‌ی رابطه** عبور می‌کند (StudyLog -> Exam -> Subject -> name).

---

## ۲.۲ `backend/planner/views.py` — مغزِ عملیاتیِ API

این فایل تعیین می‌کند هر درخواستِ HTTP دقیقاً چه کاری باید انجام دهد. دو الگو در این فایل استفاده شده:

1. **توابعِ ساده با `@api_view`**: برای عملیاتی که CRUD معمولی نیستند (`register`, `login`, `dashboard`).
2. **کلاس‌های `ViewSet`**: برای منابعی که عملیاتِ استانداردِ CRUD دارند (`Subject`, `Exam`, `StudyLog`, `StudyPlan`)؛ این کلاس‌ها با کمکِ یک `Router` (در `urls.py`) خودکار به مسیرهای `GET/POST/PUT/PATCH/DELETE` تبدیل می‌شوند.

### احراز هویت: `register` و `login`

هردو با `@permission_classes([AllowAny])` علامت‌گذاری شده‌اند — یعنی برخلافِ بقیه‌ی مسیرهای پروژه (که پیش‌فرضشان نیازمندِ لاگین‌بودن است)، این دو مسیر بدونِ توکن هم قابلِ‌دسترسی‌اند (طبیعتاً؛ کسی که هنوز حساب ندارد نمی‌تواند لاگین کرده باشد). از 2026-09-10 هردو `@throttle_classes([AuthBurstThrottle])` هم دارند: محدودسازیِ نرخِ ویژه‌ی احرازِ هویت (کلاسش در `planner/throttles.py`؛ نرخِ scopeِ 'auth' از `DJANGO_AUTH_THROTTLE_RATE` — پیش‌فرضِ توسعه 10000/min = مؤثراً نامحدود) — دفاعِ brute-force: بورستِ حدسِ رمز از یک IP پس از سقفِ نرخ، ۴۲۹ با هدرِ `Retry-After` می‌گیرد.

- `register`: داده‌ی ورودی را به `UserSerializer` می‌دهد؛ اگر معتبر بود، کاربر را می‌سازد و **بلافاصله** یک جفت توکنِ JWT (دسترسی + تمدید) هم صادر می‌کند تا کاربر مجبور نباشد بلافاصله بعد از ثبت‌نام دوباره وارد شود.
- `login`: با تابعِ آماده‌ی جنگو `authenticate(username, password)` رمزِ واردشده را دوباره Hash و با دیتابیس مقایسه می‌کند؛ در صورتِ موفقیت، توکن صادر می‌کند، در غیرِ این‌صورت خطای ۴۰۱ برمی‌گرداند.

از 2026-09-06 مسیرِ `POST /api/auth/refresh/` هم عضوِ همین خانواده است: فرانت‌اند بعد از هر پاسخِ 401، توکنِ Refreshِ ذخیره‌شده را به آن می‌فرستد؛ توکنِ معتبر 200 و توکنِ دسترسیِ تازه می‌گیرد، توکنِ منقضی/بی‌اعتبار 401 — و همان 401 است که به فرانت‌اند می‌گوید «نشست تمام است، کاربر را به صفحه‌ی ورود بفرست».
از 2026-09-08 مسیرِ `POST /api/auth/logout/` هم عضوِ همین خانواده است (ویویِ آماده‌ی `TokenBlacklistView`): فرانت‌اند موقعِ خروج، توکنِ Refresh را به آن می‌فرستد تا رویِ سرور باطل شود؛ توکنِ معتبر 200 و بی‌اعتبار 401. به‌همراه‌ی چرخشِ فعالِ توکنِ تمدید (هر تمدید توکنِ تازه صادر می‌کند و قبلی را باطل می‌کند)، هر توکنِ Refresh فقط یک‌بار قابلِ استفاده است.
از 2026-09-09 همه‌ی پیام‌هایِ اختصاصیِ این لایه (خطایِ ورود، اعتبارسنجی‌ها، هشدارهای داشبورد و...) با `gettext` پیچیده شده‌اند و `LocaleMiddleware` زبانِ فعال را از هدرِ `Accept-Language` تعیین می‌کند: بدونِ هدر = فارسی (همانِ متنِ اصلی)، `en` = ترجمه از `locale/en`؛ رشته‌های دارایِ متغیر با الگوی `%(name)s` پارامتری‌اند.

### `SubjectViewSet`, `ExamViewSet`, `StudyLogViewSet`

هر سه از الگوی مشابهی پیروی می‌کنند که یک بار توضیحش می‌دهیم:

- **`get_queryset`**: به‌جای یک `queryset` ثابت، این متد بازنویسی شده تا همیشه بر اساسِ `self.request.user` فیلتر کند. این **مهم‌ترین نکته‌ی امنیتیِ کل API** است: تضمین می‌کند هر کاربر فقط داده‌های خودش را ببیند، حتی اگر شناسه‌ی رکوردِ کاربرِ دیگری را حدس بزند (جلوگیری از آسیب‌پذیریِ نوعِ IDOR). در `SubjectViewSet` علاوه بر فیلتر، از `prefetch_related('exams__study_logs')` هم استفاده شده تا محاسبه‌ی پیشرفتِ درس (که به امتحان‌ها و لاگ‌ها نیاز دارد) باعثِ کوئری‌های تکراری (مشکلِ N+1) نشود.
- **`perform_create`**: هنگامِ ساختِ رکوردِ جدید، فیلدِ مالکیت (`user`) از روی کاربرِ لاگین‌کرده پر می‌شود، نه از ورودیِ خودِ کاربر — یعنی کسی نمی‌تواند برای کاربرِ دیگری داده بسازد. در `ExamViewSet` و `StudyLogViewSet` یک لایه‌ی امنیتیِ اضافه هم هست: حتی اگر کاربر شناسه‌ی یک `Subject`/`Exam` متعلق به کاربرِ دیگری را در بدنه‌ی درخواست بفرستد، با یک بررسیِ صریح (`if subject.user != self.request.user`) و پرتاب‌کردنِ `PermissionDenied`، این تلاش رد می‌شود. از 2026-08-29 همین بررسی در `ExamViewSet.perform_update` هم تکرار می‌شود — یعنی هنگامِ «ویرایشِ امتحان» هم نمی‌توان درسِ آن را به درسِ کاربرِ دیگری تغییر داد.

- **`pagination_class` (از 2026-09-06):** هر سه ViewSet کلاسِ مشترکِ `OptionalPageNumberPagination` را دارند — صفحه‌بندیِ «اختیاری» که فقط با `?page=` یا `?page_size=` فعال می‌شود؛ بدونِ آنها همان لیستِ کاملِ قبلی برمی‌گردد (فرانت‌اندِ فعلی پارامتری نمی‌فرستد). قالبِ پاسخِ فعال: `{count, next, previous, results}`؛ سقفِ `page_size` = ۱۰۰؛ فقط `?page=` = اندازه‌ی ۲۰؛ `page_size` نامعتبر/غیرمثبتِ تنها = صفحه‌بندی غیرفعال؛ صفحه‌ی نامعتبر/خارج از محدوده = ۴۰۴ استانداردِ DRF. مرتب‌سازیِ قطعیِ صفحه‌ها از `Meta.ordering` خودِ مدل‌ها تأمین می‌شود (درس/گزارش: جدیدترین اول؛ امتحان: نزدیک‌ترین تاریخ اول).

### `StudyPlanViewSet` — جایی که از REST خالص فاصله گرفته شده

این ViewSet دو رفتارِ غیرمعمول دارد که هردو **آگاهانه** طراحی شده‌اند:

**۱) بازنویسیِ متدِ `list()`:**
به‌جای برگرداندنِ فهرستِ خامِ رکوردهای `StudyPlan`، این متد:
1. تنظیماتِ کاربر (ساعتِ آزادِ روزانه) را با تابعِ کمکیِ `_get_or_create_plan_settings` می‌گیرد.
2. برنامه را **زنده و از نو** با `generate_study_plan` (از `utils.py`) محاسبه می‌کند — یعنی هیچ نسخه‌ی کش‌شده‌ای خوانده نمی‌شود؛ همیشه با آخرین وضعیتِ درس/امتحان‌ها هماهنگ است.
3. خروجیِ خام را با `format_plan_for_frontend` به ساختاری که فرانت‌اند می‌خواهد (بر اساسِ پارامترِ URL به نامِ `range`، مقدار `daily` یا `weekly`) تبدیل می‌کند.

**۲) اکشنِ سفارشیِ `generate`:**
با دکوریتورِ `@action(detail=False, methods=['post'], url_path='generate')`، مسیرِ `/api/study-plan/generate/` ساخته می‌شود. این متد:
1. مقدارِ `daily_available_hours` ارسالی را می‌خواند و اعتبارسنجی می‌کند (باید عددی بینِ ۰ تا ۲۴ باشد).
2. تنظیماتِ کاربر را با این مقدارِ جدید به‌روزرسانی می‌کند.
3. برنامه را دوباره محاسبه و (اگر واقعاً برنامه‌ای ساخته شد، نه یک پیامِ خطا) در فیلدِ `plan_data` ذخیره می‌کند.
4. همان ساختارِ فرمت‌شده‌ی `list()` را برمی‌گرداند تا فرانت‌اند بلافاصله بتواند رندر کند.

از 2026-09-05 (مایگریشنِ `0008`)، متدِ `perform_create` این ViewSet هم محافظِ یکتایی دارد: فراخوانیِ مستقیمِ `POST /api/study-plan/` وقتی رکوردِ تنظیمات از قبل وجود دارد، پاسخِ ۴۰۰ با پیامِ راهنما می‌گیرد — نه خطایِ ۵۰۰ خامِ `IntegrityError`. چکِ پیشینی انجام می‌شود و اگر رقتی در فاصله‌ی چک تا INSERT رخ دهد، قیدِ DB خودش مانع می‌شود و خطا باز هم به ۴۰۰ خوانا تبدیل می‌شود.

### `dashboard` — تجمیع‌کننده‌ی نهایی

این تابع (زیرِ `@api_view(['GET'])`) یک تصویرِ کلی از وضعیتِ کاربر می‌سازد و دقیقاً با ساختاری که فرانت‌اند (`renderDashboard` در `app.js`) انتظار دارد، پاسخ می‌دهد:

1. **پیشرفتِ درس‌ها**: با حلقه زدن روی همه‌ی درس‌های کاربر (با `prefetch_related` برای جلوگیری از N+1) و صدا زدنِ `compute_subject_progress` برای هرکدام، هم فهرستِ جزئیات و هم میانگینِ کلی محاسبه می‌شود.
2. **امتحان‌های پیشِ‌رو و هشدارها**: برای هرکدام از ۸ امتحانِ نزدیک‌ترِ کاربر، بر اساسِ تعدادِ روزهای باقی‌مانده، یک هشدار می‌سازد: **قرمز (danger)** اگر ۳ روز یا کمتر مانده و هنوز ساعتِ مطالعه باقی است، **زرد (warning)** اگر بین ۴ تا ۷ روز مانده.
3. **یادآوریِ روزِ جاری**: از همان خروجیِ الگوریتم (`generate_study_plan`)، فقط تسک‌های «امروز» را برمی‌دارد و یک جمله‌ی طبیعیِ فارسی می‌سازد (نوع `info`، آبی‌رنگ) — این باعث می‌شود پنلِ «هشدارها و یادآوری‌ها» واقعاً هم هشدار داشته باشد هم یادآوریِ خنثی.
4. **تقسیم‌زمانِ پیشنهادی**: با تابعِ `build_subject_distribution` (توضیح در `utils.py`)، سهمِ هر درس از هفته‌ی پیشِ‌رو محاسبه می‌شود.
5. در پایان، `urgent_alerts_count` (فقط تعدادِ هشدارهای قرمز) جدا از فهرستِ کاملِ `alerts` محاسبه می‌شود تا کارتِ «هشدارهای فوری» در داشبورد فقط موارد واقعاً فوری را بشمارد، نه هر نوع هشدار/یادآوری.

### تابعِ کمکیِ خصوصیِ `_get_or_create_plan_settings`

یک تابعِ کوچکِ سطحِ‌ماژول (نه متدِ کلاس) که در چند جای این فایل (هم در `StudyPlanViewSet`، هم در `dashboard`) استفاده می‌شود. کارش این است: اگر کاربر از قبل رکوردِ تنظیماتی نداشته باشد، با مقدارِ پیش‌فرضِ ۲ ساعت می‌سازد، وگرنه همان رکوردِ موجود را برمی‌گرداند (`get_or_create`). نوشتنِ این منطق در یک تابعِ مشترک، از تکرارِ کد جلوگیری می‌کند (اصلِ DRY: Don't Repeat Yourself).

---

## ۲.۳ `backend/planner/urls.py` — نقشه‌ی مسیرهای اپلیکیشن

این فایل با کمکِ `DefaultRouter` از Django REST Framework، مسیرهای CRUD را **خودکار** از روی ViewSetها می‌سازد؛ به‌جای این‌که هرکدام از ۵ عملیات (list/create/retrieve/update/destroy) دستی نوشته شود:

```python
router.register(r'subjects', views.SubjectViewSet, basename='subject')
router.register(r'exams', views.ExamViewSet, basename='exam')
router.register(r'study-plan', views.StudyPlanViewSet, basename='study-plan')
router.register(r'study-logs', views.StudyLogViewSet, basename='studylog')
```

سپس هفت مسیرِ باقیمانده دستی اضافه می‌شوند: `auth/register/`, `auth/login/`, `auth/refresh/`, `auth/logout/`, `dashboard/`, `predictions/` (از 2026-09-09 — مؤلفه‌ی یادگیریِ آماری؛ فقط با احرازِ هویت) و `health/` (از 2026-09-10 — پایشِ سلامت؛ عمومی و معاف از throttle). سه‌تایِ اول توابعِ ساده‌ی همین فایل‌اند؛ `auth/refresh/` اما ویویِ آماده‌ی `TokenRefreshView` از SimpleJWT است (بدنه‌ی `{"refresh": "..."}` می‌گیرد و توکنِ دسترسیِ تازه برمی‌گرداند — با چرخشِ فعال، توکنِ Refreshِ تازه هم؛ قبلی بلافاصله باطل می‌شود) و `auth/logout/` ویویِ آماده‌ی `TokenBlacklistView` است (از 2026-09-08؛ توکنِ Refreshِ داده‌شده را در لیستِ سیاه ابطال می‌کند)؛ هر دو مثلِ register/login بدونِ هدرِ Authorization در دسترس‌اند.

**نکته‌ی فنیِ مهم که در کامنت‌های خودِ فایل هم آمده:** ترتیب اجرا حیاتی است — تمامِ فراخوانی‌های `router.register(...)` باید **پیش از** خطِ `path('', include(router.urls))` نوشته شوند. چون `router.urls` در همان لحظه‌ای که خوانده می‌شود، لیستِ نهاییِ URLها را از روی ViewSetهای *تا آن لحظه ثبت‌شده* می‌سازد؛ اگر چیزی بعد از آن ثبت شود، اصلاً به فهرستِ نهایی اضافه نمی‌شود.

---

## ۲.۴ `backend/planner/utils.py` — الگوریتمِ «هوشمندِ» برنامه‌ریزی

این فایل قلبِ منطقیِ پروژه است و **هیچ سرویسِ هوش مصنوعیِ خارجی** در آن استفاده نشده؛ تماماً پایتونِ خالص و یک فرمولِ ریاضیِ ساده و قابل‌توضیح.

### `compute_subject_progress(subject)`

درصدِ پیشرفتِ یک درس را حساب می‌کند. ایده: هر امتحان یک «ساعتِ باقی‌مانده» دارد که با هر `StudyLog` کم می‌شود؛ پس همیشه داریم:

```
کل ساعتِ یک امتحان = ساعتِ باقی‌مانده‌ی فعلی + مجموعِ ساعاتِ لاگ‌شده
```

با جمعِ این مقدار روی همه‌ی امتحان‌های یک درس، پیشرفتِ کلِ درس به دست می‌آید. خروجی یک تاپلِ سه‌تایی است: `(ساعاتِ مطالعه‌شده, کل ساعات, درصد پیشرفت)`.

### `generate_study_plan(user, daily_available_hours)` — الگوریتمِ اصلی

**مرحله‌ی ۱ — محاسبه‌ی نیازِ وزنیِ هر امتحان (یک‌بار، ثابت):**

$$
\text{نیازِ روزانه‌ی خام} = \frac{\text{ساعتِ باقی‌مانده}}{\text{روزهای باقی‌مانده}}, \qquad
\text{نیازِ وزنی} = \text{نیازِ روزانه‌ی خام} \times \text{سختیِ درس}
$$

ضرب در سختیِ درس باعث می‌شود درسِ سخت‌تر، حتی با حجمِ مشابه، سهمِ بیشتری از زمان بگیرد.

**مرحله‌ی ۲ — حلقه روی هر روز (از امروز تا حداکثر ۶۰ روزِ بعد، برای جلوگیری از محاسبه‌ی بی‌فایده‌ی امتحان‌های خیلی دور):**

برای هر روز، فقط امتحان‌هایی که هنوز تاریخشان نگذشته در نظر گرفته می‌شوند و ساعتِ آزادِ روزانه‌ی کاربر بینشان **متناسب با نسبتِ نیازِ وزنیِ هرکدام** تقسیم می‌شود:

$$
\text{سهمِ درس از امروز} = \frac{\text{نیازِ وزنیِ آن درس}}{\sum \text{نیازِ وزنیِ درس‌های فعال}}, \qquad
\text{ساعتِ تخصیص‌یافته} = \text{سهم} \times \text{ساعتِ آزادِ روزانه}
$$

اگر هیچ امتحانِ آینده‌ای نباشد، یا مجموعِ نیازِ وزنی صفر شود (همه‌چیز پوشش داده شده)، تابع به‌جای یک برنامه‌ی خالی، یک پیامِ توضیحیِ فارسی برمی‌گرداند.

### `format_plan_for_frontend(raw_plan, range_type)`

خروجیِ خامِ بالا (دیکشنریِ تاریخ -> لیستِ تسک) را به شکلی که صفحه‌ی «برنامه‌ی مطالعه» می‌تواند مستقیماً رندر کند تبدیل می‌کند. مهم‌ترین کارِ این تابع، **ادغامِ روزهای پیاپیِ هم‌برنامه** است: چون تا وقتی تاریخِ هیچ امتحانی نگذرد، الگوریتم دقیقاً همان ترکیبِ درس/ساعت را برای چند روزِ پشتِ‌سرهم پیشنهاد می‌دهد، نمایشِ چند بلوکِ کاملاً یکسان زیر هم در نمای هفتگی بی‌فایده و تکراری به‌نظر می‌رسد. این تابع با ساختنِ یک «امضا» (Signature) از ترکیبِ درس/ساعتِ هر روز، روزهای هم‌امضا را در یک بازه (مثلاً «امروز تا پنج‌شنبه») ادغام می‌کند.

### `build_subject_distribution(raw_plan, days, top_n)`

داده‌ی نمودارِ «تقسیم‌زمانِ پیشنهادی» را می‌سازد. نکته‌ی مهمِ طراحی: چون هر روز کلِ ساعتِ آزاد به‌طور کامل تخصیص می‌یابد، **مجموعِ ساعتِ هر روز تقریباً همیشه ثابت است** — پس نموداری بر مبنای «روز» بی‌معنی می‌شود. به‌جایش، این تابع سهمِ هر **درس** را در طولِ هفته‌ی پیشِ‌رو جمع می‌زند و نسبت به پرکارترین درس نرمال می‌کند (پرکارترین درس = ۱۰۰٪).

---

## ۲.۵ `backend/planner/management/commands/seed_demo_data.py`

یک **Management Command سفارشی**: هر فایلی که در مسیرِ `planner/management/commands/` قرار بگیرد و کلاسِ `Command` (ارث‌بر از `BaseCommand`) در آن تعریف شود، خودکار به یک زیر-دستور برای `manage.py` تبدیل می‌شود.

**هدف:** برای یک کاربرِ مشخص، چند درس و امتحانِ نمونه با **تاریخِ آینده** می‌سازد تا بشود داشبورد و برنامه‌ریزِ مطالعه را با داده‌ی واقعی (نه خالی) تست کرد.

**اجرا:** `python manage.py seed_demo_data <username> [--reset]`

**نحوه‌ی کار:**
1. `add_arguments`: یک آرگومانِ اجباری (`username`) و یک پرچمِ اختیاری (`--reset`) تعریف می‌کند (با ماژولِ استانداردِ `argparse` که جنگو زیرِ پوسته از آن استفاده می‌کند).
2. `handle`: ابتدا کاربر را پیدا می‌کند (اگر نبود، خطای خوانا می‌دهد). اگر `--reset` داده شده بود، همه‌ی درس‌های قبلیِ آن کاربر را حذف می‌کند (که به‌خاطرِ `on_delete=CASCADE`، امتحان‌ها و لاگ‌های مرتبط هم خودکار حذف می‌شوند).
3. روی یک فهرستِ ثابتِ ۴ درسِ نمونه (با سختی، هدف‌نمره و فاصله‌ی زمانیِ متفاوت) حلقه می‌زند و با `get_or_create` هرکدام را می‌سازد (اگر از قبل با همین نام وجود داشت، همان استفاده می‌شود، چیزی تکراری ساخته نمی‌شود).
4. برای هر درس، اگر امتحانِ آینده‌ای نداشت، یک امتحانِ نمونه با تاریخِ آینده می‌سازد.
5. برای اولین درس، یک `StudyLog` نمونه هم ثبت می‌کند تا نوارِ پیشرفت در داشبورد صفر نباشد (این هم به‌طورِ ضمنی منطقِ `StudyLog.save()` را تست می‌کند).


## ۲.۶ `backend/planner/tests.py` — مجموعه‌تستِ خودکار (از 2026-08-29)

۱۸۵ تستِ Django/DRF روی `APITestCase` (به‌علاوهِ دو کلاسِ `TransactionTestCase` — قیدِ DB و حذفِ گزارشِ یتیم — + کلاسِ `JWTTokenRotationBlacklistTests` (چرخش/لیستِ سیاه/خروجِ سرور-محور) + کلاسِ `StudyLogConcurrencyLockTests` (قفلِ هم‌زمانی و خواندنِ تازه) + کلاسِ `SettingsEnvVarsTests` با `SimpleTestCase` برای متغیرهایِ محیطی + کلاس‌هایِ چندزبانی: `I18nAcceptLanguageTests` (۹) و `I18nCatalogIntegrityTests` (۶) + پنج کلاسِ آمادگیِ استقرار (سلامت/throttle/هدرهایِ امنیتی؛ از 2026-09-10)) که کلِ سطحِ API و الگوریتم را پوشش می‌دهند. کلاسِ پایه‌ی `BaseAPITestCase` دو کاربرِ نمونه (alice/bob) می‌سازد، متدِ `client_as(user)` کلاینتِ احراز‌هویت‌شده با **همان مسیرِ واقعیِ JWT** (توکنِ `SimpleJWT` در هدرِ `Authorization: Bearer`) برمی‌گرداند — یعنی لایه‌ی احراز هویت هم داخلِ تست‌هاست، نه دور زده‌شده با `force_authenticate` — و از 2026-09-10 `tearDown` آن کش را پاک می‌کند تا شمارنده‌هایِ throttle بینِ تست‌ها نشت نکنند.

| کلاس | تعداد | پوشش |
|---|---|---|
| `AuthAPITests` | ۱۰ | ثبت‌نام/ورود، توکن‌ها، رمزِ write_only، 401 برای endpointهای محافظت‌شده، و تمدیدِ توکن (معتبر → 200 + توکنِ کارا؛ بی‌اعتبر → 401) |
| `JWTTokenRotationBlacklistTests` | ۷ | چرخشِ توکنِ Refresh (توکنِ تازه در پاسخِ تمدید)، باطل‌شدنِ توکنِ قبلی (۴۰۱)، ادامه‌ی زنجیره‌ی تمدید، کارکردنِ توکنِ دسترسیِ چرخیده، خروجِ سرور-محور (ابطال + ۴۰۱ِ توکنِ بی‌اعتبار + اثرِ نگذاشتنِ رویِ نشستِ دیگر) |
| `SubjectAPITests` | ۱۱ | CRUD، فیلدِ `notes`، نگاشتِ `target_score`، یکتاییِ نام (create و rename)، جداسازیِ کاربران |
| `ExamAPITests` | ۱۴ | CRUD، ویرایشِ PATCH/PUT، فیلدِ `notes`، و چهار سناریوی امنیتی (403/404) |
| `StudyLogAPITests` | ۱۴ | کسرِ خودکارِ ساعت، غیرمنفی‌شدن، پیشرفت ۲۵٪، جداسازی، بازگرداندنِ ساعت در حذف (دقیق / کسرِ محدود / بدونِ کسر / چندگانه / آبشاری) |
| `StudyPlanAPITests` | ۹ | تنظیماتِ پیش‌فرضِ ۲ ساعت، شکلِ پاسخ، اعتبارسنجیِ ساعت (0/25/abc)، get_or_create، رگرسیونِ POST دوباره → ۴۰۰ |
| `StudyPlanUniqueConstraintTests` | ۲ | قیدِ یکتاییِ `StudyPlan.user` در خودِ DB (CREATE دوم → IntegrityError) + idempotentبودنِ `get_or_create` — با `TransactionTestCase` |
| `DashboardAPITests` | ۶ | شکلِ پاسخ، شمارش‌ها، سه نوعِ هشدار (danger/warning/info)، نرمال‌سازیِ نمودار |
| `StudyPlanAlgorithmTests` | ۷ | تستِ واحدِ توابعِ `utils.py`: پیشرفت، تخصیصِ کاملِ ساعتِ روزانه، ادغامِ روزهایِ هفتگی |
| `PaginationAPITests` | ۹ | صفحه‌بندیِ اختیاری: رگرسیونِ لیستِ خامِ بدونِ پارامتر (هر سه endpoint)، شکلِ `{count,next,previous,results}`، پیمایشِ کاملِ صفحه‌ها بدونِ هم‌پوشانی، ۴۰۴ِ صفحه‌ی نامعتبر، رفتارِ page_size نامعتبر، جداسازیِ کاربران، و تستِ واحدِ قواعدِ `get_page_size` |
| `SettingsEnvVarsTests` | ۷ | متغیرهایِ محیطیِ تنظیمات: تستِ واحدِ `_env_bool`/`_env_list` + پنج بوتِ واقعیِ مفسرِ جدا (پیش‌فرضِ توسعه بدونِ متغیر؛ ردِ بوتِ Production بدونِ کلید/بدونِ Host؛ بوتِ سالم با ترکیبِ کامل؛ خاموش‌کردنِ CORS allow-all) |
| `StudyLogConcurrencyLockTests` | ۶ | قفلِ هم‌زمانی (2026-09-09): کسر از مقدارِ تازه‌ی DB نه snapshotِ حافظه (رگرسیونِ Lost Update)، بازگشتِ ساعت به مقدارِ لحظه‌ایِ DB، قفل‌بودنِ مسیرِ save/delete (جاسوسِ QuerySet.get — در SQLite در SQL دیده نمی‌شود)، ویرایشِ بدونِ قفل/کسر، خطایِ روشنِ DoesNotExist هنگامِ حذفِ هم‌زمانِ امتحان |
| `StudyLogOrphanDeleteTests` | ۱ | حذفِ گزارشِ یتیم (کلیدِ خارجیِ شکسته — ساختِ یتیم با خاموش‌کردنِ موقتِ FK، بیرونِ تراکنشِ تست): فقط خودِ گزارش حذف، بدونِ resurrection — با `TransactionTestCase` |
| `I18nAcceptLanguageTests` | ۹ | چندزبانی (2026-09-09): پیش‌فرضِ فارسیِ بدونِ هدر، ترجمه با `Accept-Language: en`، پایداری با fa، fallback با زبانِ نامعتبر (de)، هدرِ واقعیِ مرورگر، ترجمه‌ی پیامِ سریالایزر (gettext_lazy)، خطاهای generate، عنوان/نشانِ برنامه و هشدارِ داشبورد — همه با interpolation پارامتری |
| `I18nCatalogIntegrityTests` | ۶ | چندزبانی (2026-09-09): وجود/خواناییِ django.mo با gettext پایتونی، رگرسیونِ کامپایل (هیچ msgstr خالی یا برابرِ msgid نیست)، fallbackِ کلیدِ ناشناس، ترجمه‌ی lazyِ `WEEKDAY_FA`، شکلِ تنظیمات و ترتیبِ `LocaleMiddleware` — با `SimpleTestCase` |
| `MLCalibrationMathTests` | ۹ | مؤلفه‌ی ML (2026-09-09): LSQ از مبدأ با اعدادِ دستیِ قابل‌ردیابی، انقباضِ بیزی (نمونه‌ی کم = محافظه‌کاری)، مهارِ سقف/کفِ β، مرزهایِ برچسبِ سوگیری، سه سطحِ ریسک + مرزِ days_left=۰ و گردشدن، اعمالِ β در پیش‌بینی — با `SimpleTestCase` و بدونِ DB |
| `MLTrainingDataTests` | ۵ | مؤلفه‌ی ML: فقط امتحان‌هایِ گذشته‌ی دارایِ گزارش نمونه می‌شوند، بازسازیِ planned از «باقی‌مانده + کسرشده‌ها»، ردِ planned صفر، مرزِ «امروز» (آموزش نه/پیش‌بینی بله)، جداسازیِ کاربران |
| `MLPredictionsAPITests` | ۷ | مؤلفه‌ی ML: 401 بدونِ توکن، کاربرِ بی‌تاریخچه (β=۱)، اثرِ یادگیری روی پیش‌بینی (۶→۸ ساعت)، فقط امتحان‌هایِ آینده‌ی فعال و مرتب، سه سطحِ ریسک با پیش‌فرضِ ۲ ساعت، حساسیتِ ریسک به تنظیماتِ خودِ کاربر، جداسازیِ مدل بین کاربران |
| `HealthEndpointTests` | ۷ | آمادگیِ استقرار (2026-09-10): عمومی‌بودنِ /api/health/ (۲۰۰ بدونِ لاگین)، payload دقیقاً چهارت کلیدِ ماشین‌خوان، engine واقعی، فلگِ debug، ردِ POST (۴۰۵)، معاف‌ماندن از throttle حتی با نرخِ ۱/دقیقه، ۵۰۳ِ ساخت‌یافته با خرابیِ دیتابیس (patch اتصال) |
| `ThrottlingAPITests` | ۹ | محدودسازیِ نرخ (2026-09-10): بورستِ login (سومی ۴۲۹) و register، کلیدِ per-IP (X-Forwarded-For)، هدرِ Retry-After، نرخِ per-user روی endpoint داده و جداسازیِ آن از IP، 401 قبل از throttle برایِ مسیرهایِ محافظت‌شده، کارکردِ loginِ عادی زیرِ نرخِ سخت، انقضایِ پنجره (نرخِ 2/sec + گذرِ زمان) — نرخ‌ها با patch مستقیم رویِ `SimpleRateThrottle.THROTTLE_RATES` (snapshotِ import) |
| `ThrottleDevSafeDefaultsTests` | ۱ | رگرسیونِ ۶.۱۰: ۲۵ درخواستِ بی‌احراز + ۱۰ احرازِشده‌ی متوالی با پیش‌فرضِ توسعه = هیچ ۴۲۹ |
| `ThrottleAndSecuritySettingsTests` | ۱۰ | متغیرهایِ جدید: تستِ واحدِ `_env_throttle_rate`/`_env_int`/`_env_proxy_ssl_header` (قالب‌هایِ معتبر/خراب) + بوتِ واقعیِ مفسرِ جدا: پیش‌فرضِ توسعه دست‌نخورده، هشدارِ stderr با DEBUG=false و نرخ‌هایِ پیش‌فرض، نبودِ هشدار با نرخ‌هایِ ست‌شده، اعمالِ ترکیبِ کاملِ Production (نرخ‌ها + SSL + کوکی + HSTS + هدرِ پروکسی)، ردِ بوت با نرخ/HSTS/هدرِ خراب — با `SimpleTestCase` |
| `SecurityHeadersResponseTests` | ۶ | هدرهایِ امنیتیِ شرطی: HSTS فقط وقتی روشن و درخواستِ امن (پشتِ پروکسی)، نبودنش در پیش‌فرضِ توسعه، 301 فقط وقتی ریدایرکتِ SSL روشن، فلگِ Secure کوکیِ CSRF فقط وقتی روشن (و نه در پیش‌فرض) |

**نکته‌های طراحی:** همه‌ی تاریخ‌ها نسبت به `date.today()` ساخته می‌شوند (تست‌ها هرگز کهنه نمی‌شوند)؛ هر رفعِ باگِ تاریخ‌دار، رگرسیون‌تستِ خودش را دارد؛ و اجرای تست‌ها از دیتابیسِ درون‌حافظه‌ی Django استفاده می‌کند (به `db.sqlite3` اصلی دست نمی‌زند). همین تست‌ها در اولین اجرا باگِ «خطای ۵۰۰ نامِ درسِ تکراری» را کشف کردند که با `validate_name` در سریالایزر رفع شد — نمونه‌ای از ارزشِ تستِ نوشتن. بازگرداندنِ ساعت در حذف (2026-09-03) هم پنج رگرسیون‌تستِ اختصاصی دارد — از جمله حالتِ «کسرِ محدود» که تضمین می‌کند حذفِ گزارش، ساعتِ امتحان را از مقدارِ اولیه‌اش بیشتر نکند. قفلِ هم‌زمانی و خواندنِ تازه (2026-09-09) هم شش تستِ اختصاصی دارد — دو رگرسیونِ Lost Update رویِ کدِ قبلی قطعی‌آور FAIL می‌شوند (کدِ قدیمی با snapshotِ حافظه اعدادِ ۱ به‌جای ۰ و ۵ به‌جای ۱۳ می‌نوشت). از 2026-09-09 کلِ مجموعه روی هر دو موتور (SQLite و PostgreSQL از طریقِ `DATABASE_URL`) سبز است و `PostgresForUpdateTests` وجودِ قفلِ واقعیِ FOR UPDATE را در SQLِ PostgreSQL بررسی می‌کند.
قیدِ یکتاییِ `StudyPlan.user` (2026-09-05) هم رگرسیون‌هایِ خودش را دارد: POST دوباره به endpoint باید ۴۰۰ بگیرد (نه ۵۰۰) و در سطحِ DB، CREATE دوم باید IntegrityError بدهد — دومی با `TransactionTestCase` نوشته شده چون این خطا تراکنشِ تستِ معمولی را می‌شکند.


## ۲.۷ `backend/planner/ml.py` — مؤلفه‌ی یادگیریِ آماری (از 2026-09-09)

آخرین آیتمِ `TODO.md` — «یک مدلِ رگرسیونیِ ساده که بر اساسِ داده‌هایِ تاریخیِ `StudyLog` هر کاربر، زمانِ واقعیِ موردنیاز را پیش‌بینی کند» — این‌جا با پایتونِ خالص و بدونِ هیچ وابستگیِ جدید پیاده شد. مدل، «کالیبراسیونِ تخمین‌هایِ ساعتیِ کاربر» است:

- **نمونه‌ی آموزشی** به‌ازای هر امتحانِ *گذشته* (exam_date < today) که گزارشِ مطالعه دارد: `planned = study_hours_remaining + Σ hours_deducted` (بازسازیِ تخمینِ اولیه) و `actual = Σ hours_studied` (واقعیتِ ثبت‌شده — مطالعه‌ی فراتر از طرح، خودش سیگنالِ دست‌کم‌گیری است). امتحانِ آینده/امروزی در آموزش نیست (جلوگیری از نشتِ آینده).
- **برازش**: رگرسیونِ کمینه‌ی مربعاتِ *از مبدأ* (β = Σxy/Σx²) — نمونه‌هایِ پرحجم‌تر وزنِ طبیعیِ بیشتری می‌گیرند؛ سپس **انقباضِ بیزی** به‌سمتِ ۱ (معادلِ ۴ نمونه‌ی فرضیِ «تخمین درست است») تا داده‌ی کم عمداً محافظه‌کار باشد، و مهارِ نهاییِ β در [۰٫۲۵ , ۴].
- **پیش‌بینی**: `GET /api/predictions/` برایِ هر امتحانِ آینده‌ی فعال: `predicted = β × study_hours_remaining` + نیازِ روزانه (پیش‌بینی/روزهایِ باقی‌مانده) + ریسک نسبت به «ساعتِ آزادِ روزانه‌ی» همان کاربر از `StudyPlan` (high: ≥ کلِ ظرفیت، medium: ≥ ۷۵٪، low). کاربرِ بی‌تاریخچه: β=۱ یعنی همان تخمینِ دستی‌اش.
- **قراردادِ ترجمه**: مقدارهایِ risk/bias ماشین‌خوان‌اند؛ متنِ نمایشی فقط در فرانت‌اند (i18n.js) ساخته می‌شود — پس هیچ رشته‌ی gettextِ جدیدی در این قابلیت نیست و کاتالوگِ locale/en دست‌نخورده ماند.
- **مرزِ جداسازی**: ریاضیاتِ خالص (بدونِ ORM) بالای فایل، فقط دو تابعِ پایین به مدل‌ها دست می‌زنند (importِ داخلِ بدنه، الگویِ utils.py) — و هر دو رویِ `user` فیلتر می‌شوند (قرینه‌ی نکته‌ی ۶.۳ AI_CONTEXT).

**اجرا:** `python manage.py test planner -v 2` (از پوشه‌ی `backend`)

---

## ۲.۸ `backend/planner/throttles.py` — محدودسازیِ نرخِ مسیرهایِ احرازِ هویت (از 2026-09-10)

بستنِ سطحِ حمله‌ی «حدسِ رمزِ عبور» (brute-force): چرخش/ابطالِ توکنِ Refresh (2026-09-08) توکنِ *دزدیده‌شده* را از بازی خارج می‌کند، ولی کسی که دارد رمز را *حدس می‌زند* می‌تواند بی‌وقفه به login/register درخواست بزند — این کلاس همان فاصله را پر می‌کند.

- **`AuthBurstThrottle`** زیرکلاسِ `SimpleRateThrottle`ِ DRF با `scope = 'auth'`: نرخش از `REST_FRAMEWORK['DEFAULT_THROTTLE_RATES']['auth']` (یعنی `DJANGO_AUTH_THROTTLE_RATE`) خوانده می‌شود و کلیدِ شمارش = هویتِ IPِ درخواست (`get_ident` — پشتِ Nginx همان X-Forwarded-Forِ بازنویسی‌شده با IPِ واقعیِ کاربر).
- چرا کلاسِ سفارشی و نه `ScopedRateThrottle`ِ آماده؟ آن کلاس نرخ را از ویژگیِ `view.throttle_scope` می‌خواند که باید در هر ویو جدا ست شود (و فراموش‌کردنش بی‌صدا دفاع را خاموش می‌کند)؛ این کلاس scope را خودش سفت کرده تا روی ویوهایِ تابعیِ register/login فقط با یک `@throttle_classes([AuthBurstThrottle])` اعمال شود.
- `get_cache_key` مستقل از ویو ساخته می‌شود (وابسته به هیچ ویژگیِ view نیست) تا روی هر دو نوعِ ویو (تابعی/کلاسی) یکسان کار کند.
- رفتارِ پاسخ: درخواستِ بیش از سقف = ۴۲۹ با هدرِ `Retry-After` (ثانیه تا آزادشدنِ پنجره)؛ پنجره‌ی شمارشِ `X/min` دقیقاً ۶۰ ثانیه‌ی لغزان است. شمارنده‌ها در کشِ پیش‌فرضِ جنگو (LocMemCache) زندگی می‌کنند — هر فرایندِ جدا سقفِ خودش را دارد (جزئیاتِ صادقانه و راهِ حسابش در `05_deployment.md` بخشِ ۵.۹).
- در نقشه‌ی بزرگ‌ترِ محدودسازی: `register`/`login` فقط همین scopeِ 'auth' را می‌شمارند (جایگزینِ throttleهایِ پیش‌فرض با decorator)، درخواست‌هایِ بی‌احرازِ دیگر scopeِ `anon` (هر IP) و درخواست‌هایِ احراز‌شده scopeِ `user` (هر کاربر) را — نرخ‌هایِ هر سه از متغیرهایِ محیطی و همه با پیش‌فرضِ dev-safeِ 10000/min. `/api/health/` عمداً از همه‌ی آن‌ها معاف است (`@throttle_classes([])`).

---
---

*پایان بخش ۲ از ۵. بخش ۳ به صفحاتِ رابط کاربری (HTML) می‌پردازد.*
