# مستند فنی پروژه — بخش ۳ از ۴: صفحات رابط کاربری (HTML)

**پروژه: برنامه‌ریز هوشمند مطالعه (Smart Study Planner)**

> ادامه‌ی مستندسازیِ فایل‌به‌فایلِ پروژه. بخش‌های ۱ و ۲، بک‌اند (Django) را پوشش دادند؛
> این بخش به ۷ صفحه‌ی HTMLِ فرانت‌اند می‌پردازد — همان چیزی که کاربر در مرورگر می‌بیند.

---

## نکته‌ی مشترکِ همه‌ی صفحات

هیچ فریم‌ورکِ جاوااسکریپتی (React/Vue/Angular) استفاده نشده؛ هر صفحه یک فایلِ HTML خالص است که `styles.css` و `app.js` را لود می‌کند. یک الگوی مشترک در همه‌ی صفحاتِ محافظت‌شده (همه به‌جز ورود/ثبت‌نام) تکرار می‌شود:

```html
<body data-page="...">
    <div class="app-shell">
        <aside class="sidebar">...</aside>   <!-- ناوبریِ مشترک بین صفحات -->
        <main class="main-panel">
            <header class="topbar">...</header>   <!-- عنوان صفحه + خروج -->
            <div class="page-content">...</div>    <!-- محتوای مخصوصِ همان صفحه -->
        </main>
    </div>
</body>
```

ویژگیِ `data-page` روی تگِ `<body>` نکته‌ی فنیِ کلیدی است: در انتهای `app.js` یک «روترِ سبک» وجود دارد که بر اساسِ همین مقدار، تابعِ راه‌اندازیِ (`init...Page`) مخصوصِ همان صفحه را صدا می‌زند. توضیحِ کاملِ این روتر در بخش ۴ (توضیحِ `app.js`) آمده است.

---

## ۳.۱ `static/login.html`

**`data-page="login"`** → در روتر، تابعِ `handleLogin()` صدا زده می‌شود.

ساختار بسیار ساده است (فقط یک `auth-card` وسط‌چین، بدون سایدبار/تاپ‌بار، چون کاربرِ هنوز واردنشده به آن‌ها دسترسی ندارد):
- فرمِ `loginForm` با دو ورودی (`username`, `password`)
- یک `<div id="toast">` برای نمایشِ پیامِ خطا (مثلاً «نام کاربری یا رمز اشتباه است»)
- لینکی به `register.html` برای کاربرانِ بدونِ حساب

هنگامِ Submit، `handleLogin()` در `app.js` یک درخواستِ `POST` به `/api/auth/login/` می‌زند؛ در صورتِ موفقیت، توکنِ JWT در `localStorage` مرورگر ذخیره و کاربر به `index.html` (داشبورد) هدایت می‌شود.

---

## ۳.۲ `static/register.html`

**`data-page="register"`** → تابعِ `handleRegister()`.

مشابهِ صفحه‌ی ورود، با یک تفاوتِ مهم در تجربه‌ی کاربری: فیلدهای فرم عبارتند از `username`, `email` (اختیاری), `password`. نکته‌ی جالبِ این صفحه، **رفتارِ پویا بعد از ثبت‌نامِ موفق** است — دکمه‌ی `id="authActionBtn"` که در ابتدا نوعش `submit` است، بعد از موفقیت با جاوااسکریپت به یک دکمه‌ی معمولیِ «ورود به حساب» تبدیل می‌شود (متن و رفتارش عوض می‌شود، بدونِ نیاز به رفرشِ صفحه)، و پیامِ «حساب دارید؟» (`id="loginPrompt"`) مخفی می‌شود — چون دیگر لازم نیست.

---

## ۳.۳ `static/index.html` — داشبورد اصلی

**`data-page="dashboard"`** → تابعِ `initDashboardPage()` که خودش `loadDashboard()` را اجرا می‌کند (یک درخواستِ `GET` به `/api/dashboard/`).

این پیچیده‌ترین صفحه از نظرِ تعدادِ بخش‌هاست. محتوای `page-content` شاملِ:

1. **چهار کارتِ آماری** (`class="metrics-grid"`): تعدادِ درس‌ها، امتحان‌های ثبت‌شده، میانگینِ پیشرفت، هشدارهای فوری. هر کارت یک `<strong id="...">` دارد که مقدارش مستقیماً از پاسخِ API پر می‌شود (مثلاً `id="totalSubjects"` از `data.total_subjects`).
2. **پنلِ «پیشرفتِ درس‌ها»** (`id="progressList"`): نوارِ پیشرفتِ هر درس.
3. **پنلِ «تقویمِ مطالعه»** (`id="timelineList"`): فهرستِ امتحان‌های پیشِ‌رو، مرتب‌شده بر اساسِ نزدیک‌ترین تاریخ.
4. **پنلِ «تقسیم‌زمانِ پیشنهادی»** (`id="studyDistribution"`): نمودارِ میله‌ایِ CSSیِ سهمِ هر درس از هفته‌ی پیشِ‌رو.
5. **پنلِ «هشدارها و یادآوری‌ها»** (`id="alertsList"`): هشدارهای قرمز/زرد + یادآوریِ آبیِ برنامه‌ی امروز.

هر پنل در ابتدا یک `<p class="empty-state">` به‌عنوانِ حالتِ پیش‌فرض/بارگذاری دارد که با پاسخِ واقعیِ API جایگزین می‌شود. دکمه‌ی `id="refreshDashboard"` به کاربر اجازه می‌دهد دستی دوباره داده‌ها را بارگذاری کند.

---

## ۳.۴ `static/subjects.html` — مدیریت درس‌ها

**`data-page="subjects"`** → تابعِ `initSubjectsPage()`.

دو بخشِ اصلی:

1. **فرمِ «افزودنِ درسِ جدید»** (`id="subjectForm"`): فیلدهای `name`, `difficulty` (۱ تا ۵)، `target_score` (هدف‌نمره‌ی اختیاری)، `total_hours`، و `notes`. Submit آن به `POST /api/subjects/` می‌رود.
   - **نکته‌ی شفاف که باید مطرح شود:** فیلدهای `total_hours` و `notes` در فرم وجود دارند، ولی مدلِ `Subject` در بک‌اند چنین فیلدهایی ندارد؛ یعنی این دو مقدار فعلاً توسط سریالایزر نادیده گرفته می‌شوند و ذخیره نمی‌شوند. این یک محدودیتِ شناخته‌شده است (در `README.md` پروژه هم به آن اشاره شده) که در آینده یا باید فیلدِ متناظر به مدل اضافه شود، یا این ورودی‌ها از فرم حذف/بازتعریف شوند.
2. **جدولِ «فهرستِ درس‌ها»** (`id="subjectsTableBody"`): پنج ستون (نام، سختی، پیشرفت، هدف، ساعتِ باقی‌مانده). یک `<select id="subjectSort">` هم وجود دارد که با تغییرش، `loadSubjects()` دوباره با کلیدِ مرتب‌سازیِ جدید (الفبا/سختی/پیشرفت) صدا زده می‌شود — این مرتب‌سازی کاملاً **سمتِ کلاینت** انجام می‌شود (سرور همیشه فهرستِ خام را می‌فرستد).

---

## ۳.۵ `static/exams.html` — مدیریت امتحان‌ها

**`data-page="exams"`** → تابعِ `initExamsPage()`.

1. **فرمِ «ثبتِ امتحان یا ددلاین»** (`id="examForm"`): یک `<select id="examSubject">` که گزینه‌هایش پویا و از طریقِ `populateSubjectSelect()` پر می‌شوند (فهرستِ درس‌های خودِ کاربر)، به‌همراه `exam_date`, `chapters_remaining`, `study_hours_remaining`, و یک یادداشتِ اختیاری. Submit به `POST /api/exams/` می‌رود.
   - **نکته‌ی شفاف:** یک ورودیِ مخفی (`id="examId"`) و یک دکمه‌ی «انصراف» (`onclick="resetExamForm()"`) در فرم وجود دارند که برای یک قابلیتِ **«ویرایشِ امتحان»** در نظر گرفته شده بودند، ولی تابعِ `resetExamForm()` فعلاً در `app.js` پیاده‌سازی نشده است. یعنی این بخش یک قابلیتِ نیمه‌کاره/آماده‌برای‌آینده است، نه یک باگِ فعال (چون دکمه‌اش هم با `style="display: none;"` مخفی است).
2. **جدولِ «فهرستِ امتحان‌ها»** (`id="examTableBody"`): پنج ستون شاملِ درس، تاریخ (با تعدادِ روزهای باقی‌مانده در زیرش)، ساعتِ باقی‌مانده، درجه‌ی اهمیت (که با ایموجیِ رنگی 🔴🟡🟢 نمایش داده می‌شود)، و وضعیت. یک `<select id="examFilter">` هم برای نمایشِ «فقط امتحان‌های آینده» یا «همه» وجود دارد.

---

## ۳.۶ `static/study_plan.html` — برنامه‌ی مطالعه‌ی هوشمند

**`data-page="study-plan"`** → تابعِ `initStudyPlanPage()`.

سه پنل:

1. **«بازه‌ی زمانی»**: دو دکمه‌ی تغییرِ نما (`data-range="daily"` و `data-range="weekly"`) که مستقیماً همان مقدار به‌عنوانِ پارامترِ `?range=` در `GET /api/study-plan/` فرستاده می‌شود. کنارش، یک خلاصه‌ی آماری (کلِ ساعتِ توصیه‌شده، درس‌های در اولویت، میانگینِ روزانه) نمایش داده می‌شود.
2. **«ساعتِ آزادِ روزانه»**: یک فرمِ کوچک (`id="dailyHoursForm"`) که با Submit شدن، به `POST /api/study-plan/generate/` می‌رود تا تنظیماتِ کاربر به‌روزرسانی و برنامه دوباره ساخته شود.
3. **«جزئیاتِ برنامه»** (`id="planContainer"`): هر بلوکِ نمایشی، یا یک روزِ تنها یا یک **بازه‌ی ادغام‌شده‌ی چند روزِ هم‌برنامه** است (مثل «امروز تا پنج‌شنبه»)؛ توضیحِ کاملِ چرایی این ادغام در بخشِ ۲ (توضیحِ `format_plan_for_frontend`) آمده است.

---

## ۳.۷ `static/study_log.html` — ثبت مطالعه

**`data-page="study-log"`** → تابعِ `initStudyLogPage()`.

این صفحه، آخرین و مکمل‌ترین حلقه‌ی سیستم است: بدونِ آن، تنها راهِ افزایشِ «پیشرفتِ» یک درس، دستورِ `seed_demo_data` بود؛ با این صفحه، کاربر واقعاً می‌تواند مطالعه‌ی روزانه‌اش را ثبت کند.

1. **فرمِ «امروز چقدر خواندی؟»** (`id="studyLogForm"`): یک `<select id="logExam">` که با `populateExamSelectForLog()` پر می‌شود و برای هر امتحان، ساعتِ باقی‌مانده‌اش را هم در متنِ گزینه نشان می‌دهد (مثلاً «مهندسی اینترنت — ۲۰۲۶-۰۷-۲۱ (۸.۴ ساعت باقی‌مانده)»)؛ به‌همراه `date` (پیش‌فرض: امروز)، `hours_studied`، و یادداشتِ اختیاری. Submit به `POST /api/study-logs/` می‌رود که خودکار ساعتِ باقی‌مانده‌ی امتحانِ مربوطه را هم کم می‌کند (منطقِ آن در `StudyLog.save()`، بخشِ ۱).
2. **جدولِ «تاریخچه‌ی مطالعه»** (`id="studyLogTableBody"`): هر ردیف یک دکمه‌ی «حذف» دارد. نکته‌ی فنی: چون حذفِ یک گزارش، ساعتِ کسرشده را به امتحان **برنمی‌گرداند** (تصمیمِ طراحیِ عمدی برای سادگی)، پیش از حذف یک `confirm()` با متنِ صریح («ساعتِ باقی‌مانده‌ی امتحان دوباره برنمی‌گردد») از کاربر گرفته می‌شود.

---

*پایان بخش ۳ از ۴. بخش ۴ به منطقِ جاوااسکریپتی (`app.js`) و طراحیِ بصری (`styles.css`) می‌پردازد.*
