# TODO.md

> تمامِ موارد این فایل مستقیماً از وضعیتِ فعلیِ کد/کامنت‌ها/`README.md` استخراج شده‌اند (نه از حدس). هرجا منبعِ استنتاج مشخص است، در پرانتز ذکر شده.

---

## Bugs (ناهماهنگی‌های فعلیِ تأییدشده)

> باگ‌های ثبت‌شده‌ی این بخش به‌مرور رفع شده‌اند: فیلدهای یتیمِ فرمِ درس و ارجاعِ تعریف‌نشده‌ی `resetExamForm` در 2026-08-25، و فیلدِ یتیمِ `note` در فرمِ امتحان در 2026-08-29 (همراه با قابلیتِ «ویرایشِ امتحان») — شرحِ کامل در `CHANGELOG.md`. در حالِ حاضر باگِ تأییدشده‌ی بازی در این بخش نیست.


## High Priority

> آیتمِ «افزودنِ تست‌های خودکار» در 2026-08-29 انجام شد: ۶۲ تست در `backend/planner/tests.py` (هفت کلاس: Auth/Subject/Exam/StudyLog/StudyPlan/Dashboard/الگوریتم) + رفعِ باگِ کشف‌شده در همان کار (خطای ۵۰۰ نامِ درسِ تکراری — شرح در `CHANGELOG.md`). در حالِ حاضر آیتمِ بازیِ دیگری در این بخش نیست.

## Medium Priority

- **بازگرداندنِ ساعت هنگامِ حذفِ `StudyLog`**: در حالِ حاضر حذفِ یک گزارشِ مطالعه، مقدارِ کسرشده را به `exam.study_hours_remaining` برنمی‌گرداند. این محدودیت آگاهانه و مستند‌شده است (`README.md` بخشِ ۱۱)، نه یک باگِ فراموش‌شده؛ ولی اگر رفعش بخواهد انجام شود، باید متدِ `delete()` روی مدلِ `StudyLog` بازنویسی شود (مشابهِ الگویِ `save()` که همین الان روی همین مدل پیاده‌سازی شده).
- **محدودیتِ یکتاییِ `StudyPlan.user` در سطحِ دیتابیس**: فیلدِ `user` روی مدلِ `StudyPlan` یک `ForeignKey` معمولی است (نه `unique=True` یا `OneToOneField`)، در حالی که کدِ `views.py` با الگویِ `get_or_create` فرض می‌کند هر کاربر فقط یک رکورد دارد. تحتِ Race Condition (دو درخواستِ هم‌زمان از یک کاربر) نظراً می‌شود بیش از یک رکورد ساخته شود. راه‌حل: افزودنِ `unique=True` (یا تبدیل به `OneToOneField`) + یک Migrationِ جدید. *(منبع: `backend/planner/models.py`)*
- **افزودنِ Pagination به Endpointهای فهرستی**: هیچ‌کدام از `REST_FRAMEWORK` در `settings.py` یا ViewSetهای مربوطه، تنظیماتِ صفحه‌بندی ندارند؛ `/api/subjects/`, `/api/exams/`, `/api/study-logs/` همیشه کاملِ نتایج را برمی‌گردانند. با افزایشِ تعدادِ رکوردها، این می‌تواند مشکلِ کارایی ایجاد کند. *(منبع: `backend/backend/settings.py`)*
- **بازبینیِ تنظیماتِ امنیتی پیش از استقرار**: `DEBUG=True`, `SECRET_KEY` هاردکدشده در `settings.py`, و `CORS_ALLOW_ALL_ORIGINS=True` همگی مناسبِ محیطِ توسعه‌اند، نه Production. *(منبع: `README.md` بخشِ ۱۱، `backend/backend/settings.py`)*

## Low Priority

- **پشتیبانیِ چندزبانی**: رابطِ کاربری در حالِ حاضر فقط فارسی است؛ هیچ زیرساختِ i18n در فرانت‌اند یا بک‌اند وجود ندارد. *(منبع: `README.md` بخشِ ۱۱)*
- **سخت‌ترکردنِ تنظیماتِ JWT برای Production**: `ROTATE_REFRESH_TOKENS=False` و `BLACKLIST_AFTER_ROTATION=False` در `SIMPLE_JWT` (یعنی توکنِ دزدیده‌شده تا پایانِ عمرش معتبر می‌ماند). *(منبع: `backend/backend/settings.py`)*
- **آسیب‌پذیریِ XSS در رندرِ فرانت‌اند (باقی‌مانده)**: تابعِ `escapeHtml` در 2026-08-29 به `app.js` اضافه شد و `renderExams` (نامِ درس + یادداشتِ امتحان) الان escape می‌کند؛ ولی `renderSubjects`/`renderStudyLogs`/`renderDashboard` همچنان داده‌ی API را بدونِ escapeکردن مستقیم در `innerHTML` می‌گذارند — نامِ درسی حاویِ HTML/JS (مثل `<img onerror=...>`) اجرا می‌شود. فعلاً فقط Self-XSS است (هر کاربر فقط داده‌ی خودش را می‌بیند)، ولی برای استقرارِ واقعی باید با همان تابعِ `escapeHtml` اصلاح شوند. *(منبع: تحلیلِ عمیقِ 2026-08-25، `static/app.js`)*
- **نبودِ هدایت به صفحه‌ی ورود هنگامِ انقضای توکن**: فرانت‌اند فقط access token را ذخیره می‌کند، refresh token را دور می‌ریزد و پاسخِ 401 هیچ‌گاه به `login.html` هدایت نمی‌کند؛ بعد از انقضای توکنِ دسترسی (۱ روز)، همه‌ی صفحات فقط Toast خطا نشان می‌دهند. راه‌حل: ذخیره‌ی refresh token + پیاده‌سازیِ منطقِ تمدید/هدایت در `apiRequest`. *(منبع: تحلیلِ عمیقِ 2026-08-25، `static/app.js`)*
- **نبودِ تراکنشِ اتمیک در `StudyLog.save()`**: کسرِ ساعتِ امتحان با `select_for_update`/`transaction.atomic` محافظت نشده؛ درخواست‌های هم‌زمان می‌توانند کسر را از دست بدهند (Lost Update). برای تک‌کاربره/SQLite فعلاً بحرانی نیست، ولی پیش از PostgreSQL چندکاربره باید اصلاح شود. *(منبع: تحلیلِ عمیقِ 2026-08-25، `backend/planner/models.py`)*

## Improvements

- **جایگزینیِ SQLite با PostgreSQL** اگر پروژه قرار است چندکاربره/Production شود (SQLite برای نوشتنِ هم‌زمانِ زیاد مناسب نیست). *(منبع: `README.md` بخشِ ۴.۱ و ۱۱)*
- **افزودنِ یک مؤلفه‌ی یادگیریِ آماری/ماشینی واقعی**: طبقِ `README.md` بخشِ ۱۱، محدودیتِ اولیه‌ی پروژه اجازه‌ی استفاده از کتابخانه‌هایی مثلِ Scikit-learn را می‌داد، ولی فعلاً چنین چیزی پیاده نشده؛ الگوریتمِ فعلی کاملاً قانون‌محور (Rule-based) است. ایده‌ی مطرح‌شده در همان بخش: یک مدلِ رگرسیونیِ ساده که بر اساسِ داده‌های تاریخیِ `StudyLog` هر کاربر، «زمانِ واقعیِ موردنیاز» را پیش‌بینی کند (به‌جایِ اتکایِ صرف به مقدارِ دستیِ `study_hours_remaining`).

## Future Ideas

> این بخش عمداً کوتاه نگه داشته شده: طبقِ قانونِ مستندسازیِ این پروژه، فقط ایده‌هایی که مستقیماً از خودِ فایل‌های پروژه (نه از تاریخچه‌ی گفتگوهای قبلی) قابل‌استنتاج‌اند اینجا ثبت می‌شوند.

- گسترشِ همان ایده‌ی رگرسیونِ بخشِ Improvements به سطحِ «پیش‌بینیِ ریسکِ عقب‌افتادن از برنامه» برای هر درس، بر مبنایِ نسبتِ پیشرفتِ واقعی به زمانِ باقی‌مانده.

---

## نکته برایِ AI بعدی

اگر هنگامِ کار روی یکی از موارد بالا اطلاعاتِ جدیدی کشف کردی که این فایل را منسوخ می‌کند (مثلاً یک باگ رفع شد یا یک تصمیم گرفته شد)، این فایل را همان لحظه به‌روزرسانی کن؛ این فایل باید همیشه بازتابِ وضعیتِ *فعلیِ* پروژه باشد، نه تاریخچه (تاریخچه در `CHANGELOG.md` است).
