# TODO.md

> تمامِ موارد این فایل مستقیماً از وضعیتِ فعلیِ کد/کامنت‌ها/`README.md` استخراج شده‌اند (نه از حدس). هرجا منبعِ استنتاج مشخص است، در پرانتز ذکر شده.

---

## Bugs (ناهماهنگی‌های فعلیِ تأییدشده)

> باگ‌های ثبت‌شده‌ی این بخش به‌مرور رفع شده‌اند: فیلدهای یتیمِ فرمِ درس و ارجاعِ تعریف‌نشده‌ی `resetExamForm` در 2026-08-25، و فیلدِ یتیمِ `note` در فرمِ امتحان در 2026-08-29 (همراه با قابلیتِ «ویرایشِ امتحان») — شرحِ کامل در `CHANGELOG.md`. در حالِ حاضر باگِ تأییدشده‌ی بازی در این بخش نیست.

## High Priority

> آیتمِ «افزودنِ تست‌های خودکار» در 2026-08-29 انجام شد: ۶۲ تست در `backend/planner/tests.py` (هفت کلاس: Auth/Subject/Exam/StudyLog/StudyPlan/Dashboard/الگوریتم) + رفعِ باگِ کشف‌شده در همان کار (خطای ۵۰۰ نامِ درسِ تکراری — شرح در `CHANGELOG.md`). در حالِ حاضر آیتمِ بازیِ دیگری در این بخش نیست.

## Medium Priority

> آیتمِ «بازگرداندنِ ساعت هنگامِ حذفِ `StudyLog`» در 2026-09-03 انجام شد: فیلدِ `hours_deducted` (Migrationِ `0007`، همراهِ Backfill برای گزارش‌های قدیمی) عددِ دقیقِ کسرشده را نگه می‌دارد و متدِ `delete()` رویِ همان مدل آن را اتمیک به امتحان برمی‌گرداند — شرحِ کامل در `CHANGELOG.md`.
> آیتمِ «محدودیتِ یکتاییِ `StudyPlan.user` در سطحِ دیتابیس» در 2026-09-05 انجام شد: فیلدِ `user` به `OneToOneField` تبدیل شد (Migrationِ `0008`، همراهِ پاکسازیِ رکوردهایِ تکراریِ احتمالیِ قدیمی) و POST دوباره به `/api/study-plan/` حالا پاسخِ ۴۰۰ خوانا می‌گیرد — شرحِ کامل در `CHANGELOG.md`.
> آیتمِ «افزودنِ Pagination به Endpointهای فهرستی» در 2026-09-06 انجام شد: کلاسِ `OptionalPageNumberPagination` رویِ سه ViewSet درس/امتحان/گزارش — صفحه‌بندی فقط با `?page=`/`?page_size=` فعال می‌شود (بدونِ آنها همان لیستِ کاملِ قبلی، پس فرانت‌اندِ فعلی دست‌نخورده ماند)؛ سقفِ `page_size` صد رکورد است و ۹ تستِ جدید (مجموع ۷۹) پوششش می‌دهد — شرحِ کامل در `CHANGELOG.md`.
> آیتمِ «نبودِ هدایت به صفحه‌ی ورود هنگامِ انقضای توکن» در 2026-09-06 انجام شد: مسیرِ `POST /api/auth/refresh/` (TokenRefreshView) + ذخیره‌ی refresh token در `handleLogin` + تمدیدِ بی‌صدا و تلاشِ مجدد در `apiRequest` هنگامِ 401 + هدایت به `login.html?expired=1` وقتی تمدید ممکن نباشد — شرحِ کامل در `CHANGELOG.md`.
> آیتمِ «بازبینیِ تنظیماتِ امنیتیِ پیش از استقرار» در 2026-09-06 انجام شد: `SECRET_KEY`/`DEBUG`/`ALLOWED_HOSTS`/`CORS` از متغیرهایِ محیطی خوانده می‌شوند (پیش‌فرض‌ها همانِ توسعه؛ بدونِ هیچ متغیری هیچ چیز عوض نمی‌شود) + سپرِ راه‌اندازیِ Production — ۷ تستِ جدید (مجموع ۸۹) — شرحِ کامل در `CHANGELOG.md`.
> در حالِ حاضر آیتمِ بازیِ دیگری در این بخش نیست.


## Low Priority
> آیتمِ «آسیب‌پذیریِ XSS در رندرِ فرانت‌اند» در 2026-09-04 به‌طورِ کامل رفع شد: همه‌ی رندرکننده‌های `app.js` (`renderSubjects`/`renderStudyLogs`/`renderDashboard`/`renderPlan`/`renderExams`) داده‌ی متنی را با `escapeHtml` پاک‌سازی می‌کنند — شرح در `CHANGELOG.md`.
> آیتمِ «سخت‌ترکردنِ تنظیماتِ JWT برای Production» در 2026-09-08 انجام شد: چرخش و لیستِ سیاهِ توکنِ Refresh فعال شد (هر تمدید توکنِ تازه صادر می‌کند و قبلی بلافاصله باطل می‌شود) + خروجِ سرور-محور با `POST /api/auth/logout/` (توکنِ خروج‌کرده دیگر قابلِ تمدید نیست) — ۷ تستِ جدید (مجموع ۹۶)؛ شرحِ کامل در `CHANGELOG.md`.
> آیتمِ «نبودِ قفلِ هم‌زمانیِ (`select_for_update`) در `StudyLog.save()`/`delete()`» در 2026-09-09 انجام شد: سطرِ امتحان پیش از کسر/بازگشتِ ساعت داخلِ تراکنش قفل و «تازه» خوانده می‌شود (مبنایِ محاسبه = مقدارِ لحظه‌ایِ دیتابیس، نه snapshotِ حافظه — Lost Update بسته شد؛ در SQLite قفل بی‌اثر ولی خواندنِ تازه مؤثر است؛ پیش‌نیازِ PostgreSQL) — ۷ تستِ جدید (مجموع ۱۰۳)؛ شرحِ کامل در `CHANGELOG.md`.

> آیتمِ «پشتیبانیِ چندزبانی» در 2026-09-09 انجام شد: فارسی/انگلیسی در هر دو لایه — بک‌اند با `LocaleMiddleware` + هدرِ `Accept-Language` + کاتالوگِ `locale/en` (msgid = متنِ فارسی؛ django.mo کامپایل‌شده commit شده) و فرانت‌اند با `static/i18n.js` (دیکشنریِ ۱۷۴ کلیدی + `t()`) + دکمه‌ی تعویضِ زبان در هر ۷ صفحه. بدونِ انتخابِ زبان، رفتارِ قبلی (فارسی) دست‌نخورده است — ۱۵ تستِ جدید (مجموع ۱۳۱)؛ شرحِ کامل در `CHANGELOG.md`.

## Improvements

> آیتمِ «جایگزینیِ SQLite با PostgreSQL» در 2026-09-09 انجام شد (به‌صورتِ قابلیت): موتورِ دیتابیس از متغیرِ محیطیِ `DATABASE_URL` خوانده می‌شود — بدونِ متغیر همان SQLiteِ توسعه، با `postgres://...` موتورِ PostgreSQL (+ درایورِ psycopg در requirements.txt)؛ قفلِ FOR UPDATE هم در PostgreSQL فعال است و کلِ ۱۱۶ تست روی هر دو موتور سبزند. «تعویضِ» واقعی = کارِ استقرار: ست‌کردنِ متغیر + انتقالِ داده (دستورِ `dumpdata`/`loaddata` در `README.md` بخشِ ۱۰) — شرحِ کامل در `CHANGELOG.md`.
> آیتمِ «افزودنِ یک مؤلفه‌ی یادگیریِ آماری/ماشینی واقعی» در 2026-09-09 انجام شد: `backend/planner/ml.py` — مدلِ کالیبراسیونِ «تخمینِ ساعتیِ کاربر ↔ واقعیتِ ثبت‌شده» با رگرسیونِ کمینه‌ی مربعاتِ از مبدأ + انقباضِ بیزی + مهار، آموزش‌گرفته فقط از امتحان‌هایِ گذشته‌ی خودِ کاربر (بدونِ نشتِ آینده)؛ endpoint جدیدِ `GET /api/predictions/` (ساعتِ واقعیِ پیش‌بینی‌شده + نیازِ روزانه + ریسکِ عقب‌افتادن نسبت به ساعتِ آزادِ خودِ کاربر) + پنلِ «پیش‌بینیِ هوشمند» در صفحه‌ی برنامه؛ پایتونِ خالص بدونِ وابستگیِ جدید — ۲۱ تستِ جدید (مجموع ۱۵۲) — شرحِ کامل در `CHANGELOG.md`.
> آیتمِ «امنیتِ حسابِ کاربری: اجرایِ سیاستِ رمز + مسیرِ تغییرِ رمز» در 2026-09-10 انجام شد: تا این تاریخ `AUTH_PASSWORD_VALIDATORS` در settings تعریف شده بود ولی هیچ‌جا صدا زده نمی‌شد (ثبت‌نام با رمزِ «12345678» ممکن بود) و کاربرِ لاگین‌شده هم راهی برایِ عوض‌کردنِ رمز نداشت. حالا ثبت‌نام و تغییرِ رمز هر دو از همان اعتبارسنج‌ها می‌گذرند (پیام‌ها مالِ جنگو — fa/en خودکار) و `POST /api/auth/password/` (رمزِ فعلی + رمزِ جدید) همه‌ی نشست‌ها — توکن‌هایِ Refreshِ برجسته و حتی توکن‌هایِ دسترسیِ قبل ازِ تغییر (کلاسِ `SessionInvalidatingJWTAuthentication` + `UserSecurityProfile`) — را باطل و جفتِ توکنِ تازه برایِ همین دستگاه صادر می‌کند؛ دکمه/مودالِ فرانت‌اند با app.js تزریق می‌شود — شرحِ کامل در `CHANGELOG.md`.

> آیتمِ «استقرارِ واقعیِ Production» در 2026-09-10 به‌صورتِ «آمادگیِ کاملِ کد + راهنما» انجام شد (گامِ بعدیِ پیشنهادیِ `HANDOFF.md` بعد از خالی‌شدنِ TODO): محدودسازیِ نرخِ سه‌scope (`anon`/`user`/`auth` — دفاعِ brute-force رویِ register/login) + `GET /api/health/` عمومیِ معاف از throttle برایِ مانیتورینگ/Load Balancer + چهار تنظیمِ امنیتیِ شرطیِ TLS (`SECURE_SSL_REDIRECT`/کوکی‌هایِ Secure/HSTS/هدرِ پروکسی) — همه با متغیرهایِ محیطی و پیش‌فرضِ خاموش/dev-safe + مستندِ گام‌به‌گامِ `05_deployment.md` (PostgreSQL + گنیکورن + systemd + Nginx + Certbot) — ۳۳ تستِ جدید (مجموع ۱۸۵) — شرحِ کامل در `CHANGELOG.md`. «اجرایِ» راهنما رویِ سرورِ واقعی کارِ عملیاتیِ انسانی است (خارج از ریپو)؛ در حالِ حاضر آیتمِ بازیِ دیگری در این بخش نیست.

## Future Ideas

> این بخش عمداً کوتاه نگه داشته شده: طبقِ قانونِ مستندسازیِ این پروژه، فقط ایده‌هایی که مستقیماً از خودِ فایل‌های پروژه (نه از تاریخچه‌ی گفتگوهای قبلی) قابل‌استنتاج‌اند اینجا ثبت می‌شوند.

> ایده‌ی «پیش‌بینیِ ریسکِ عقب‌افتادن» در 2026-09-09 در دلِ همان مؤلفه‌ی ML تحقق یافت (`GET /api/predictions/` برای هر امتحانِ آینده، نسبت به ساعتِ آزادِ روزانه‌ی کاربر) — بخشِ Future Ideas فعلاً خالی است.

---

## نکته برایِ AI بعدی

اگر هنگامِ کار روی یکی از موارد بالا اطلاعاتِ جدیدی کشف کردی که این فایل را منسوخ می‌کند (مثلاً یک باگ رفع شد یا یک تصمیم گرفته شد)، این فایل را همان لحظه به‌روزرسانی کن؛ این فایل باید همیشه بازتابِ وضعیتِ *فعلیِ* پروژه باشد، نه تاریخچه (تاریخچه در `CHANGELOG.md` است).
