# CHANGELOG.md

> این تاریخچه بر اساسِ شواهدِ واقعیِ موجود در پروژه نوشته شده: تاریخ‌های بخشِ اول مستقیماً از هدرِ خودکارِ فایل‌های Migration (`# Generated by Django ... on ...`) خوانده شده‌اند. برای تغییراتِ بعد از آخرین Migration، **هیچ تاریخِ دقیقی در پروژه ثبت نشده** (نه در Git، نه در کد)؛ این پروژه فاقدِ مخزنِ Gitِ فعال با تاریخچه‌ی Commit در این نسخه از فایل‌هاست، پس آن تغییرات بدونِ تاریخ و فقط به‌ترتیبِ منطقی فهرست شده‌اند. اگر مخزنِ Git با `git log` در دسترس بود، این بخش باید با تاریخ‌های واقعیِ Commit جایگزین/تکمیل شود.

---

## بخشِ تاریخ‌دار (بر اساسِ فایل‌های Migration)

### 2026-04-20 — `0001_initial`
**Added**
- ساختِ اولیه‌ی مدل‌های `Exam`, `Subject`, `StudyPlan` و کلیدهای خارجیِ اولیه.

### 2026-04-22 — `0002_alter_subject_unique_together_subject_target_grade_and_more`
**Added**
- فیلدِ `target_grade` به مدلِ `Subject` (هدف‌نمره‌ی اختیاری).

**Changed**
- حذفِ محدودیتِ یکتاییِ قبلیِ `Subject` (برداشتن و آماده‌سازی برای تعریفِ مجدد در Migrationِ بعدی).
- اصلاحِ اعتبارسنجی/متنِ راهنمای فیلدهای `chapters_remaining`, `exam_date`, `study_hours_remaining` (روی `Exam`) و `daily_available_hours`, `plan_data` (روی `StudyPlan`).
- تغییرِ نوعِ فیلدِ `difficulty` روی `Subject`.

**Removed**
- فیلدِ قدیمیِ `target_score` از `Subject` (جایگزین‌شده با `target_grade`).

### 2026-04-24 — `0003_remove_studyplan_exam_alter_subject_name_and_more`
**Removed**
- فیلدِ `exam` از مدلِ `StudyPlan` (رابطه‌ی مستقیمِ StudyPlan↔Exam که در طراحیِ اولیه بود، حذف شد).

**Changed**
- بازتعریفِ `unique_together` روی `Subject` به `('user', 'name')`.

### 2026-06-10 — `0004_alter_exam_chapters_remaining_and_more`
**Added**
- مدلِ جدیدِ `StudyLog` (فیلدهای `date`, `hours_studied`, `notes`, کلیدهای خارجی به `user` و `exam`).

**Changed**
- افزودنِ مقدارِ پیش‌فرض (`default=0` / `default=0.0` / `default=3`) به فیلدهای `chapters_remaining`, `study_hours_remaining` (روی `Exam`) و `difficulty` (روی `Subject`).

### 2026-08-25 — `0005_subject_notes` (رفعِ باگ‌های ثبت‌شده در `TODO.md`)
**Added**
- فیلدِ `notes` (یادداشتِ کوتاهِ اختیاری) به مدلِ `Subject` + Migrationِ متناظر. این فیلد قبلاً در فرمِ `subjects.html` جمع‌آوری می‌شد ولی در مدل وجود نداشت و بی‌صدا دور ریخته می‌شد (فیلد یتیم).
- تابعِ `resetExamForm()` در `app.js`: فرمِ امتحان را به حالتِ «ثبتِ جدید» برمی‌گرداند (پاک‌کردنِ فیلدها، خالی‌کردنِ `examId`، مخفی‌کردنِ دکمه‌ی «انصراف»). زیرساختِ آماده برای قابلیتِ «ویرایشِ امتحان».
- کلاسِ `.text-muted` در `styles.css` (متنِ کم‌رنگِ کمکی داخل جدول‌ها) — این کلاس قبلاً در `app.js` استفاده می‌شد ولی در CSS تعریف نشده بود (کلاسِ بی‌اثر).
- تست‌های دودِ خارج از مخزنِ اصلی (backend: 22 مورد، frontend: 11 مورد) برای اعتبارسنجیِ همین تغییرات؛ با محیطِ Django واقعی (نسخه‌ی 5.2.17 در venv) اجرا شدند.

**Changed**
- نمایشِ یادداشتِ درس در جدولِ فهرستِ درس‌ها (`renderSubjects`): به‌صورتِ زیرنویسِ کم‌رنگ زیرِ نامِ درس، بدونِ افزودنِ ستونِ جدید.

**Removed**
- فیلدِ «حجم کل ساعات مطالعه» (`total_hours`) از فرمِ `subjects.html` و از payloadِ `handleSubjectForm` در `app.js`. دلیلِ تصمیم: مفهومِ «کل ساعت» در این سیستم در سطحِ امتحان (`Exam.study_hours_remaining`) دنبال می‌شود و فیلدِ محاسباتیِ `total_hours` سریالایزر هم از همان‌جا مشتق می‌شود؛ یک ورودیِ دستیِ هم‌نام با معنای متفاوت فقط داده‌ی گمراه‌کننده تولید می‌کرد. ضمناً این فیلد `required` بود — یعنی کاربر مجبور بود فیلدی را پر کند که دور ریخته می‌شد.

**Fixed**
- باگ ۱ِ `TODO.md` (فیلدهای یتیمِ فرمِ درس): `notes` به مدل اضافه شد و واقعاً ذخیره می‌شود؛ `total_hours` حذف شد (شرحِ تصمیم در بخشِ Removed).
- باگ ۲ِ `TODO.md` (ارجاعِ تابعِ تعریف‌نشده): `exams.html` دکمه‌ی مخفیِ «انصراف» را به `onclick="resetExamForm()"` وصل می‌کرد ولی این تابع تعریف نشده بود (ReferenceErrorِ بالقوه). تابع تعریف شد.
- حرفِ سرگردانِ «آ» که به‌اشتباه قبل از اولین `<option>` کشویِ مرتب‌سازی در `subjects.html` تایپ شده بود و در UI رندر می‌شد.

---

### 2026-08-29 — `0006_exam_notes` (قابلیتِ «ویرایشِ امتحان» + رفعِ فیلدِ یتیمِ `note`)
**Added**
- قابلیتِ «ویرایشِ امتحان» در فرانت‌اند (تکمیلِ آیتمِ High Priorityِ `TODO.md`): دکمه‌ی «ویرایش» روی هر ردیفِ جدولِ امتحان‌ها (`renderExams` + `startExamEdit`)، تشخیصِ حالتِ ویرایش در `handleExamForm` (با `examId` پر، درخواست به `PATCH /api/exams/{id}/` می‌رود)، بازگشتِ کامل به حالتِ ثبت با `resetExamForm` (متنِ دکمه‌ی submit و عنوانِ پنل هم برمی‌گردند) و اتصالِ دکمه‌ی «انصراف از ویرایش» با `addEventListener` (به‌جای onclick درون‌خطی).
- فیلدِ `notes` (یادداشتِ اختیاری) به مدلِ `Exam` + Migrationِ `0006_exam_notes` و افزودن به `ExamSerializer` — رفعِ فیلدِ یتیمِ `note` در فرمِ امتحان که مقدارش بی‌صدا دور ریخته می‌شد. یادداشت در جدول به‌صورتِ زیرنویسِ کم‌رنگ زیرِ نامِ درس (همان الگوی جدولِ درس‌ها) و در placeholder نمونه («میان‌ترم؛ سالن ۲…») نمایش داده می‌شود.
- متدِ امنیتیِ `perform_update` روی `ExamViewSet`: اگر در ویرایش، درسِ امتحان عوض شود، درسِ جدید باید متعلق به همان کاربر باشد (قبلاً فقط `perform_create` این را بررسی می‌کرد و مسیرِ PATCH باز بود).
- تابعِ کمکیِ `escapeHtml` در `app.js` + `apiPatch` (Wrapper جدید روی `apiRequest`).
- تست‌های دودِ خارج از مخزن برای همین تغییرات: backend ۲۴ مورد (شاملِ ۴ سناریوی امنیتی: 403 برای درسِ دیگری، 404 برای امتحانِ دیگری) و frontend ۵۰ مورد (شاملِ رندرِ دکمه‌ی ویرایش، پرشدنِ فرم، PATCH/POST، بازگشت از حالتِ ویرایش و escapeشدنِ XSS).

**Changed**
- `exams.html`: کلاس‌های CSS فرمِ امتحان با `styles.css` هم‌راستا شدند — قبلاً `form-group`/`full-width`/`btn btn-primary`/`btn btn-secondary` بودند که **هیچ‌کدام در CSS تعریف نشده بودند** و فرمِ امتحان عملاً بی‌استایل رندر می‌شد؛ حالا همان کلاس‌های استانداردِ بقیه‌ی فرم‌ها (`form-field`/`form-field--full`/`primary-button`/`ghost-button`) استفاده می‌شوند.
- ستونِ «عملیات» به جدولِ امتحان‌ها اضافه شد (colspan حالتِ خالی از ۵ به ۶).
- در `renderExams`، نامِ درس و یادداشتِ امتحان با `escapeHtml` رندر می‌شوند (اولین گامِ بستنِ آسیب‌پذیریِ XSS ثبت‌شده در `TODO.md`؛ بقیه‌ی توابعِ رندر همچنان باقی‌اند).

**Fixed**
- فیلدِ یتیمِ `note` در فرمِ امتحان (`name="note"` → حالا `name="notes"` و واقعاً ذخیره می‌شود) — همان کلاسِ باگی که در 2026-08-25 برای فرمِ درس رفع شد.
- فرمِ امتحان بدونِ استایل: کلاس‌های فرم در `exams.html` با کلاس‌های تعریف‌شده در `styles.css` یکی نبودند (شرح در Changed).

---

### 2026-08-29 — بازطراحیِ دکمه‌های عملیات (ویرایش / حذف / انصراف از ویرایش)

**Added**
- کلاس‌های دکمه‌ی اختصاصیِ «سطحِ سفید» در `styles.css`: `.btn-edit` (قرصیِ آبی با آیکونِ مداد — دکمه‌ی «ویرایش» در ردیف‌های جدولِ امتحان‌ها)، `.btn-cancel-edit` (قرصیِ قرمز با آیکونِ × — «انصراف از ویرایش» در فرمِ امتحان) و `.btn-danger-sm` (قرصیِ قرمز با آیکونِ سطلِ زباله — «حذف» در جدولِ گزارش‌های مطالعه). آیکون‌ها SVG درون‌خطی‌اند که با CSS `mask` رندر می‌شوند تا رنگشان همیشه با رنگِ متنِ دکمه (`currentColor`) و حالتِ hover هماهنگ بماند.
- حالت‌های کاملِ hover / active / focus-visible برای هر سه دکمه: بالا آمدنِ ملایم (translateY)، سایه‌ی رنگیِ هم‌خانواده‌ی رنگِ دکمه و outlineِ کیبوردی برای دسترس‌پذیری.
- قانونِ `.panel .ghost-button:not(.toggle-button)`: نسخه‌ی «سطحِ روشن» برای دکمه‌های ghostِ داخلِ پنل‌های سفید (پس‌زمینه‌ی سفید + بوردر + سایه‌ی ملایم و hover آبی).
- تستِ سازگاریِ استاتیکِ جدید (خارج از مخزن) که تعریفِ کلاس‌ها در CSS، استفاده‌ی آن‌ها در app.js/exams.html و ثبتِ CHANGELOG را بررسی می‌کند.

**Fixed**
- ریشه‌ی مشکلِ دیده‌نشدنِ دکمه‌ها: `.ghost-button` از ابتدا برای سایدبارِ تیره طراحی شده بود (سفیدِ نیمه‌شفاف روی گرادیانِ آبی) و روی پنل‌های سفیدِ محتوا تقریباً نامرئی بود. با قانونِ جدیدِ `.panel .ghost-button` دکمه‌های «پاک‌سازی» (فرم‌های درس/ثبتِ مطالعه) و «به‌روزرسانی» (داشبورد) بدونِ تغییرِ HTML اصلاح شدند؛ دکمه‌های toggleِ «روزانه/هفتگی» که طراحیِ خاصِ خودشان (قرصِ بدونِ بوردر داخلِ toggle-group) را دارند، با `:not(.toggle-button)` مستثنی شدند.
- دکمه‌ی «حذف» در جدولِ گزارش‌های مطالعه که همین مشکلِ نامرئی‌بودن را داشت، به `.btn-danger-sm` مهاجرت کرد تا با دکمه‌ی «ویرایش» از نظرِ بصری هم‌خانواده باشد.

---

### 2026-08-29 — مجموعه‌تستِ خودکار (62 مورد) + رفعِ خطای ۵۰۰ نامِ درسِ تکراری

**Added**
- ۶۲ تستِ خودکار در `backend/planner/tests.py` (بستنِ آیتمِ High Priorityِ `TODO.md`): هفت کلاسِ تست روی `APITestCase` — `AuthAPITests` (۷)، `SubjectAPITests` (۱۱)، `ExamAPITests` (۱۴، شاملِ سناریوهای امنیتیِ ویرایش: 403 درسِ بیگانه / 404 امتحانِ بیگانه)، `StudyLogAPITests` (۹)، `StudyPlanAPITests` (۸)، `DashboardAPITests` (۶) و `StudyPlanAlgorithmTests` (۷، تستِ واحدِ توابعِ `utils.py`). اجرا با `python manage.py test planner` از پوشه‌ی `backend`؛ تست‌ها از دیتابیسِ درون‌حافظه‌ی خودِ Django استفاده می‌کنند و به `db.sqlite3` اصلی دست نمی‌زنند.
- اصولِ طراحیِ تست‌ها: همه‌ی تاریخ‌ها نسبی‌اند (`date.today() + N روز`) تا تست‌ها هرگز با گذشتِ زمان کهنه نشوند؛ احراز هویت با همان مسیرِ واقعیِ JWT (`RefreshToken.for_user` + هدرِ Bearer، نه `force_authenticate`) تا لایه‌ی احراز هویت هم تست شود؛ و هر رفعِ باگِ تاریخ‌دار (فیلدهای یتیمِ درس/امتحان، کسرِ ساعتِ `StudyLog`، امنیتِ `perform_update`، رفتارِ API پس از `migrate` شدن فیلدهای `notes`) حالا یک رگرسیون‌تستِ همیشگی دارد.

**Fixed**
- خطای ۵۰۰ هنگامِ ثبتِ درسِ هم‌نام برای همان کاربر (کشفِ خودکار در حینِ نوشتنِ همین تست‌ها): محدودیتِ `unique_together = ('user', 'name')` فقط در سطحِ دیتابیس بود و چون فیلد `user` در `SubjectSerializer` حضور ندارد، DRF نمی‌توانست `UniqueTogetherValidator` بسازد؛ نتیجه، `IntegrityError` خام و پاسخِ ۵۰۰ بود. متدِ `validate_name` به سریالایزر اضافه شد (با پیامِ فارسی و پشتیبانی از حالتِ ویرایش با `exclude(pk=self.instance.pk)`) — حالا همان سناریو 400 برمی‌گرداند. مسیرِ ویرایشِ نامِ درس به نامِ درسِ دیگرِ همان کاربر هم همین‌طور پوشش داده شد.

---


### 2026-09-03 — `0007_studylog_hours_deducted` (بازگرداندنِ ساعت هنگامِ حذفِ گزارشِ مطالعه)

**Added**
- متدِ `StudyLog.delete()`: قرینه‌ی `save()` — ساعتِ کسرشده را اتمیک (داخلِ `transaction.atomic`) به امتحانِ مربوطه برمی‌گرداند (بستنِ آیتمِ Medium Priorityِ `TODO.md`). مسیرِ API حذفِ گزارش (`DELETE /api/study-logs/{id}/`) از همین متد می‌گذرد؛ حذفِ آبشاری (مثلاً حذفِ خودِ امتحان که گزارش‌هایش را هم می‌برد) عمداً از مسیرِ BulkDeleteِ سریعِ جنگو می‌گذرد و ساعتی برنمی‌گرداند — امتحانی باقی نمی‌ماند که چیزی به آن برگردد.
- فیلدِ `StudyLog.hours_deducted` (Migrationِ `0007`): عددِ دقیقی که در لحظه‌ی ثبتِ هر گزارش از امتحان کم شده است. چرا لازم است؟ چون کسرِ ساعت در `save()` همیشه به مقدارِ موجودِ امتحان محدود بود (اگر ۲ ساعت مانده باشد و ۵ ساعت ثبت شود، فقط ۲ ساعت کم می‌شود)؛ بازگرداندنِ ساده‌ی «همانِ ساعتِ مطالعه» در حذف می‌توانست ساعتِ امتحان را از مقدارِ اولیه‌اش بیشتر کند. این فیلد مسئله را ریشه‌ای حل می‌کند: حذفِ گزارش دقیقاً همان عددی را برمی‌گرداند که واقعاً کسر شده بود.
- Backfill در خودِ Migration: گزارش‌های قدیمی (قبل از این تغییر) با «کلِ ساعتِ ثبت‌شده» پر می‌شوند — بهترین تخمینِ ممکن، چون عددِ دقیقِ تاریخی قابلِ بازیابی نیست؛ فقط در حالتِ استثنایی که امتحان از قبل تمام بوده، این تخمین بیش‌برآورد است (مستند در docstring خودِ Migration).
- ۵ تست جدید در `tests.py` (مجموع: ۶۷): بازگرداندنِ دقیق، حالتِ کسرِ محدود (Clamp)، گزارشِ بدونِ کسر، حذفِ یکی از چند گزارش، و ایمنیِ حذفِ آبشاری.
- فرانت‌اند (`app.js`): پیامِ `confirm()` حذف از هشدارِ «ساعت برنمی‌گردد» به اطلاعِ «ساعت به امتحان برمی‌گردد» تغییر کرد و بعد از حذفِ موفق، کشویِ امتحان‌ها (که متنِ «X ساعت باقی‌مانده» کنارِ هر گزینه دارد) هم تازه می‌شود.

**Changed**
- `StudyLog.save()` بازنویسی شد: عددِ کسرشده «قبل» از INSERT محاسبه و همراهِ خودِ رکورد ذخیره می‌شود و کلِ عملیاتِ «ذخیره‌ی گزارش + کسرِ ساعتِ امتحان» داخلِ `transaction.atomic` اجرا می‌شود — رفعِ بخشِ «نبودِ تراکنش» از آیتمِ شناخته‌شده‌ی `TODO.md` (بخشِ `select_for_update` برای رفعِ کاملِ Lost Update هنوز باقی است).
- «عدم بازگشتِ خودکارِ ساعت هنگامِ حذفِ گزارش» از فهرستِ محدودیت‌های `README.md` (بخشِ ۱۱) حذف شد و توضیحِ رفتارِ جدید به بخش‌های ۶/۷/۸ همان فایل و به `docs/01` و `docs/02` و `docs/04` اضافه شد؛ قاعده‌ی حیاتیِ ۶.۱ در `AI_CONTEXT.md` هم به‌روز شد.

### 2026-09-04 — بستنِ کاملِ XSS در رندرِ فرانت‌اند (همه‌ی رندرکننده‌ها)

**Fixed**
- تمامِ داده‌های متنیِ کاربر که از API می‌آیند، پیش از درج در `innerHTML` با تابعِ موجودِ `escapeHtml` پاک‌سازی می‌شوند — تا 2026-08-29 فقط `renderExams` این کار را می‌کرد (اولین گام). اکنون این قاعده در همه‌ی رندرکننده‌ها برقرار است:
  - `renderSubjects`: نامِ درس و یادداشتِ درس.
  - `renderStudyLogs`: نامِ امتحان (`exam_name`) و یادداشتِ گزارش.
  - `renderDashboard`: نامِ درس در پنلِ پیشرفت، نامِ امتحان در تقویم، متنِ پیام و `subject` هشدارها (پیام‌های سرور خودِ نامِ درس را در خود دارند)، و `label` نمودارِ توزیعِ زمان.
  - `renderPlan`: `subject` هر تسک، `title` بلوک‌ها، `hours_badge` و پیامِ حالتِ خالی.
- قاعده در کامنتِ بالای `escapeHtml` مستند شد: هر مقدارِ متنیِ API باید escape شود؛ اعدادِ اعتبارسنجی‌شده‌ی سمتِ سرور (ساعت/درصد/تاریخِ تولیدشده توسط جنگو) بدونِ escape می‌مانند. `alertType` از قبل با فهرستِ سفیدِ `validTypes` فیلتر می‌شود؛ کارت‌های آماری، گزینه‌های کشوها و پیامِ خوش‌آمد از مسیرِ امنِ `textContent` می‌گذرند (هرگز HTML parse نمی‌شوند).
- گاردِ رگرسیون (سوئیتِ خارج از مخزن، `scripts/test_xss_frontend.js`): ۳۰ تستِ رفتاری با payloadهای مخرب (`<img onerror=...>`، `<script>`، `"><svg onload=...>`) روی هر پنج رندرکننده + اسکنرِ استاتیکِ مختصات‌آگاه که هر interpolation خامِ جدید در template literal هایِ منتسب به `innerHTML` را در برابرِ فهرستِ سفیدِ صریح (اعداد/تاریخ‌ها/متغیرهای HTML-ساز) می‌سنجد.

**Changed**
- آیتمِ «آسیب‌پذیریِ XSS در رندرِ فرانت‌اند (باقی‌مانده)» از `TODO.md` (بخشِ Low Priority) حذف شد؛ `README.md` (قابلیتِ ۱۰ جدید)، `HANDOFF.md`، `AI_CONTEXT.md` و `04_frontend_logic_and_design.md` (بخش‌های ۳ و ۴) به‌روز شدند.


### 2026-09-05 — `0008_studyplan_user_unique` (یکتاییِ `StudyPlan.user` در سطحِ دیتابیس)

**Changed**
- فیلدِ `user` روی مدلِ `StudyPlan` از یک `ForeignKey` معمولی به `OneToOneField` (با همان `related_name='study_plans'`) تبدیل شد: قیدِ «هر کاربر حداکثر یک رکوردِ تنظیماتِ برنامه» حالا در خودِ دیتابیس اعمال می‌شود، نه فقط به‌عنوانِ فرضِ کد. این ریشه‌ی مشکلِ Race Condition در الگوی `get_or_create` را می‌بندد: با دو درخواستِ هم‌زمان، INSERT دومی توسط قیدِ UNIQUE رد می‌شود و جنگو خودش آن را به یک `get` موفق تبدیل می‌کند.
- Migrationِ `0008` پیش از اعمالِ قید، رکوردهایِ تکراریِ احتمالیِ قدیمی را پاکسازی می‌کند: برای هر کاربرِ دارایِ تکرار فقط جدیدترین رکورد (بیشترین `created_at` و سپس `id`) نگه داشته می‌شود — `daily_available_hours` همان تنظیمِ نهاییِ کاربر است و `plan_data` صرفاً مرجع/کش است. عملیاتِ reverse عمداً noop است (رکوردهایِ حذف‌شده قابلِ بازیابی نیستند).
- `StudyPlanViewSet.perform_create` چکِ پیشینی + گرفتنِ `IntegrityError` اضافه کرد: فراخوانیِ مستقیمِ `POST /api/study-plan/` وقتی تنظیمات از قبل موجود است، به‌جایِ خطایِ ۵۰۰ خامِ دیتابیس پاسخِ ۴۰۰ با پیامِ فارسی می‌گیرد (همان الگویِ نامِ درسِ تکراری). فرانت‌اند اصلی هرگز از این مسیر استفاده نمی‌کند (همیشه `generate/`)، ولی endpoint عمومی است و رفتارِ تمیز باید داشته باشد.

**Added**
- ۳ تستِ جدید (مجموع ۷۰): رگرسیونِ API (POST دوباره → ۴۰۰، نه ۵۰۰؛ فقط یک رکورد ساخته می‌شود) + کلاسِ `StudyPlanUniqueConstraintTests` با `TransactionTestCase` برای اثباتِ قید در خودِ DB (CREATE دوم → `IntegrityError`؛ چون این خطا تراکنشِ تستِ معمولی را می‌شکند) + idempotentماندنِ `get_or_create`.
- راستی‌آزماییِ مستقلِ Migration روی دیتابیسِ شبیه‌سازی‌شده‌ی legacy با سه رکوردِ تکراری (خارج از مخزن، `scripts/verify_unique_0008.py`): ۹/۹ PASS — پس از migrate فقط جدیدترین رکورد مانده، INSERT خامِ دوم با `UNIQUE constraint failed` رد می‌شود، `get_or_create` ایمن است و reverse هم بدونِ خطا.

**Removed**
- آیتمِ «محدودیتِ یکتاییِ `StudyPlan.user` در سطحِ دیتابیس» از `TODO.md` (بخشِ Medium) حذف شد؛ `README.md`، `HANDOFF.md`، `AI_CONTEXT.md`، `01_config_and_models.md` و `02_server_logic_and_api.md` به‌روز شدند.

### 2026-09-06 — صفحه‌بندیِ اختیاریِ endpointهای فهرستی
**Added**
- کلاسِ `OptionalPageNumberPagination` در `views.py` (زیرکلاسِ `PageNumberPagination`ِ DRF) و نصبِ آن رویِ سه ViewSet فهرستی: `SubjectViewSet`, `ExamViewSet`, `StudyLogViewSet`. صفحه‌بندی «اختیاری» است: تا وقتی کلاینت نه `?page=` فرستاده و نه `?page_size=`، پاسخ دقیقاً همان «لیستِ کاملِ JSON» قبل است — فرانت‌اندِ فعلیِ `app.js` هیچ پارامتری نمی‌فرستد و هیچ تغییری حس نمی‌کند. به‌محضِ فرستادنِ هرکدام از دو پارامتر، شکلِ پاسخ به قالبِ استانداردِ DRF تبدیل می‌شود: `{"count": N, "next": "...?page=2", "previous": null, "results": [...]}`.
- قواعدِ اندازه‌ی صفحه (با تستِ واحدِ مستقیم رویِ خودِ کلاس): `page_size` صریح و مثبت (با سقفِ `max_page_size=100` — مقادیرِ بزرگ‌تر به ۱۰۰ محدود می‌شوند)؛ فقط `?page=` بدونِ `page_size` یعنی اندازه‌ی پیش‌فرضِ ۲۰؛ `page_size` نامعتبر/غیرمثبتِ تنها → صفحه‌بندی فعال نمی‌شود (همان لیستِ کامل)؛ و شماره‌ی صفحه‌ی نامعتبر/خارج از محدوده → ۴۰۴ استانداردِ DRF.
- ۹ تستِ جدید در کلاسِ `PaginationAPITests` (مجموع ۷۹): رگرسیونِ سازگاریِ عقب (هر سه endpoint بدونِ پارامتر → لیستِ خام)، شکلِ صفحه‌بندی‌شده و کلیدهایش، پیمایشِ کاملِ صفحه‌های ۱ تا ۳ بدونِ هم‌پوشانی، صفحه‌ی آخر (next=null)، ۴۰۴ِ صفحه‌ی نامعتبر، `page_size` نامعتبر بدونِ page، جداسازیِ کاربران در `count`/نتایج، و تستِ واحدِ قواعدِ `get_page_size` (پیش‌فرض/سقف/غیرفعال).
- راستی‌آزماییِ سرِ زنده (`runserver` + HTTP واقعی): هر سه endpoint در هر دو شکلِ پاسخ، لینک‌های next/previous، سقفِ ۱۰۰ و ۴۰۴ِ صفحه‌ی نامعتبر.

**Changed**
- آیتمِ Medium «افزودنِ Pagination به Endpointهای فهرستی» در `TODO.md` بسته شد؛ نکته‌ی ۶.۹ِ `AI_CONTEXT.md` از «بدونِ Pagination» به «صفحه‌بندیِ اختیاری + قاعده‌ی سازگاری» بازنویسی شد.

*(این تغییر مایگریشن ندارد و `settings.py` هم دست نخورده — مرتب‌سازیِ قطعیِ صفحه‌ها از `Meta.ordering` موجودِ خودِ مدل‌ها می‌آید: درس/گزارش جدیدترین اول، امتحان نزدیک‌ترین تاریخ اول.)*


### 2026-09-06 — تمدیدِ خودکارِ نشست: ذخیره‌ی Refresh Token + هدایت به صفحه‌ی ورود هنگامِ انقضا
**Added**
- مسیرِ `POST /api/auth/refresh/` در `planner/urls.py` با ویویِ آماده‌ی `TokenRefreshView` از SimpleJWT: بدنه‌ی `{"refresh": "..."}` می‌گیرد و توکنِ دسترسیِ تازه برمی‌گرداند (توکنِ Refreshِ معتبر = 200؛ بی‌اعتبار/منقضی = 401 — همین 401 سیگنالِ «نشست تمام است» برایِ فرانت‌اند است). مثلِ register/login بدونِ هدرِ Authorization در دسترس است چون خودِ توکنِ Refresh اثباتِ هویت است.
- در `app.js`: کلیدِ `ssp_refresh_token` در `storageKeys` + توابعِ کمکیِ مربوطه؛ `handleLogin` از این پس توکنِ Refresh را هم کنارِ توکنِ دسترسی ذخیره می‌کند (قبلاً دور ریخته می‌شد — ریشه‌ی کلِ مشکل).
- `refreshAccessToken()`: تمدیدِ بی‌صدا با dedupe — چند درخواستِ هم‌زمانِ ۴۰۱‌شده همه منتظرِ یک Promise واحد می‌مانند و فقط یک بار به سرور زده می‌شود؛ اگر یک روز `ROTATE_REFRESH_TOKENS` روشن شد، توکنِ Refreshِ تازه را هم بی‌صدا ذخیره می‌کند.
- `forceLogoutExpired()`: پاک‌سازیِ کاملِ نشست (دو توکن + نامِ کاربری) و هدایت به `login.html?expired=1`؛ صفحه‌ی ورود با دیدنِ همین پارامتر یک Toast توضیحی («نشست شما منقضی شده بود...») نشان می‌دهد تا کاربر بی‌خبر وسطِ کار بیرون نیفتد. رویِ خودِ صفحه‌ی ورود هدایتی انجام نمی‌شود (جلویِ حلقه).
- منطقِ ۴۰۱ در `apiRequest`: پاسخِ 401 (در درخواستِ غیرِ skipAuth) اول یک‌بار بی‌صدا تمدید و «همان درخواست» دوباره زده می‌شود (`_isRetry` جلویِ حلقه را می‌گیرد)؛ اگر تمدید ممکن نشد یا تکرار هم ۴۰۱ داد، `forceLogoutExpired` اجرا و خطایِ خوانایِ فارسی پرتاب می‌شود. `logout()` هم از این پس توکنِ Refresh را پاک می‌کند.
- ۳ تستِ جدید در `AuthAPITests` (مجموع ۸۲): تمدیدِ معتبر → 200 + توکنِ تازه که واقعاً رویِ endpoint محافظت‌شده کار می‌کند؛ توکنِ Refreshِ بی‌اعتبار → 401؛ نبودِ هدرِ Authorization → 403 نمی‌دهد (مسیرِ عمومی است).
- راستی‌آزماییِ E2E با مرورگرِ واقعی (Chromium): ورود از فرمِ واقعی → هر دو توکن در localStorage؛ خراب‌کردنِ توکنِ دسترسی → تمدیدِ بی‌صدا و بارگذاریِ داده‌ها بدونِ هدایت؛ خراب‌کردنِ هر دو توکن → هدایت به `login.html?expired=1` + Toast + پاک‌سازیِ کامل؛ ورودِ مجدد → بدونِ حلقه. دو اسکرین‌شاتِ شاهد.

**Changed**
- آیتمِ Low «نبودِ هدایت به صفحه‌ی ورود هنگامِ انقضای توکن» در `TODO.md` بسته شد؛ بخشِ ۸ِ `AI_CONTEXT.md` و `docs/02`/`docs/04` با مسیرِ تمدید و استراتژیِ 401 همگام شدند.

*(مایگریشن ندارد؛ فقط `planner/urls.py` + `static/app.js` + `planner/tests.py`. برایِ دیدنِ نسخه‌ی جدیدِ فرانت‌اند، Ctrl+F5 توصیه می‌شود. کاربرانِ لاگین‌کرده‌ی قبل از این تغییر فقط توکنِ دسترسیِ قدیمی را دارند — با اولین 401 مستقیم به صفحه‌ی ورود هدایت می‌شوند و با ورودِ مجدد، نشستِ جدیدِ دو‌توکنی ساخته می‌شود.)*


### 2026-09-06 — تنظیماتِ امنیتیِ Production از متغیرهایِ محیطی
**Added**
- پنج متغیرِ محیطی در `backend/backend/settings.py` با پیش‌فرضِ «dev-safe» — بدونِ ست‌کردنِ هیچ متغیری، رفتارِ اجرایِ محلی ذره‌ای تغییر نمی‌کند (۸۹ تست بدونِ متغیر سبز): `DJANGO_SECRET_KEY` (کلیدِ واقعی؛ پیش‌فرض = همانِ کلیدِ توسعه‌ی قبلی)، `DJANGO_DEBUG` (پیش‌فرض true)، `DJANGO_ALLOWED_HOSTS` (فهرستِ جدا با کاما؛ پیش‌فرض خالی)، `DJANGO_CORS_ALLOW_ALL` (پیش‌فرض true) و `DJANGO_ALLOWED_ORIGINS` (مبدأهایِ مجاز وقتی allow-all خاموش است؛ پیش‌فرض خالی). مقادیرِ بولیِ مثبت: `1`/`true`/`yes`/`on` — هر چیزِ دیگر (حتیِ «2») منفی است تا مقدارِ اشتباه، بی‌صدا حالتِ امن را باز نکند.
- سپرِ راه‌اندازیِ Production: با `DJANGO_DEBUG=false`، اگر کلیدِ توسعه بماند یا `DJANGO_ALLOWED_HOSTS` خالی باشد، جنگو همانِ لحظه‌ی بوت با `ImproperlyConfigured` و پیامِ راهنما متوقف می‌شود — نه بوتِ بی‌صدایِ ناامن، نه ۴۰۰هایِ گیج‌کننده‌ی بعداً. تولیدِ کلید: `python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"`.
- ۷ تستِ جدید در کلاسِ `SettingsEnvVarsTests` (مجموع ۸۹): تستِ واحدِ `_env_bool`/`_env_list` + پنج «بوتِ واقعی» که هرکدام یک مفسرِ پایتونِ تازه با متغیرهایِ دلخواه spawn می‌کند — بوتِ توسعه بدونِ متغیر (رگرسیونِ پیش‌فرض‌ها)، ردِ بوتِ Production بدونِ کلید، ردِ بوتِ Production بدونِ Host، بوتِ سالم با ترکیبِ کامل، و خاموش‌کردنِ CORS allow-all با فهرستِ مبدأها.
- `*.patch` به `.gitignore` اضافه شد تا فایل‌های پچِ تحویلیِ `git am` وضعیتِ `git status` را کثیف نکنند.

**Changed**
- `SECRET_KEY`/`DEBUG`/`ALLOWED_HOSTS`/`CORS_ALLOW_ALL_ORIGINS` (و `CORS_ALLOWED_ORIGINS`ِ تازه) از مقادیرِ هاردکد به متغیرهایِ محیطی با همانِ پیش‌فرض‌هایِ قبلی تغییر کردند؛ جدولِ کاملِ متغیرها + مثال‌هایِ ویندوز/لینوکس به `README.md` (بخشِ ۱۰) اضافه شد.
- آیتمِ Medium «بازبینیِ تنظیماتِ امنیتیِ پیش از استقرار» در `TODO.md` بسته شد؛ بخشِ ۹ِ `AI_CONTEXT.md` بازنویسی و نکته‌ی ۶.۱۰ افزوده شد؛ `HANDOFF.md`، `01_config_and_models.md` و `02_server_logic_and_api.md` همگام شدند.

*(مایگریشن ندارد — فقط `settings.py`، `tests.py`، `.gitignore` و مستندات. برایِ استقرارِ واقعی، جدولِ متغیرها در `README.md` بخشِ ۱۰ را ببینید.)*


### 2026-09-08 — سخت‌ترکردنِ JWT: چرخشِ توکنِ Refresh + لیستِ سیاه + خروجِ سرور-محور

**Added**

- اپِ `rest_framework_simplejwt.token_blacklist` در `INSTALLED_APPS` (تنظیمات): زیرساختِ لیستِ سیاه — دو جدولِ `OutstandingToken` (توکن‌های صادرشده) و `BlacklistedToken` (توکن‌های باطل‌شده) را به دیتابیس اضافه می‌کند. مایگریشن‌هایش داخلِ خودِ کتابخانه است و فایلی به ریپو اضافه نمی‌شود؛ **یک‌بار `python manage.py migrate` بعد از اعمالِ این تغییر لازم است** (تست‌ها مستقل از آن سبزند چون دیتابیسِ تست از نو ساخته می‌شود؛ ولی سرورِ توسعه بدونِ migrate موقعِ صدورِ توکن خطا می‌دهد).
- مسیرِ `POST /api/auth/logout/` در `planner/urls.py` با ویویِ آماده‌ی `TokenBlacklistView` از SimpleJWT: بدنه‌ی `{"refresh": "..."}` می‌گیرد و همان توکن را در لیستِ سیاه ثبت می‌کند (200؛ توکنِ بی‌اعتبار → 401). مثلِ refresh بدونِ هدرِ Authorization در دسترس است.
- خروجِ سرور-محور در `app.js`: `logout()` از این پس اول توکنِ Refresh را به `/api/auth/logout/` می‌فرستد تا رویِ سرور باطل شود، بعد localStorage را پاک و به صفحه‌ی ورود برمی‌گردد (اگر سرور در دسترس نبود، خروجِ محلی باز هم انجام می‌شود — کاربر به‌خاطرِ قطعیِ سرور در حسابِ باز نمی‌ماند). تا قبل از این تغییر، توکنِ کاربرِ خروج‌کرده تا پایانِ عمرش (تا ۷ روز) قابلِ تمدید بود.
- ۷ تستِ جدید در کلاسِ `JWTTokenRotationBlacklistTests` (مجموع ۹۶): چرخش (پاسخِ تمدید، توکنِ Refreshِ تازه هم دارد)؛ باطل‌شدنِ توکنِ قبلی بعد از چرخش (۴۰۱)؛ ادامه‌ی زنجیره‌ی تمدید (هر دو نسلِ قبلی مرده‌اند)؛ کارکردنِ توکنِ دسترسیِ صادرشده در چرخش؛ ابطالِ توکن با خروجِ سرور-محور؛ ردِ توکنِ بی‌اعتبار در خروج (۴۰۱، نه ۵۰۰)؛ و اثرِ نگذاشتنِ ابطالِ توکنِ یک نشست رویِ نشستِ کاربرِ دیگر.

**Changed**

- `ROTATE_REFRESH_TOKENS=True` و `BLACKLIST_AFTER_ROTATION=True` در `SIMPLE_JWT`: هر تمدید حالا علاوه بر توکنِ دسترسی، یک توکنِ Refreshِ تازه صادر می‌کند و توکنِ قبلی بلافاصله باطل می‌شود — هر توکنِ تمدید فقط یک‌بار قابلِ استفاده است؛ توکنِ دزدیده‌شده با اولین تمدیدِ مالکِ واقعی از بازی خارج می‌شود و خروجِ کاربر توکنش را واقعاً می‌کشد.
- کامنتِ «آینده‌نگرِ» `refreshAccessToken` در `app.js` به‌روز شد (چرخش فعال است و ذخیره‌ی توکنِ تازه برایِ ادامه‌ی زنجیره حیاتی است) — خودِ منطقِ فرانت‌اند از 2026-09-06 آماده بود و ذره‌ای تغییر نکرد.
- آیتمِ Low «سخت‌ترکردنِ تنظیماتِ JWT برای Production» در `TODO.md` بسته شد؛ `HANDOFF.md`، `README.md`، `AI_CONTEXT.md` (نکته‌ی ۶.۱۱ + جدولِ بخشِ ۹) و `docs/01`/`docs/02`/`docs/04` همگام شدند.

*(مایگریشنِ داخلِ ریپو ندارد؛ ولی migrate لازم است — اپِ token_blacklist جدول‌های خودش را در دیتابیسِ موجود می‌سازد. توکن‌های Refreshِ صادرشده‌ی قبل از این تغییر هم بدونِ مشکل کار می‌کنند و اولین تمدیدشان آن‌ها را واردِ چرخه‌ی ثبت/ابطال می‌کند.)*


### 2026-09-09 — مؤلفه‌ی یادگیریِ آماری: کالیبراسیونِ تخمین‌هایِ ساعتی + پیش‌بینیِ ساعتِ واقعی و ریسکِ عقب‌افتادن

**Added**
- `backend/planner/ml.py` — مدلِ «کالیبراسیونِ کاربر»: رگرسیونِ کمینه‌ی مربعاتِ از مبدأ (β = Σxy/Σx²) رویِ نمونه‌هایِ (planned, actual) ساخته‌شده از امتحان‌هایِ *گذشته‌ی* هر کاربر (planned = بازسازیِ تخمینِ اولیه از «باقی‌مانده + جمعِ کسرشده‌ها»، actual = جمعِ ساعاتِ ثبت‌شده)، همراهِ انقباضِ بیزی به‌سمتِ ۱ (PRIOR_STRENGTH=4)، مهارِ نهاییِ β در بازه‌ی [۰٫۲۵ , ۴] و برچسبِ سوگیری (دست‌کم‌گیر/زیادرو/دقیق). پیاده‌سازی با پایتونِ خالص — بدونِ scikit-learn و بدونِ هیچ وابستگیِ جدید.
- endpoint جدیدِ `GET /api/predictions/` (فقط با احرازِ هویت): مدلِ آموخته‌شده + به‌ازای هر امتحانِ آینده‌ی فعال: ساعتِ اعلام‌شده، ساعتِ واقعیِ پیش‌بینی‌شده، نیازِ روزانه و سطحِ ریسک (high/medium/low نسبت به ساعتِ آزادِ روزانه‌ی همان کاربر از `StudyPlan`) + خلاصه. داده‌ی پاسخ عمداً ماشین‌خوان است؛ متنِ نمایشی در فرانت‌اند ترجمه می‌شود (هیچ رشته‌ی gettextِ جدیدی اضافه نشد و کاتالوگِ locale/en دست‌نخورده ماند).
- پنلِ «پیش‌بینیِ هوشمندِ زمانِ واقعی» در صفحه‌ی برنامه‌ی مطالعه: کارتِ وضعیتِ مدل (ضریب، سوگیری، نمونه‌ها، اعتماد) + یک بلوک به‌ازای هر امتحان؛ `loadPredictions`/`renderPredictions` در `app.js` + ۱۹ کلیدِ جدید در `static/i18n.js` (مجموع ۱۹۳)؛ بعد از تغییرِ ساعتِ آزادِ روزانه، پیش‌بینی‌ها هم تازه می‌شوند.
- ۲۱ تستِ جدید (مجموع ۱۵۲): `MLCalibrationMathTests` (۹ — LSQ دقیق، انقباض، مهار، مرزهای سوگیری/ریسک، اعمالِ β)، `MLTrainingDataTests` (۵ — فقط امتحان‌هایِ گذشته، بازسازیِ planned، ردِ planned صفر، مرزِ امروز، جداسازیِ کاربران) و `MLPredictionsAPITests` (۷ — 401، کاربرِ بی‌تاریخچه، اثرِ یادگیری روی پیش‌بینی، فیلترِ امتحان‌هایِ فعال، سه سطحِ ریسک، حساسیت به تنظیماتِ کاربر، جداسازیِ مدل بین کاربران).

**Changed**
- `TODO.md`: آخرینِ آیتمِ فعال (مؤلفه‌ی ML) بسته شد — ایده‌ی «پیش‌بینیِ ریسکِ عقب‌افتادن» از بخشِ Future Ideas هم در همین قابلیت تحقق یافت؛ TODO فعلاً آیتمِ بازی ندارد.
- README: ردیفِ `/api/predictions/` در جدولِ API، قابلیتِ ۱۸، شمارنده‌های تست ۱۳۱→۱۵۲ و بازنویسیِ نکته‌ی «بدونِ یادگیریِ ماشینِ واقعی» در بخشِ ۱۱ (+ رفعِ نکته‌ی ازته‌ی «عدمِ پشتیبانیِ چندزبانی» که از Taskِ چندزبانی جامانده بود)؛ AI_CONTEXT (نکته‌ی ۶.۱۵ + جدول‌ها)، HANDOFF و docs 02/03/04 هم‌راستا شدند.

### 2026-09-09 — چندزبانی (fa/en): هدرِ Accept-Language + کاتالوگِ gettext + دکمه‌ی تعویضِ زبان

**Added**

- دکمه‌ی «English/فارسی» (`#langToggle`) در همه‌ی ۷ صفحه: زبانِ انتخابی در `localStorage` (کلیدِ `ssp_lang`) ذخیره و صفحه رفرش می‌شود تا همه‌ی رندرهای پویا با زبانِ تازه ساخته شوند؛ در حالتِ en جهتِ صفحه LTR می‌شود (اسکریپتِ کوچکِ اولِ `<head>` جهت را قبل از اولین رندر عوض می‌کند تا «فلشِ» چیدمان دیده نشود).
- فایلِ `static/i18n.js` (۳۰۳ خط): دیکشنریِ فارسی→انگلیسی (۱۷۴ کلید؛ «کلید» همان متنِ فارسیِ اصلی = نقشِ msgid) + تابعِ `t(key, params)` برایِ رشته‌های پویای app.js + `applyI18n()` که متن‌های ثابتِ HTML و `placeholder`/`title`/`aria-label` را فقط در حالتِ en با TreeWalker ترجمه می‌کند + خروجِ `module.exports` برایِ تست‌پذیری در Node. فارسی = هیچ دست‌کاری‌ای در DOM (همان متنِ اصلیِ HTML).
- کاتالوگِ بک‌اند `backend/locale/en/LC_MESSAGES/django.po` + `django.mo` کامپایل‌شده (۳۰ ترجمه): پیام‌هایِ اختصاصیِ views/serializers/utils. فایلِ .mo عمداً commit شده (روی ویندوزِ کاربر msgfmt موجود نیست)؛ کامپایل فقط بعد از تغییرِ ترجمه با `scripts/compile_mo.py` لازم است.
- ۱۵ تستِ جدید (مجموع ۱۱۶ → ۱۳۱): `I18nAcceptLanguageTests` (۹) — پیش‌فرضِ فارسیِ بدونِ هدر، ترجمه با en، پایداری با fa، fallback با زبانِ نامعتبر (de)، هدرِ واقعیِ مرورگر، ترجمه‌ی پیامِ سریالایزر (gettext_lazy)، خطاهای generate، عنوان/نشانِ برنامه و هشدارِ داشبورد (همه با interpolation پارامتری)؛ `I18nCatalogIntegrityTests` (۶) — وجود و خواناییِ django.mo با gettext پایتونی، رگرسیونِ کامپایل (هیچ msgstr خالی یا برابرِ msgid نیست)، fallbackِ کلیدِ ناشناس، ترجمه‌ی lazyِ روزهای هفته، شکلِ تنظیمات و ترتیبِ LocaleMiddleware. به‌علاوه‌ی ۴۱ چکِ فرانت‌اند در `scripts/test_i18n_frontend.js` (Node؛ خارج از ریپو).

**Changed**

- `settings.py`: `LocaleMiddleware` اضافه شد (بعد از SessionMiddleware و قبل از CommonMiddleware — ترتیبِ لازمِ مستندِ جنگو)؛ `LANGUAGE_CODE='fa'`؛ `LANGUAGES=[fa, en]`؛ `LOCALE_PATHS=[BASE_DIR/'locale']`. بدونِ Accept-Language رفتار همانِ قبل است (fa = متنِ اصلی)؛ پیام‌های آماده‌ی DRF/SimpleJWT هم حالا کاتالوگِ fa خودِ کتابخانه‌ها را می‌گیرند (سازگارتر با رابطِ فارسی؛ قبلاً انگلیسی بودند).
- `views.py`/`serializers.py`/`utils.py`: همه‌ی پیام‌هایِ اختصاصی با `gettext`/`gettext_lazy` (`_()`) پیچیده شدند؛ رشته‌های دارایِ متغیر به الگوی msgid پارامتری (`%(name)s`) تبدیل شدند (ترتیبِ کلمات در ترجمه آزاد می‌ماند)؛ `WEEKDAY_FA` حالا gettext_lazy است (ترجمه تا زمانِ رندر به‌تعویق می‌افتد).
- `app.js`: همه‌ی رشته‌های نمایشی با `t()` پیچیده شدند؛ `apiRequest` هدرِ `Accept-Language` (en/fa) می‌فرستد تا پیام‌های خطای API هم با زبانِ کاربر برگردند (هدرِ ساف‌لیستِ CORS — preflight جدید نمی‌سازد).
- ۷ صفحه‌ی HTML: اسکریپتِ جهتِ زودهنگام + دکمه‌ی `#langToggle` + بارگذاریِ `i18n.js` قبل از `app.js` (پیش‌نیاز: `t` باید قبل از اولین رندر تعریف شده باشد).

*(مایگریشن ندارد — اسکیما دست‌نخورده ماند. برایِ تغییرِ ترجمه‌ها: `django.po` را ویرایش و با `scripts/compile_mo.py` دوباره کامپایل کنید؛ کلیدِ فرانت‌اند را هم در `static/i18n.js` به‌روز کنید.)*


### 2026-09-09 — پشتیبانیِ PostgreSQL از طریقِ DATABASE_URL (پیش‌فرضِ امنِ توسعه: SQLite)

**Added**

- متغیرِ محیطیِ `DATABASE_URL`: بدونِ این متغیر هیچ چیز عوض نمی‌شود (همان SQLiteِ توسعه)؛ با آدرسِ `postgres://user:pass@host:port/dbname` موتور به PostgreSQL عوض می‌شود — «جایگزینیِ SQLite با PostgreSQL» از TODO حالا یک قابلیتِ آماده است و قفلِ `FOR UPDATE`ِ همان روز در PostgreSQL واقعاً فعال می‌شود (رگرسیون‌تستِ جدید SQL را بررسی می‌کند). پارامترهایِ اختیاریِ انتهایِ URL: `?sslmode=require`، `?conn_max_age=60` و `?host=/var/run/postgresql` (سوکتِ یونیکس). رمزِ دارایِ کاراکترِ خاص باید URL-encode شود (`@` → `%40`).
- درایورِ `psycopg[binary]` (نسخه‌ی ۳) به `requirements.txt` اضافه شد — نصب: `pip install -r requirements.txt`؛ اگر متغیر ست شده باشد ولی درایور نصب نباشد، بوت با پیامِ راهنمایِ نصب متوقف می‌شود (`ImproperlyConfigured`)، نه با خطایِ مبهمِ وسطِ اولین کوئری.
- هشدارِ یک‌خطی: `DJANGO_DEBUG=false` بدونِ `DATABASE_URL` روی stderr یادآوری می‌کند که Production روی SQLite است (برایِ دمو/دفاع قابل‌قبول، برایِ چندکاربره نه) — بوت متوقف نمی‌شود.
- ۱۳ تستِ جدید (مجموع ۱۰۳ → ۱۱۶): کلاسِ `DatabaseUrlSettingsTests` — پیش‌فرضِ SQLiteِ دست‌نخورده، پارسِ کاملِ URL (user/pass/host/port/name با URL-decode)، پارامترهایِ query، ردِ schemeِ نامعتبر/پورتِ غیر عددی/نبودِ نامِ دیتابیس/conn_max_age غیر عددی، نبودِ درایور (یک‌بار با تزریقِ importِ شکسته و یک‌بار با بوتِ واقعیِ مفسرِ جدا + ماژولِ سایه‌ی psycopg)، بوتِ واقعیِ هر دو حالت، و هشدارِ Production-روی-SQLite؛ به‌علاوه‌ی کلاسِ `PostgresForUpdateTests` — رگرسیونِ «FOR UPDATE در SQL» که فقط وقتی موتورِ فعال PostgreSQL است اجرا می‌شود (در SQLite skip؛ پوششِ رفتاری‌اش در کلاس‌های قبل است). تستِ یتیمِ PRAGMA هم شرطیِ موتور گرفت تا کلِ مجموعه روی PostgreSQL هم سبز شود — و شد: ۱۱۶/۱۱۶ روی هر دو موتور.

**Changed**

- `DATABASES` در `settings.py` حالا با تابعِ `_resolve_database()` ساخته می‌شود — ترتیبِ بررسی: scheme → نامِ دیتابیس → پورت → پارامترهای query → درایور؛ هر خطا همان بوت با پیامِ راهنما متوقف می‌شود. مقدارِ پیش‌فرضِ `ENGINE`/`NAME` دقیقاً همانِ قبلی است (رگرسیون‌تست دارد).
- `StudyLogOrphanDeleteTests` فقط روی SQLite اجرا می‌شود (ساختِ یتیم با `PRAGMA foreign_keys` مخصوصِ SQLite است).

*(مایگریشن و migrate ندارد — اسکیما دست‌نخورده ماند و `makemigrations --check` پاک است. کلِ مجموعه‌ی ۱۱۶تایی هم روی SQLite و هم روی PostgreSQL واقعی سبز شده است.)*


### 2026-09-09 — قفلِ هم‌زمانیِ گزارشِ مطالعه: select_for_update + خواندنِ تازه

**Changed**

- `StudyLog.save()`: پیش از محاسبه‌ی «ساعتِ واقعاً کسرشده»، سطرِ امتحان داخلِ همان تراکنشِ اتمیک با `Exam.objects.select_for_update().get(pk=self.exam_id)` قفل و از دیتابیس «تازه» خوانده می‌شود — به‌جای snapshotِ ممکن‌الوقوعِ `self.exam` در حافظه. مبنایِ کسر از این پس مقدارِ لحظه‌ایِ دیتابیس است و دو درخواستِ هم‌زمان دیگر نمی‌توانند کسری را از دست بدهند (Lost Update). اگر امتحانِ گزارشِ تازه در لحظه‌ی ذخیره حذف شده باشد، خطایِ روشنِ `DoesNotExist` می‌گیرد و هیچ رکوردِ نیمه‌کاره‌ای ذخیره نمی‌شود.
- `StudyLog.delete()`: بازگشتِ ساعت هم رویِ نمونه‌یِ قفل‌شده و تازه‌خوانده‌شده جمع می‌شود (نه کپیِ کش‌شده‌یِ ابتدایِ درخواست — مثلاً `select_related`)؛ ترتیبِ قفل‌ها با `save()` یکسان شد (اول امتحان، بعد گزارش) تا در دیتابیس‌های چندکاربره بینِ این دو متد بن‌بست (Deadlock) رخ ندهد. حذفِ گزارشِ یتیم (کلیدِ خارجیِ شکسته) مثلِ قبل فقط خودِ گزارش را حذف می‌کند — حالا با یک SELECT کمتر و بدونِ خطرِ «زنده‌کردنِ» امتحانِ حذف‌شده که در کدِ قبلی ممکن بود.
- نکته‌ی SQLite: قفلِ `FOR UPDATE` در SQLite بی‌صدا نادیده گرفته می‌شود؛ «خواندنِ تازه» به‌تنهایی آثارِ Lost Update را در عمل از بین می‌برد و قفلِ واقعیِ سطر در PostgreSQL/MySQL فعال می‌شود — این تغییر پیش‌نیازِ مهاجرت به PostgreSQL بود.

**Added**

- ۷ تستِ جدید (مجموع ۹۶ → ۱۰۳): کلاسِ `StudyLogConcurrencyLockTests` — کسر از مقدارِ تازه‌ی دیتابیس نه snapshotِ حافظه (رگرسیونِ Lost Update؛ رویِ کدِ قبلی قطعی‌آور FAIL می‌شود)، بازگشتِ ساعت به مقدارِ لحظه‌ایِ دیتابیس، قفل‌بودنِ مسیرِ save/delete (جاسوس رویِ `QuerySet.get` چون در SQLite در SQL دیده نمی‌شود)، ویرایشِ بدونِ قفل و بدونِ کسرِ مجدد، و خطایِ روشن هنگامِ حذفِ هم‌زمانِ امتحان؛ به‌علاوه‌ی کلاسِ `StudyLogOrphanDeleteTests` با `TransactionTestCase` برای حذفِ یتیمِ واقعی (ساختِ یتیم فقط بیرونِ تراکنشِ تست و با خاموش‌کردنِ موقتِ FK ممکن است).
- آیتمِ Low «نبودِ قفلِ هم‌زمانی (select_for_update) در `StudyLog.save()`/`delete()`» در `TODO.md` بسته شد؛ `HANDOFF.md`، `README.md`، `AI_CONTEXT.md` (نکته‌ی ۶.۱۲ + جدول‌ها) و `01_config_and_models.md`/`02_server_logic_and_api.md` همگام شدند.

*(مایگریشن و migrate ندارد — فقط رفتارِ ذخیره/حذف عوض شد و اسکیمای دیتابیس دست‌نخورده ماند؛ `makemigrations --check` پاک است.)*


## بخشِ بدونِ تاریخِ دقیق (بعد از آخرین Migration)

> این تغییرات از روی شواهدِ داخلِ کدِ فعلی (کامنت‌ها، مستنداتِ `docs/`، و `README.md`) قابل‌تأیید هستند، ولی چون هیچ تاریخِ ثبت‌شده‌ای در پروژه ندارند، بدونِ تاریخِ مشخص و فقط به‌ترتیبِ منطقیِ احتمالی فهرست می‌شوند.

**Added**
- API کامل (Register/Login با JWT، CRUD برای `Subject`/`Exam`/`StudyLog`، Endpoint‌های `study-plan` و `dashboard`).
- ماژولِ الگوریتمِ برنامه‌ریزی (`utils.py`): `compute_subject_progress`, `generate_study_plan`, `format_plan_for_frontend`, `build_subject_distribution`.
- ۷ صفحه‌ی فرانت‌اند (`login`, `register`, `index`/داشبورد, `subjects`, `exams`, `study_plan`, `study_log`) و فایل‌های مشترکِ `app.js`/`styles.css`.
- صفحه‌ی `study_log.html` به‌همراه توابعِ مرتبط در `app.js` (`populateExamSelectForLog`, `renderStudyLogs`, `loadStudyLogs`, `deleteStudyLog`, `handleStudyLogForm`) — قابلیتی که حلقه‌ی «برنامه → مطالعه → پیشرفتِ واقعی» را کامل کرد.
- Management Command سفارشیِ `seed_demo_data` برای تولیدِ داده‌ی نمونه‌ی تست.
- فیلدِ `urgent_alerts_count` و نوعِ سومِ هشدار (`info`، برای یادآوریِ خنثیِ برنامه‌ی امروز) در پاسخِ `/api/dashboard/`.
- مستنداتِ تکمیلی: `README.md`، چهار فایلِ `docs/01` تا `docs/04`، `.gitignore`، `backend/requirements.txt`.
- کامنت‌گذاریِ توضیحی (فارسی) در تمامِ فایل‌های کدِ پروژه (بدونِ تغییرِ منطق).

**Changed**
- الگوریتمِ نمودارِ «تقسیم‌زمانِ پیشنهادی» در داشبورد: از محاسبه‌ی بر‌مبنایِ «مجموعِ ساعتِ هر روز» (که چون الگوریتم هر روز کلِ ساعتِ آزاد را تخصیص می‌دهد، تقریباً همیشه عددی ثابت و بی‌فایده بود) به محاسبه‌ی بر‌مبنایِ «سهمِ هر درس از هفته‌ی پیشِ‌رو» تغییر کرد (تابعِ `build_subject_distribution`).
- نمایشِ «برنامه‌ی هفتگی»: روزهای پیاپیِ با ترکیبِ دقیقاً یکسانِ درس/ساعت، به‌جایِ نمایشِ چند بلوکِ تکراری، در یک بازه ادغام می‌شوند (تابعِ `format_plan_for_frontend` در `utils.py`).
- کارتِ «هشدارهای فوری» در داشبورد: از شمارشِ کلِ آرایه‌ی هشدارها به شمارشِ اختصاصیِ فقط هشدارهای نوعِ `danger` (فیلدِ جداگانه‌ی `urgent_alerts_count`).

**Fixed**
- باگِ ترتیبِ `router.register` در `planner/urls.py` که باعث می‌شد Endpointِ `study-logs` اصلاً در فهرستِ نهاییِ URLها ثبت نشود (چون بعد از `include(router.urls)` نوشته شده بود).
- مسیرِ `/api/study-plan/generate/` که به‌خاطرِ تعریف‌شدن به‌عنوانِ یک تابعِ آزاد (خارج از کلاسِ ViewSet) به‌جایِ یک متدِ کلاس، اصلاً به هیچ Endpointی متصل نمی‌شد.
- باگِ راست‌به‌چپ (RTL) در سایدبارِ حالتِ موبایل: سایدبار با `left: 0` (استایلِ چپ‌چینِ نامناسب) پین شده بود؛ به `right: 0` اصلاح شد.
- نوارِ بالای صفحه (`topbar`) هنگامِ اسکرول، به‌خاطرِ نداشتنِ پس‌زمینه، اجازه می‌داد محتوای زیرش از پشتش دیده شود؛ با افزودنِ `background`/`box-shadow` رفع شد.
- در پنلِ «هشدارها و یادآوری‌ها»، هر نوعِ هشدارِ غیر از `warning` به‌اشتباه با استایلِ قرمز/خطرناک (`danger`) نمایش داده می‌شد؛ با افزودنِ یک فهرستِ صریحِ `validTypes` در `app.js` اصلاح شد.

---

## نکته‌ی مهم برایِ AI بعدی

اگر در آینده مخزنِ Git با تاریخچه‌ی Commit در دسترس قرار گرفت، بخشِ «بدونِ تاریخِ دقیق» بالا باید بازنویسی و با تاریخ‌ها/شناسه‌های Commitِ واقعی جایگزین شود؛ تا آن زمان، منبعِ صحتِ این بخش صرفاً کامنت‌های داخلِ کد و مستنداتِ موجود است، نه یک لاگِ رسمی.
