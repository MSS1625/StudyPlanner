Django>=5.2,<5.3
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.4
# درایورِ PostgreSQL (نسخه‌ی ۳) — فقط وقتی لازم می‌شود که DATABASE_URL به
# یک سرورِ PostgreSQL اشاره کند؛ بدونِ این متغیر، همان SQLiteِ توسعه فعال است.
psycopg[binary]>=3.2,<4
