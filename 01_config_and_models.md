# مستند فنی پروژه — بخش ۱ از ۴: پیکربندی پروژه و مدل‌های داده

**پروژه: برنامه‌ریز هوشمند مطالعه (Smart Study Planner)**

> این مستند برای توضیحِ دقیق و کامل پروژه تهیه شده است: این‌که هر فایل از پروژه دقیقاً چه کاری انجام می‌دهد.
> به‌دلیل حجم بالای توضیحات، این مستندسازی در ۴ فایل جداگانه ارائه می‌شود:
>
> - **بخش ۱ (همین فایل):** پیکربندی پروژه (Django Settings/URLs) و مدل‌های داده (`models.py`, `admin.py`)
> - **بخش ۲:** منطق سرور و API (`serializers.py`, `views.py`, `urls.py` اپلیکیشن، `utils.py`، دستور `seed_demo_data`)
> - **بخش ۳:** صفحات رابط کاربری (۷ فایل HTML)
> - **بخش ۴:** منطق فرانت‌اند و طراحی بصری (`app.js` و `styles.css`)

---

## فهرست فایل‌های این بخش

| فایل | مسیر |
|---|---|
| `manage.py` | `backend/manage.py` |
| `settings.py` | `backend/backend/settings.py` |
| `urls.py` (پروژه) | `backend/backend/urls.py` |
| `wsgi.py` / `asgi.py` | `backend/backend/` |
| `models.py` | `backend/planner/models.py` |
| `admin.py` | `backend/planner/admin.py` |
| `apps.py` | `backend/planner/apps.py` |
| مهاجرت‌های دیتابیس (Migrations) | `backend/planner/migrations/` |

---

## ۱.۱ `backend/manage.py`

این فایل، **نقطه‌ی ورودِ خط‌فرمانِ** کل پروژه‌ی جنگو است. هر دستوری که با `python manage.py ...` اجرا می‌شود (مثل `runserver`, `migrate`, `createsuperuser`, یا حتی دستورِ سفارشیِ `seed_demo_data` که در بخش ۲ توضیح داده می‌شود) از همین فایل عبور می‌کند.

**کاری که می‌کند:**
1. متغیر محیطیِ `DJANGO_SETTINGS_MODULE` را برابر با `'backend.settings'` قرار می‌دهد؛ یعنی به جنگو می‌گوید تنظیمات پروژه را از فایل `backend/backend/settings.py` بخواند.
2. تابع `execute_from_command_line` را از جنگو وارد (Import) می‌کند و آرگومان‌های خط فرمان (`sys.argv`) را به آن پاس می‌دهد تا دستورِ درخواستی اجرا شود.
3. اگر جنگو اصلاً نصب نباشد (Import شکست بخورد)، یک پیامِ خطای خوانا نمایش می‌دهد به‌جای یک Traceback گنگ.

این فایل توسط خودِ دستور `django-admin startproject` تولید شده و طبق رویه‌ی استاندارد جنگو، دست‌نخورده باقی مانده است (نیازی به ویرایش نداشت).

---

## ۱.۲ `backend/backend/settings.py`

این فایل **مرکز پیکربندیِ کل پروژه** است؛ هر رفتار سراسری (چه اپلیکیشن‌هایی فعال‌اند، اتصال به دیتابیس، امنیت، زبان، فایل‌های استاتیک، تنظیمات JWT و DRF) از همین فایل خوانده می‌شود.

**بخش‌های کلیدی و کاری که هرکدام انجام می‌دهد:**

- **`SECRET_KEY` و `DEBUG`**: کلید امنیتیِ رمزنگاریِ داخلیِ جنگو (برای امضای Session، توکن‌های CSRF و...) و حالتِ توسعه (`DEBUG=True`) که خطاهای کامل را نمایش می‌دهد و فایل‌های استاتیک را خودکار سرو می‌کند. این تنظیمات مناسبِ محیط توسعه‌اند، نه انتشار نهایی (Production).

- **`INSTALLED_APPS`**: فهرست تمام اپلیکیشن‌های فعال. شامل اپ‌های داخلیِ جنگو (`admin`, `auth`, `staticfiles`...)، کتابخانه‌های شخص‌ثالث نصب‌شده با pip (`rest_framework` برای ساختِ API، `rest_framework_simplejwt` برای احراز هویتِ JWT، `corsheaders` برای مدیریتِ درخواست‌های Cross-Origin)، و در نهایت اپلیکیشنِ خودِ پروژه (`planner`) که تمام منطقِ دامنه در آن است.

- **`MIDDLEWARE`**: زنجیره‌ای از کدهایی که روی هر درخواست/پاسخ اجرا می‌شوند. `CorsMiddleware` عمداً نزدیک بالای لیست قرار گرفته تا هدرهای CORS به همه‌ی پاسخ‌ها اضافه شود؛ بقیه (Security، Session، CSRF، Authentication، Messages، Clickjacking) پیش‌فرض‌های استاندارد جنگو هستند.

- **`ROOT_URLCONF = 'backend.urls'`**: تعیین می‌کند نقشه‌ی اصلیِ مسیرها در کجاست (فایل بعدی که توضیح داده می‌شود).

- **`DATABASES`**: پیکربندیِ اتصال به دیتابیس. طبق محدودیت پروژه از **SQLite** استفاده شده (فایل `db.sqlite3` در ریشه‌ی `backend/`)، چون نیازی به نصب/راه‌اندازیِ یک سرور دیتابیسِ جداگانه نیست و برای مقیاسِ این پروژه (آموزشی/تک‌کاربره) کافی است.

- **`AUTH_PASSWORD_VALIDATORS`**: چهار قانونِ استانداردِ جنگو برای قدرتِ رمز عبور (شباهت به نامِ کاربری، حداقل طول، رمزهای رایج، رمزِ کاملاً عددی) که پیش از ذخیره‌ی هر رمز جدید اجرا می‌شوند.

- **`LANGUAGE_CODE`, `TIME_ZONE`, `USE_I18N`, `USE_TZ`**: تنظیمات بین‌المللی‌سازی و منطقه‌ی زمانی (UTC).

- **`STATIC_URL` و `STATICFILES_DIRS`**: اینجا یک تصمیمِ طراحیِ مهم گرفته شده: `STATICFILES_DIRS` به پوشه‌ی `static/` بیرون از `backend/` اشاره می‌کند (همان‌جایی که تمام فایل‌های HTML/CSS/JS رابط کاربری قرار دارند). نتیجه‌اش این است که وقتی سرور جنگو با `runserver` اجرا می‌شود، این فایل‌های استاتیک هم خودکار زیر آدرسِ `/static/...` در دسترس قرار می‌گیرند — یعنی نیازی به یک سرور جداگانه برای فرانت‌اند نیست.

- **`CORS_ALLOW_ALL_ORIGINS = True`**: به هر دامنه/مبدأیی اجازه می‌دهد به این API درخواست بزند. برای توسعه مناسب است؛ در استقرارِ واقعی باید با `CORS_ALLOWED_ORIGINS` به دامنه‌ی مشخصِ فرانت‌اند محدود شود.

- **`REST_FRAMEWORK`**: تنظیمات سراسریِ Django REST Framework. دو نکته‌ی کلیدی:
  - `DEFAULT_AUTHENTICATION_CLASSES`: کاربر از روی توکن JWT در هدرِ `Authorization` شناسایی می‌شود.
  - `DEFAULT_PERMISSION_CLASSES`: به‌صورت پیش‌فرض هیچ درخواستی بدونِ لاگین پذیرفته نمی‌شود؛ فقط مسیرهایی مثل ثبت‌نام/ورود صراحتاً با `AllowAny` این قانون را دور می‌زنند (توضیح در بخش ۲).

- **`SIMPLE_JWT`**: تنظیماتِ کتابخانه‌ی JWT — توکنِ «دسترسی» ۱ روز و توکنِ «تمدید» ۷ روز اعتبار دارد؛ هدر باید به‌شکلِ `Authorization: Bearer <token>` فرستاده شود.

---

## ۱.۳ `backend/backend/urls.py`

این فایل **نقشه‌ی سطحِ‌بالای مسیرهای کل پروژه** است (نه فقط اپلیکیشنِ planner). فقط دو خط منطقی دارد:

```python
path('admin/', admin.site.urls)
path('api/', include('planner.urls'))
```

یعنی:
- هر آدرسی که با `/admin/` شروع شود، به پنلِ مدیریتِ آماده‌ی جنگو می‌رود.
- هر آدرسی که با `/api/` شروع شود، به فایلِ `planner/urls.py` (نقشه‌ی مسیرهای خودِ اپلیکیشن، توضیح در بخش ۲) واگذار می‌شود. یعنی وقتی در `planner/urls.py` مسیر `subjects/` تعریف می‌شود، در عمل روی آدرسِ کاملِ `/api/subjects/` در دسترس خواهد بود.

این تفکیکِ دوسطحی (پروژه -> اپلیکیشن) یک الگوی استانداردِ جنگو است که امکان می‌دهد اگر روزی اپلیکیشنِ دومی به پروژه اضافه شود، مسیرهایش زیر پیشوندِ دیگری (مثلاً `/api/v2/`) جا بگیرد بدون تداخل با `planner`.

---

## ۱.۴ `backend/backend/wsgi.py` و `backend/backend/asgi.py`

این دو فایل، **نقطه‌ی اتصال پروژه به سرورهای وب واقعی** هستند (نه چیزی که در توسعه‌ی محلی مستقیماً با آن کار کنیم):

- `wsgi.py`: برای استقرار روی سرورهای همزمان-معمولی (Synchronous) مثل Gunicorn.
- `asgi.py`: برای استقرار روی سرورهای ناهمزمان (Asynchronous) که امکاناتی مثل WebSocket را هم پشتیبانی می‌کنند.

هردو فقط یک آبجکتِ `application` می‌سازند که سرورِ وب (در محیط Production) آن را صدا می‌زند. در این پروژه چون از دستورِ توسعه‌ایِ `runserver` استفاده می‌شود، این دو فایل عملاً دست‌نخورده و به‌همان شکلِ پیش‌فرضِ جنگو باقی مانده‌اند.

---

## ۱.۵ `backend/planner/apps.py`

یک فایلِ کوچکِ پیکربندیِ خودِ اپلیکیشنِ `planner`:

```python
class PlannerConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'planner'
```

فقط مشخص می‌کند نوعِ پیش‌فرضِ کلیدِ اصلی (Primary Key) برای مدل‌های این اپ چیست (`BigAutoField`، یعنی یک عددِ صحیحِ ۶۴بیتیِ خودافزا) و نامِ اپ چیست. این فایل هم استاندارد و اتوماتیک‌تولیدشده است.

---

## ۱.۶ `backend/planner/models.py` — قلبِ لایه‌ی داده

این فایل چهار **مدل** (معادلِ چهار جدول در دیتابیس) را تعریف می‌کند. هر مدل، یک کلاسِ پایتونی است که از `django.db.models.Model` ارث‌بری می‌کند؛ هر فیلد کلاس، یک ستونِ جدول است.

### رابطه‌ی کلیِ مدل‌ها با هم

```
User (مدل آماده‌ی جنگو)
 └── Subject (درس) — یک کاربر چند درس دارد
      └── Exam (امتحان) — یک درس چند امتحان دارد
           └── StudyLog (گزارش مطالعه) — یک امتحان چند گزارش مطالعه دارد
 └── StudyPlan (تنظیمات برنامه‌ریزی) — یک کاربر یک رکورد تنظیمات دارد
```

### کلاس `Subject`

نمایانگرِ یک «درس» که کاربر ثبت کرده است.

- `user`: کلید خارجی (ForeignKey) به مدلِ `User`؛ یعنی هر درس دقیقاً به یک کاربر تعلق دارد. با `on_delete=CASCADE`، اگر آن کاربر حذف شود، درس‌هایش هم خودکار حذف می‌شوند. `related_name='subjects'` یعنی از سمتِ کاربر می‌توان با `user.subjects.all()` به همه‌ی درس‌هایش دسترسی پیدا کرد.
- `name`: نامِ درس (رشته‌ای تا ۲۰۰ کاراکتر).
- `difficulty`: عددِ سختیِ درس بین ۱ تا ۵، با اعتبارسنجیِ `MinValueValidator`/`MaxValueValidator`؛ همین عدد مستقیماً در فرمولِ الگوریتمِ برنامه‌ریزی (بخش ۲) ضرب می‌شود.
- `target_grade`: هدف‌نمره‌ی اختیاریِ کاربر (بین ۰ تا ۲۰)؛ `null=True, blank=True` یعنی هم در دیتابیس هم در فرم، خالی‌گذاشتنش مجاز است.
- `notes`: یادداشتِ کوتاهِ اختیاری درباره‌ی درس (`null=True, blank=True`)؛ از 2026-08-25 به مدل اضافه شد (Migrationِ `0005_subject_notes`) — قبلاً فرمِ درس این مقدار را می‌فرستاد ولی مدل فیلدی نداشت و بی‌صدا دور ریخته می‌شد.
- `created_at` / `updated_at`: زمان ایجاد و آخرین ویرایش، هردو خودکار توسط جنگو پر می‌شوند.
- **`Meta.ordering = ['-created_at']`**: جدیدترین درس اول نمایش داده می‌شود.
- **`Meta.unique_together = ['user', 'name']`**: در سطحِ خودِ دیتابیس تضمین می‌کند یک کاربر دو درسِ هم‌نام نداشته باشد.
- **`__str__`**: تعیین می‌کند وقتی یک Subject در پنل ادمین یا در کنسول چاپ شود، چه متنی نشان داده شود (`"نام درس (نام‌کاربری)"`).

### کلاس `Exam`

نمایانگرِ یک «امتحان» متعلق به یک درسِ مشخص.

- `subject`: کلید خارجی به `Subject` (`related_name='exams'`، یعنی `subject.exams.all()`).
- `exam_date`: تاریخِ برگزاریِ امتحان؛ مهم‌ترین فیلد برای الگوریتمِ برنامه‌ریزی، چون «چند روز تا امتحان مانده» از همین محاسبه می‌شود.
- `chapters_remaining`: تعداد فصل‌های باقی‌مانده (بیشتر جنبه‌ی اطلاعاتی/نمایشی دارد؛ در فرمولِ اصلیِ الگوریتم مستقیماً استفاده نمی‌شود).
- `study_hours_remaining`: **مهم‌ترین فیلدِ کل پروژه**. یعنی چند ساعت مطالعه تا آمادگیِ کامل برای این امتحان باقی مانده. این مقدار هر بار که کاربر یک `StudyLog` جدید ثبت می‌کند، خودکار کم می‌شود (توضیح در ادامه‌ی همین فایل).
- **`Meta.ordering = ['exam_date']`**: نزدیک‌ترین امتحان همیشه اول لیست می‌شود.

### کلاس `StudyPlan`

نقشِ «تنظیماتِ برنامه‌ریزیِ هر کاربر» را دارد: چند ساعت در روز وقتِ آزاد دارد (`daily_available_hours`) و آخرین برنامه‌ی محاسبه‌شده (`plan_data`، یک فیلدِ `JSONField` که خروجیِ خامِ الگوریتم را ذخیره می‌کند، بیشتر برای تاریخچه/مرجع).

نکته‌ی مهم: در `views.py` (بخش ۲) با الگوی `get_or_create` از این مدل استفاده می‌شود، یعنی عملاً هر کاربر فقط یک رکوردِ تنظیمات دارد (نه چند رکوردِ جداگانه)، هرچند در سطحِ مدل هیچ محدودیتِ `unique` روی فیلدِ `user` وجود ندارد (این یکی از محدودیت‌های شناخته‌شده‌ای است که در README پروژه هم به آن اشاره شده).

### کلاس `StudyLog` — حلقه‌ی بازخوردِ سیستم

نمایانگرِ یک «گزارش مطالعه»: این‌که کاربر در یک تاریخِ مشخص، چند ساعت روی یک امتحانِ خاص کار کرده است.

- `user`, `exam`: کلیدهای خارجی به `User` و `Exam`.
- `date`: تاریخِ انجامِ مطالعه (پیش‌فرض: امروز، با `timezone.now`).
- `hours_studied`: چند ساعت مطالعه شده؛ حداقل ۰.۱ (یعنی صفر یا منفی مجاز نیست).
- `hours_deducted` (از 2026-09-03، Migrationِ `0007`): عددِ دقیقی که در لحظه‌ی ثبت از امتحانِ مربوطه کم شده است (کمینه‌ی `hours_studied` و ساعتِ موجودِ امتحان)؛ مبنای بازگرداندنِ ساعت در متدِ `delete()`. برای گزارش‌های قدیمی، Backfill خودِ Migration با «کلِ ساعتِ ثبت‌شده» پرش کرده است.
- `notes`: یادداشتِ اختیاری.

**مهم‌ترین بخشِ این کلاس، دو متدِ Override شده است: `save()` و (از 2026-09-03) `delete()`:**

```python
def save(self, *args, **kwargs):
    is_new = self.pk is None
    if is_new:
        # عددِ دقیقِ کسرشده، «قبل» از INSERT (کمینه‌ی ساعتِ مطالعه و ساعتِ موجود)
        if self.exam.study_hours_remaining > 0:
            self.hours_deducted = min(
                self.hours_studied, self.exam.study_hours_remaining
            )
        else:
            self.hours_deducted = 0.0

    with transaction.atomic():
        super().save(*args, **kwargs)
        if is_new and self.hours_deducted > 0:
            self.exam.study_hours_remaining -= self.hours_deducted
            if self.exam.study_hours_remaining < 0:  # محافظِ گردکردنِ اعشار
                self.exam.study_hours_remaining = 0
            self.exam.save()

def delete(self, *args, **kwargs):
    exam = getattr(self, 'exam', None)
    refund = self.hours_deducted or 0.0
    with transaction.atomic():
        super().delete(*args, **kwargs)
        if exam is not None and refund > 0:
            exam.study_hours_remaining += refund
            exam.save()
```

توضیح دقیق:
1. `save()` ابتدا بررسی می‌کند رکورد **جدید** است یا **ویرایش** (`self.pk is None`)؛ عددِ کسرشده (کمینه‌ی ساعتِ مطالعه و ساعتِ موجودِ امتحان) «قبل» از ذخیره محاسبه می‌شود تا همراهِ همان INSERT در دیتابیس بنشیند؛ سپس داخلِ یک تراکنشِ اتمیک، خودِ گزارش ذخیره و ساعتِ امتحان کم می‌شود (یا هر دو، یا هیچ‌کدام).
2. `delete()` قرینه‌ی آن است: مبلغِ بازگشتی همان `hours_deducted` است، نه `hours_studied` — این تفاوت در حالتِ «کسرِ محدود» حیاتی است: اگر ۲ ساعت مانده بوده و کاربر ۵ ساعت ثبت کرده باشد، فقط ۲ ساعت کسر شده و حذفِ گزارش هم دقیقاً ۲ ساعت برمی‌گرداند (وگرنه ساعتِ امتحان از مقدارِ اولیه‌اش بیشتر می‌شد).
3. شرطِ محافظتیِ `if < 0: = 0` فقط خطاهای گردکردنِ اعدادِ اعشاری را می‌گیرد؛ خودِ `min` تضمینِ اصلیِ نامنفی‌بودن است.
4. نکته‌ی حذفِ آبشاری: مسیرِ BulkDeleteِ سریعِ جنگو (حذفِ خودِ امتحان، حذفِ دسته‌ایِ QuerySet) این متدها را صدا نمی‌زند — که درست هم هست، چون در حذفِ امتحان، دیگر امتحانی نمی‌ماند که ساعتی به آن برگردد.

این طراحی یک تصمیمِ مهندسیِ آگاهانه است: به‌جای این‌که در `views.py` هر بار دستی این کسر/بازگشت انجام شود، منطق داخلِ خودِ مدل نگه‌داری می‌شود — یعنی هرجای برنامه (API، پنل ادمین، دستورِ `seed_demo_data`) که یک `StudyLog` ساخته یا حذف شود، این به‌روزرسانی خودکار و تضمین‌شده اتفاق می‌افتد؛ دیگر نیازی نیست هرکس که کدِ جدید می‌نویسد، این منطق را حفظ کند یا فراموش نکند.

---

## ۱.۷ `backend/planner/admin.py`

مدل‌ها را در **پنل مدیریتِ آماده‌ی جنگو** (آدرسِ `/admin/`) ثبت می‌کند تا بدونِ نوشتنِ هیچ View/Templateِ اضافه‌ای، بشود مستقیماً داده‌ها را در مرورگر دید، جست‌وجو کرد و دستی ویرایش کرد.

هر چهار مدل (`Subject`, `Exam`, `StudyPlan`, `StudyLog`) با دکوریتورِ `@admin.register(...)` ثبت شده‌اند و برای هرکدام:

- **`list_display`**: کدام ستون‌ها در جدولِ فهرست دیده شوند (مثلاً برای `Subject`: `id, name, difficulty, target_grade, user, created_at`).
- **`list_filter`**: امکانِ فیلترکردن از نوارِ کناریِ پنل (مثلاً فیلترِ درس‌ها بر اساسِ سختی).
- **`search_fields`**: کدام فیلدها با جعبه‌ی جست‌وجو قابل‌جست‌وجو باشند. نکته‌ی فنیِ جالب: در `StudyLogAdmin`، مقدارِ `search_fields = ['user__username', 'exam__subject__name']` استفاده شده — یعنی جست‌وجو می‌تواند از روی چند پله‌ی رابطه (StudyLog -> Exam -> Subject -> name) عبور کند، که یکی از قابلیت‌های قدرتمندِ ORM جنگو است.

---

## ۱.۸ مهاجرت‌های دیتابیس (`backend/planner/migrations/`)

پوشه‌ی `migrations` شاملِ ۷ فایلِ نسخه‌بندی‌شده است که **تاریخچه‌ی تکاملِ ساختارِ دیتابیس** را ثبت می‌کنند. هر فایل، یک تغییرِ مشخص را توصیف می‌کند و جنگو با اجرای `python manage.py migrate` آن‌ها را به‌ترتیب روی دیتابیس اعمال می‌کند:

- **`0001_initial.py`**: ساختِ اولیه‌ی جدول‌ها (`Exam` و بقیه‌ی مدل‌ها) با فیلدهای اولیه.
- **`0002_...`**: حذفِ محدودیتِ یکتاییِ قبلیِ `Subject`، افزودنِ فیلدِ `target_grade`، و اصلاحِ اعتبارسنجی‌های `chapters_remaining`/`exam_date`.
- **`0003_...`**: حذفِ یک فیلدِ قدیمیِ نامرتبط از `StudyPlan` (`exam`)، و تنظیمِ مجددِ `unique_together` روی `Subject` به‌شکلِ `('user', 'name')`.
- **`0004_...`**: افزودنِ مقدارِ پیش‌فرض (`default`) به `chapters_remaining` و `study_hours_remaining`، و اصلاحِ نوعِ فیلدِ `difficulty`.
- **`0005_subject_notes.py`** (2026-08-25): افزودنِ فیلدِ `notes` به `Subject`.
- **`0006_exam_notes.py`** (2026-08-29): افزودنِ فیلدِ `notes` به `Exam`.
- **`0007_studylog_hours_deducted.py`** (2026-09-03): افزودنِ فیلدِ `hours_deducted` به `StudyLog` + عملیاتِ Backfill برای گزارش‌های قدیمی (اولین Migrationِ این پروژه که علاوه بر تغییرِ اسکیما، داده‌ی موجود را هم اصلاح می‌کند).

این فایل‌ها **دستی نوشته نشده‌اند** — جنگو آن‌ها را خودکار از روی تفاوتِ بینِ `models.py` فعلی و آخرین حالتِ ثبت‌شده در دیتابیس تولید می‌کند (با دستورِ `python manage.py makemigrations`). وجودِ این تاریخچه دقیقاً نشان می‌دهد مدلِ داده‌ی پروژه در طولِ توسعه چند بار به‌تدریج اصلاح شده (مثلاً هدف‌نمره بعداً اضافه شده، محدودیتِ یکتایی بعداً درست شده)، که رفتارِ طبیعیِ یک پروژه‌ی واقعی است، نه یک طراحیِ یک‌باره و کامل از ابتدا.

---

*پایان بخش ۱ از ۴. برای ادامه («منطق سرور و API»)، به فایل بعدی مراجعه کنید.*
