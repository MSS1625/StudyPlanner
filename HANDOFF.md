# HANDOFF.md

> **این فایل را همیشه اول بخوان.** هدفش این است که اگر یک گفتگوی کاملاً تازه با یک AI دیگر شروع شود، همان AI فقط با خواندنِ همین یک فایل بفهمد پروژه دقیقاً کجاست و باید از کجا ادامه دهد. این فایل خلاصه یا کپیِ `README.md`/`AI_CONTEXT.md` نیست؛ فقط روی «الان کجاییم و بعدش چی» تمرکز دارد.

---

## پروژه در چه مرحله‌ای است؟

**عملیاتی و کاملاً کاربردی، ولی نه Production-ready.** هر ۸ قابلیتِ اصلیِ فهرست‌شده در `README.md` بخشِ ۸ (احراز هویت، مدیریتِ درس/امتحان، ثبتِ مطالعه، برنامه‌ی هوشمند، داشبورد، داده‌ی نمونه، رابطِ واکنش‌گرا) پیاده‌سازی و تأیید شده‌اند. تنظیماتِ امنیتی از 2026-09-06 با متغیرهایِ محیطی، از 2026-09-08 با چرخش/ابطالِ توکنِ JWT و از 2026-09-09 با قفلِ هم‌زمانیِ ثبتِ گزارش، آماده‌ی Production است (پیش‌فرض‌ها برایِ توسعه‌اند؛ جدولِ متغیرها در `README.md` بخشِ ۱۰).

مخزنِ Git اکنون روی GitHub در دسترس است (با تاریخچه‌ی Commit). آخرین کارِ ثبت‌شده در تاریخ 2026-08-25: **رفعِ هر دو باگِ بخشِ Bugsِ `TODO.md`** با محیطِ Django واقعی (5.2.17 در venv، نصب‌شده طبق `requirements.txt`) — شرحِ کامل در `CHANGELOG.md` بخشِ 2026-08-25. خلاصه‌ی تغییرات:

- فیلدِ `notes` به مدلِ `Subject` اضافه شد (Migrationِ `0005_subject_notes`) و واقعاً ذخیره/نمایش داده می‌شود.
- فیلدِ `total_hours` از فرمِ درس حذف شد (تداخلِ معنایی با فیلدِ محاسباتیِ هم‌نام).
- تابعِ `resetExamForm()` در `app.js` تعریف شد (رفعِ ReferenceErrorِ بالقوه).
- رفعِ حرفِ سرگردانِ «آ» در کشویِ مرتب‌سازیِ `subjects.html` و کلاسِ CSSِ تعریف‌نشده‌ی `.text-muted`.
- اعتبارسنجی با ۳۳ تستِ دودِ موفق (۲۲ بک‌اند + ۱۱ فرانت‌اند).

## آخرین تغییراتِ مهم

طبقِ `CHANGELOG.md`، آخرین تغییرِ مهم (2026-09-09): **قفلِ هم‌زمانیِ گزارشِ مطالعه: select_for_update + خواندنِ تازه** — کسر و بازگشتِ ساعتِ امتحان از این پس داخلِ تراکنش، رویِ سطرِ قفل‌شده و «تازه‌خوانده‌شده» انجام می‌شود (مبنایِ محاسبه = مقدارِ لحظه‌ایِ دیتابیس، نه snapshotِ حافظه)؛ Lost Update در درخواست‌هایِ هم‌زمان بسته شد و پیش‌نیازِ مهاجرت به PostgreSQL از سمتِ گزارشِ مطالعه کامل شد (۷ تستِ جدید، مجموع ۱۰۳). پیش‌تر (2026-09-08): **سخت‌ترکردنِ JWT: چرخشِ توکنِ Refresh + لیستِ سیاه + خروجِ سرور-محور** — هر تمدید توکنِ Refreshِ تازه صادر می‌کند و توکنِ قبلی بلافاصله باطل می‌شود؛ خروجِ کاربر هم توکنش را رویِ سرور ابطال می‌کند (`POST /api/auth/logout/`؛ ۷ تستِ جدید، مجموع ۹۶). پیش‌تر (2026-09-06): **تنظیماتِ امنیتیِ Production از متغیرهایِ محیطی** — پنج متغیرِ `DJANGO_SECRET_KEY`/`DJANGO_DEBUG`/`DJANGO_ALLOWED_HOSTS`/`DJANGO_CORS_ALLOW_ALL`/`DJANGO_ALLOWED_ORIGINS` با پیش‌فرض‌هایِ توسعه (بدونِ هیچ متغیری = همانِ رفتارِ قبلی) + سپرِ راه‌اندازیِ Production و ۷ تستِ بوتِ واقعی (مجموع ۸۹). پیش‌تر در همان تاریخ: **تمدیدِ خودکارِ نشست** — مسیرِ `POST /api/auth/refresh/` + ذخیره‌ی refresh token + تمدیدِ بی‌صدا و تلاشِ مجدد در 401 + هدایت به `login.html?expired=1` وقتی نشست تمام است (۳ تستِ جدید، مجموع ۸۲). پیش‌تر در همان تاریخ: **صفحه‌بندیِ اختیاریِ endpointهای فهرستی** — کلاسِ `OptionalPageNumberPagination` رویِ ViewSetهای درس/امتحان/گزارش؛ بدونِ `?page=`/`?page_size=` پاسخ همان لیستِ کاملِ قبلی است (فرانت‌اندِ فعلی دست‌نخورده)، سقفِ اندازه ۱۰۰ و ۹ تستِ جدید (مجموع ۷۹). پیش‌تر (2026-09-05): **یکتاییِ `StudyPlan.user` در سطحِ دیتابیس** — تبدیل به `OneToOneField` با Migrationِ `0008` (پاکسازیِ رکوردهایِ تکراریِ احتمالی + قیدِ UNIQUE)، رگرسیونِ API (POST دوباره → ۴۰۰، نه ۵۰۰) و ۳ تستِ جدید (مجموع ۷۰). پیش‌تر  (2026-09-04): **بستنِ کاملِ XSS در رندرِ فرانت‌اند** — همه‌ی رندرکننده‌های `app.js` (Subjects/StudyLogs/Dashboard/Plan/Exams) داده‌ی متنیِ API را با `escapeHtml` پاک‌سازی می‌کنند؛ گاردِ رگرسیونِ رفتاری + استاتیک هم اضافه شد. پیش‌تر (2026-09-03): **بازگرداندنِ ساعتِ کسرشده هنگامِ حذفِ گزارشِ مطالعه** — فیلدِ `hours_deducted` (Migrationِ `0007` با Backfill برای گزارش‌های قدیمی)، متدِ `StudyLog.delete()` اتمیک، اتمیک‌شدنِ `save()`، ۵ تست جدید (مجموع ۶۷) و به‌روزرسانیِ فرانت‌اند (پیامِ confirm و رفرشِ کشویِ امتحان‌ها). پیش‌تر (2026-08-29): مجموعه‌تستِ ۶۲ موردی + رفعِ خطای ۵۰۰ِ نامِ درسِ تکراری، بازطراحیِ دکمه‌های عملیات، قابلیتِ «ویرایشِ امتحان» + Migrationِ `0006`. پیش از آن (2026-08-25): رفعِ دو باگِ نخستِ `TODO.md`.

قبل از آن، طبقِ بخشِ «بدونِ تاریخِ دقیق»یِ `CHANGELOG.md`:

1. کامنت‌گذاریِ توضیحیِ کاملِ فارسی در تمامِ فایل‌های کدِ پروژه (بک‌اند و فرانت‌اند)، **بدونِ هیچ تغییرِ منطقی**.
2. ساختِ مستنداتِ تکمیلیِ فایل‌به‌فایل: `docs/01_config_and_models.md` تا `docs/04_frontend_logic_and_design.md`.
3. رفعِ چند باگِ فرانت‌اندی (سایدبارِ RTL در موبایل، پس‌زمینه‌ی نوارِ بالا هنگامِ اسکرول، رنگ‌بندیِ نادرستِ برخی هشدارها) و بازطراحیِ نمودارِ «تقسیم‌زمانِ پیشنهادی» و نمایِ «برنامه‌ی هفتگی».
4. اضافه‌شدنِ `README.md` جامع، `.gitignore`، و `backend/requirements.txt`.

## الان چه چیزی «در حالِ توسعه» است؟

**هیچ تغییرِ کدی در حالِ انجام نیست.** در 2026-08-25 هر دو باگِ بخشِ Bugsِ `TODO.md` رفع و مستندسازی شد. مواردِ باقی‌مانده (به‌ترتیبِ پیشنهادیِ کم‌ریسک‌ترین اول):

1. ~~تکمیلِ «ویرایشِ امتحان»~~ — ✅ در 2026-08-29 انجام شد: دکمه‌ی «ویرایش» روی ردیف‌ها، تشخیصِ POST/PATCH در `handleExamForm`، حلِ فیلدِ یتیمِ `note` (Migrationِ `0006`)، رفعِ کلاس‌های CSSِ ناموجودِ فرمِ امتحان و تست‌های دودِ ۲۴+۵۰ موردی.
2. ~~بازگرداندنِ ساعت هنگامِ حذفِ `StudyLog`~~ — ✅ در 2026-09-03 انجام شد: فیلدِ `hours_deducted` (Migrationِ `0007`) عددِ دقیقِ کسرشده را نگه می‌دارد و متدِ `delete()` رویِ مدل آن را اتمیک برمی‌گرداند؛ ۵ تست جدید هم اضافه شد (مجموع ۶۷).
3. ~~افزودنِ تست‌های خودکار به `tests.py`~~ — ✅ در 2026-08-29 انجام شد: ۶۲ تست در هفت کلاس + رفعِ باگِ ۵۰۰ِ نامِ درسِ تکراری (کشفِ خودکار توسطِ همان تست‌ها).

4. ~~رفعِ XSS باقی‌مانده در فرانت‌اند~~ — ✅ در 2026-09-04 انجام شد: همه‌ی رندرکننده‌ها (`renderSubjects`/`renderStudyLogs`/`renderDashboard`/`renderPlan`) با همان `escapeHtml` موجود اصلاح شدند؛ ۳۰ تستِ رفتاری + اسکنرِ استاتیکِ innerHTML (فهرستِ سفیدِ صریح) رگرسیون را می‌گیرند.
5. ‌‌محدودیتِ یکتاییِ `StudyPlan.user` در سطحِ دیتابیس‌‌ — ✅ در 2026-09-05 انجام شد: `OneToOneField` + Migrationِ `0008` (پاکسازیِ تکراری‌ها + قیدِ UNIQUE)، رگرسیونِ API (POST دوباره → ۴۰۰) و ۳ تستِ جدید (مجموع ۷۰).
6. ‌‌افزودنِ Pagination به Endpointهای فهرستی‌‌ — ✅ در 2026-09-06 انجام شد: کلاسِ `OptionalPageNumberPagination` (اختیاری با `?page=`/`?page_size=`، سقفِ ۱۰۰، پیش‌فرضِ ۲۰، صفحه‌ی نامعتبر → ۴۰۴) + ۹ تستِ جدید (مجموع ۷۹).

7. ‌‌هدایتِ خودکار به login.html هنگامِ انقضای توکن + ذخیره‌ی refresh token‌‌ — ✅ در 2026-09-06 انجام شد: مسیرِ `auth/refresh/` + `refreshAccessToken` با dedupe + `forceLogoutExpired` با `?expired=1`؛ E2E مرورگرِ واقعی هم سبز.

8. ‌‌تنظیماتِ امنیتیِ Production‌‌ — ✅ در 2026-09-06 انجام شد: پنج متغیرِ محیطی با پیش‌فرض‌هایِ dev-safe + سپرِ راه‌اندازی (DEBUG=false بدونِ کلیدِ واقعی یا بدونِ Host → ImproperlyConfigured) + ۷ تستِ جدید (مجموع ۸۹).
9. ‌‌سخت‌ترکردنِ JWT برای Production‌‌ — ✅ در 2026-09-08 انجام شد: چرخش/لیستِ سیاهِ توکنِ Refresh + خروجِ سرور-محور (`POST /api/auth/logout/`) + ۷ تستِ جدید (مجموع ۹۶).
10. ‌‌قفلِ هم‌زمانیِ StudyLog (select_for_update)‌ — ✅ در 2026-09-09 انجام شد: کسر/بازگشتِ ساعت رویِ سطرِ قفل‌شده و تازه‌خوانده‌شده‌یِ امتحان (Lost Update بسته شد؛ ترتیبِ قفلِ یکسان در save/delete برای جلوگیری از Deadlock) + ۷ تستِ جدید (مجموع ۱۰۳).
11. ‌‌پشتیبانیِ PostgreSQL از طریقِ DATABASE_URL‌ — ✅ در 2026-09-09 انجام شد: موتورِ دیتابیس با متغیرِ `DATABASE_URL` عوض می‌شود (بدونِ متغیر = همان SQLiteِ توسعه؛ با `postgres://` = PostgreSQL + درایورِ psycopg در requirements.txt)؛ خطاهایِ URL/درایور همانِ بوت با پیامِ راهنما متوقف می‌شوند؛ قفلِ FOR UPDATE در PostgreSQL واقعی است؛ ۱۳ تستِ جدید (مجموع ۱۱۶) که روی هر دو موتور سبزند.
12. ‌‌چندزبانی (fa/en)‌ — ✅ در 2026-09-09 انجام شد: بک‌اند با `LocaleMiddleware` + هدرِ Accept-Language + کاتالوگِ locale/en (msgid = متنِ فارسی؛ django.mo کامپایل‌شده commit شده)؛ فرانت‌اند با static/i18n.js (دیکشنریِ ۱۷۴ کلیدی + t()) و دکمه‌ی #langToggle در هر ۷ صفحه؛ app.js هدرِ Accept-Language می‌فرستد؛ بدونِ انتخابِ زبان همانِ فارسیِ قبلی؛ ۱۵ تستِ جدید (مجموع ۱۳۱) + ۴۱ چکِ Node خارج از ریپو.

این موارد دقیقاً و کامل در `TODO.md` مستند شده‌اند.

## آخرین Taskِ انجام‌شده

2026-09-09 — چندزبانی (fa/en): بک‌اند (LocaleMiddleware + Accept-Language + کاتالوگِ locale/en با django.mo کامپایل‌شده‌ی commit‌شده) + فرانت‌اند (i18n.js با دیکشنریِ ۱۷۴ کلیدی + دکمه‌ی تعویضِ زبان + جهتِ LTR در حالتِ en)؛ بدونِ انتخابِ زبان همانِ فارسیِ قبلی؛ ۱۵ تستِ جدید (مجموع ۱۳۱)؛ مستندات (CHANGELOG/TODO/این فایل/README/AI_CONTEXT/docs 01+02+04) به‌روز شدند.
2026-09-09 — پشتیبانیِ PostgreSQL: متغیرِ محیطیِ DATABASE_URL موتورِ دیتابیس را عوض می‌کند (پیش‌فرضِ امن = همانِ SQLiteِ توسعه؛ `postgres://` = PostgreSQL با درایورِ psycopg — در requirements.txt)؛ خطاهایِ URL/درایور همانِ بوت با پیامِ راهنما متوقف می‌شوند؛ قفلِ FOR UPDATE حالا در PostgreSQL واقعی است (رگرسیونِ SQL)؛ ۱۳ تستِ جدید (مجموع ۱۱۶) که روی هر دو موتور سبزند؛ مستندات (CHANGELOG/TODO/این فایل/README/AI_CONTEXT/docs 01+02) به‌روز شدند.
2026-09-09 — قفلِ هم‌زمانیِ گزارشِ مطالعه: `StudyLog.save()`/`delete()` امتحان را داخلِ تراکنش با `Exam.objects.select_for_update().get(pk=...)` قفل و «تازه» می‌خوانند (مبنایِ کسر/بازگشتِ ساعت = مقدارِ لحظه‌ایِ دیتابیس، نه snapshotِ حافظه — Lost Update بسته شد؛ ترتیبِ قفلِ یکسان در دو متد برای جلوگیری از Deadlock؛ گزارشِ یتیم فقط خودش حذف می‌شود، بدونِ resurrection)؛ در SQLite قفلِ FOR UPDATE بی‌اثر ولی خواندنِ تازه مؤثر است و قفلِ واقعی در PostgreSQL فعال می‌شود؛ ۷ تستِ جدید (مجموع ۱۰۳)؛ مستندات (CHANGELOG/TODO/این فایل/README/AI_CONTEXT/docs 01+02) به‌روز شدند.
2026-09-08 — سخت‌ترکردنِ JWT: `ROTATE_REFRESH_TOKENS`/`BLACKLIST_AFTER_ROTATION` فعال + اپِ `token_blacklist` در INSTALLED_APPS (یک‌بار migrate لازم) + مسیرِ `POST /api/auth/logout/` (TokenBlacklistView) + خروجِ سرور-محور در app.js (اول ابطال، بعد پاک‌سازی؛ fail-open در قطعیِ سرور)؛ ۷ تستِ جدید در `JWTTokenRotationBlacklistTests` (مجموع ۹۶)؛ مستندات (CHANGELOG/TODO/این فایل/README/AI_CONTEXT/docs 01+02+04) به‌روز شدند.

## Task بعدی (پیشنهادی، بر مبنای `TODO.md`)

ترتیبِ طبیعی (کم‌ریسک‌ترین اول):

1. **افزودنِ مؤلفه‌ی یادگیریِ آماری/ماشینی** — ایده‌ی رگرسیونِ ساده روی داده‌هایِ تاریخیِ `StudyLog` (شرح در `TODO.md` بخشِ Improvements) — آخرینِ آیتمِ باقی‌مانده‌ی TODO.

## مشکلاتِ شناخته‌شده

فهرستِ کامل در `TODO.md` (بخش‌های Bugs، High/Medium/Low Priority). همه‌ی آیتم‌هایِ امنیتیِ شناخته‌شده بسته شده‌اند (XSS در 2026-09-04، یکتاییِ `StudyPlan.user` در 2026-09-05، Pagination/هدایتِ انقضایِ توکن/تنظیماتِ محیطیِ Production در 2026-09-06، چرخش/لیستِ سیاهِ JWT در 2026-09-08، قفلِ هم‌زمانیِ گزارشِ مطالعه در 2026-09-09)؛ باقی‌مانده‌هایِ اصلی: فقط مؤلفه‌ی ML (Improvements) — چندزبانی و PostgreSQL در 2026-09-09 بسته شدند.

## چه بخش‌هایی نباید بدونِ دلیلِ مشخص تغییر کنند؟

- **`README.md`** — طبقِ قانونِ مستندسازیِ این پروژه، بدونِ دلیلِ صریح ویرایش نشود.
- **۱۴ نکته‌ی بخشِ ۶ در `AI_CONTEXT.md`** — این‌ها Invariantهای حیاتی‌اند (مثلِ محلِ کسرِ ساعت در `StudyLog.save()`، ترتیبِ `router.register`، فیلترِ اجباریِ `request.user` در هر `get_queryset`). نقضِ هرکدام باگ یا آسیب‌پذیریِ امنیتی ایجاد می‌کند.
- **فرمولِ الگوریتم در `utils.py`** — طراحیِ فعلی (وزن‌دهیِ متناسب + ادغامِ روزهای هم‌برنامه + توزیعِ بر‌مبنایِ درس نه روز) نتیجه‌ی رفعِ چند مشکلِ طراحیِ قبلی است (شرح در `CHANGELOG.md`، بخشِ Changed). قبل از تغییرِ این فرمول‌ها، حتماً همان بخش را بخوان تا مشکلاتِ قبلی دوباره رخ ندهند.

## اولین فایل‌هایی که یک AI تازه باید بخواند (به‌ترتیب)

1. **همین فایل (`HANDOFF.md`)**
2. `AI_CONTEXT.md` — به‌خصوص بخشِ ۶ (نکاتِ حیاتی)
3. `TODO.md` — برای دیدنِ دقیقِ کارِ باقی‌مانده
4. اگر روی بخشِ خاصی کار می‌کند: `01` تا `04` (فایل‌های شماره‌دار در ریشه‌ی پروژه) (بسته به این‌که بک‌اند/پیکربندی، API/الگوریتم، صفحاتِ HTML، یا `app.js`/`styles.css` مدِ نظر است)
5. `README.md` فقط اگر توضیحِ روایی/معماریِ کامل لازم باشد (اغلبِ اطلاعاتِ عملیاتی در ۴ فایلِ بالا خلاصه شده)

## محدودیت‌های محیطِ ابزاری (نکته‌ی عملیاتی مهم)

**تاریخچه:** در محیطِ ساختِ مستنداتِ اولیه، Django/pip نصب نبود و تغییراتِ آن دوره فقط با `python3 -m py_compile`، `node --check` و تست‌های شبیه‌سازی‌شده‌ی دستی صحت‌سنجی می‌شدند.

**وضعیت فعلی (از 2026-08-25):** محیطِ کاری دارای venv با Django 5.2.17 و DRF 3.18 (نصب‌شده از `requirements.txt`) است؛ `manage.py check` و `migrate` با موفقیت اجرا شده و رفعِ باگ‌ها با ۳۳ تستِ دودِ واقعی (۲۲ بک‌اند + ۱۱ فرانت‌اند) اعتبارسنجی شده است. در ادامه‌ی کار هم توصیه می‌شود قبل از هر تغییرِ بزرگ، `python manage.py check` را اجرا و بعد از تغییر، تست‌های خودکارِ داخلِ مخزن (`python manage.py test planner` از پوشه‌ی `backend` — از 2026-08-29 موجود است) و در صورت نیاز تست‌های دودِ قدیمیِ `scripts/` را دوباره اجرا کنید.

## هر نکته‌ی مهمِ دیگر

- کاربر (صاحبِ پروژه) فارسی‌زبان است و پروژه را برایِ ارائه/دفاعِ دانشگاهی هم استفاده می‌کند؛ لحنِ توضیحات باید دقیق و صادقانه باشد (شاملِ اذعان به محدودیت‌ها)، نه فقط تبلیغاتی.
- پروژه به‌تازگی روی GitHub قرار گرفته (طبقِ گفتگوهایِ قبلی که در فایل‌ها منعکس نیستند، ولی وجودِ `.gitignore` آماده این را تأیید می‌کند)؛ پس تغییراتِ آینده احتمالاً باید با `git add`/`commit`/`push` هم همراه شوند، نه فقط تحویلِ فایل.
