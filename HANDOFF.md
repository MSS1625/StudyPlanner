# HANDOFF.md

> **این فایل را همیشه اول بخوان.** هدفش این است که اگر یک گفتگوی کاملاً تازه با یک AI دیگر شروع شود، همان AI فقط با خواندنِ همین یک فایل بفهمد پروژه دقیقاً کجاست و باید از کجا ادامه دهد. این فایل خلاصه یا کپیِ `README.md`/`AI_CONTEXT.md` نیست؛ فقط روی «الان کجاییم و بعدش چی» تمرکز دارد.

---

## پروژه در چه مرحله‌ای است؟

**عملیاتی و کاملاً کاربردی، ولی نه Production-ready.** هر ۸ قابلیتِ اصلیِ فهرست‌شده در `README.md` بخشِ ۸ (احراز هویت، مدیریتِ درس/امتحان، ثبتِ مطالعه، برنامه‌ی هوشمند، داشبورد، داده‌ی نمونه، رابطِ واکنش‌گرا) پیاده‌سازی و تأیید شده‌اند. تنظیماتِ امنیتی (`DEBUG=True` و مشابه) هنوز در سطحِ توسعه هستند، نه استقرارِ واقعی.

هیچ مخزنِ Git با تاریخچه‌ی Commit در این نسخه از فایل‌ها در دسترس نبود؛ پروژه احتمالاً به‌تازگی به GitHub منتقل شده یا در حالِ انتقال است (شواهد: وجودِ `.gitignore` آماده در ریشه‌ی پروژه).

## آخرین تغییراتِ مهم

طبقِ `CHANGELOG.md` (بخشِ «بدونِ تاریخِ دقیق»)، آخرین دسته از تغییراتِ مهم که در کدِ فعلی قابل‌تأیید هستند:

1. کامنت‌گذاریِ توضیحیِ کاملِ فارسی در تمامِ فایل‌های کدِ پروژه (بک‌اند و فرانت‌اند)، **بدونِ هیچ تغییرِ منطقی**.
2. ساختِ مستنداتِ تکمیلیِ فایل‌به‌فایل: `docs/01_config_and_models.md` تا `docs/04_frontend_logic_and_design.md`.
3. رفعِ چند باگِ فرانت‌اندی (سایدبارِ RTL در موبایل، پس‌زمینه‌ی نوارِ بالا هنگامِ اسکرول، رنگ‌بندیِ نادرستِ برخی هشدارها) و بازطراحیِ نمودارِ «تقسیم‌زمانِ پیشنهادی» و نمایِ «برنامه‌ی هفتگی» تا واقعاً معنادار باشند (جزئیات در `CHANGELOG.md`).
4. اضافه‌شدنِ `README.md` جامع، `.gitignore`، و `backend/requirements.txt`.
5. همین الان (این commit مستندسازی): ساختِ `AI_CONTEXT.md`, `CHANGELOG.md`, `TODO.md`, و همین فایلِ `HANDOFF.md`.

## الان چه چیزی «در حالِ توسعه» است؟

**هیچ تغییرِ کدی در حالِ انجام نیست.** آخرین درخواستِ کاربر پیش از این مستندسازی، شروعِ کار روی سه موردِ زیر بود (که در `TODO.md` هم ثبت شده‌اند)، ولی **کاری برای هیچ‌کدام هنوز در کد انجام نشده** — فقط بحث/برنامه‌ریزی شده بود که با همین درخواستِ مستندسازی متوقف شد:

1. تکمیلِ قابلیتِ «ویرایشِ امتحان» در `exams.html`/`app.js`.
2. تصمیم‌گیری درباره‌ی فیلدهای یتیمِ `total_hours`/`notes` در فرمِ درس.
3. بازگرداندنِ ساعت هنگامِ حذفِ یک `StudyLog` (Override کردنِ متدِ `delete()` روی مدل).

این سه مورد **دقیقاً و کامل** در `TODO.md` (بخش‌های Bugs / High Priority / Medium Priority) مستند شده‌اند.

## آخرین Taskِ انجام‌شده

ساختِ ساختارِ مستنداتِ استانداردِ AI-collaboration (این فایل و سه فایلِ همراهش: `AI_CONTEXT.md`, `CHANGELOG.md`, `TODO.md`). **هیچ کدی در این Task تغییر نکرد** — طبقِ درخواستِ صریحِ کاربر، این مرحله فقط مستندسازی بود.

## Task بعدی (پیشنهادی، بر مبنایِ آخرین درخواستِ صریحِ کاربر)

کاربر گفته بود می‌خواهد قدم‌به‌قدم موارد را تکمیل کند. ترتیبِ طبیعی (کم‌ریسک‌ترین اول):

1. **تصمیم درباره‌ی `total_hours`/`notes`** — چون ساده‌ترین و کم‌ریسک‌ترین است: یا حذفِ این دو ورودی از `subjects.html` (و پاک‌کردنِ ارسال‌شان در `handleSubjectForm` در `app.js`)، یا افزودنِ فیلدهای متناظر به مدلِ `Subject` (+ یک Migrationِ جدید با `python manage.py makemigrations`، که نیازمندِ محیطِ Django واقعی است چون در این محیطِ ابزاریِ فعلی Django نصب نیست — به بخشِ «محدودیت‌های محیطِ ابزاری» پایین نگاه کن).
2. **بازگرداندنِ ساعت هنگامِ حذفِ `StudyLog`** — نیازمندِ Override کردنِ `delete()` روی مدلِ `StudyLog` (مشابهِ الگویِ موجودِ `save()` روی همان مدل). نیازمندِ Migration نیست (فقط منطق، نه ساختارِ دیتابیس).
3. **تکمیلِ «ویرایشِ امتحان»** — پیچیده‌ترین مورد؛ نیازمندِ: دکمه‌ی «ویرایش» روی هر ردیفِ جدولِ `exams.html` (که فعلاً اصلاً وجود ندارد)، تابعِ `resetExamForm()` در `app.js`، و تغییرِ `handleExamForm()` برای تشخیصِ حالتِ ایجاد در برابرِ ویرایش (POST در برابرِ PATCH). Endpointِ بک‌اند (`PATCH /api/exams/{id}/`) از قبل آماده است.

## مشکلاتِ شناخته‌شده

فهرستِ کامل در `TODO.md` (بخش‌های Bugs، High/Medium/Low Priority). مهم‌ترین‌ها: فیلدهای یتیمِ فرمِ درس، عدمِ Pagination، عدمِ محدودیتِ یکتاییِ `StudyPlan.user`، خالی‌بودنِ `tests.py`.

## چه بخش‌هایی نباید بدونِ دلیلِ مشخص تغییر کنند؟

- **`README.md`** — طبقِ قانونِ مستندسازیِ این پروژه، بدونِ دلیلِ صریح ویرایش نشود.
- **۹ نکته‌ی بخشِ ۶ در `AI_CONTEXT.md`** — این‌ها Invariantهای حیاتی‌اند (مثلِ محلِ کسرِ ساعت در `StudyLog.save()`، ترتیبِ `router.register`، فیلترِ اجباریِ `request.user` در هر `get_queryset`). نقضِ هرکدام باگ یا آسیب‌پذیریِ امنیتی ایجاد می‌کند.
- **فرمولِ الگوریتم در `utils.py`** — طراحیِ فعلی (وزن‌دهیِ متناسب + ادغامِ روزهای هم‌برنامه + توزیعِ بر‌مبنایِ درس نه روز) نتیجه‌ی رفعِ چند مشکلِ طراحیِ قبلی است (شرح در `CHANGELOG.md`، بخشِ Changed). قبل از تغییرِ این فرمول‌ها، حتماً همان بخش را بخوان تا مشکلاتِ قبلی دوباره رخ ندهند.

## اولین فایل‌هایی که یک AI تازه باید بخواند (به‌ترتیب)

1. **همین فایل (`HANDOFF.md`)**
2. `AI_CONTEXT.md` — به‌خصوص بخشِ ۶ (نکاتِ حیاتی)
3. `TODO.md` — برای دیدنِ دقیقِ کارِ باقی‌مانده
4. اگر روی بخشِ خاصی کار می‌کند: `docs/01` تا `docs/04` (بسته به این‌که بک‌اند/پیکربندی، API/الگوریتم، صفحاتِ HTML، یا `app.js`/`styles.css` مدِ نظر است)
5. `README.md` فقط اگر توضیحِ روایی/معماریِ کامل لازم باشد (اغلبِ اطلاعاتِ عملیاتی در ۴ فایلِ بالا خلاصه شده)

## محدودیت‌های محیطِ ابزاری (نکته‌ی عملیاتی مهم)

در محیطِ ابزاری‌ای که این مستندات و کدِ اخیر در آن ساخته شده‌اند، **Django/pip نصب نبود و دسترسیِ اینترنت هم برایِ نصب وجود نداشت** (شبکه محدود به چند دامنه‌ی خاص است، PyPI جزوِ آن‌ها نیست). به همین دلیل، تغییراتِ اخیر همیشه با `python3 -m py_compile` (برای سینتکسِ پایتون)، `node --check` (برای `app.js`)، و تست‌هایِ شبیه‌سازی‌شده‌ی دستی (اجرایِ توابعِ خالصِ `utils.py` با Mock Objects، بدونِ بالا آوردنِ کاملِ جنگو) صحت‌سنجی شده‌اند، **نه با اجرایِ واقعیِ `python manage.py runserver` یا `migrate`**. اگر AI بعدی به محیطی با Django/pip واقعی دسترسی دارد، پیشنهاد می‌شود قبل از تغییراتِ بزرگ، یک‌بار `python manage.py check` و `python manage.py test` را اجرا کند تا مطمئن شود فرضیاتِ این مستندات هنوز درست است.

## هر نکته‌ی مهمِ دیگر

- کاربر (صاحبِ پروژه) فارسی‌زبان است و پروژه را برایِ ارائه/دفاعِ دانشگاهی هم استفاده می‌کند؛ لحنِ توضیحات باید دقیق و صادقانه باشد (شاملِ اذعان به محدودیت‌ها)، نه فقط تبلیغاتی.
- پروژه به‌تازگی روی GitHub قرار گرفته (طبقِ گفتگوهایِ قبلی که در فایل‌ها منعکس نیستند، ولی وجودِ `.gitignore` آماده این را تأیید می‌کند)؛ پس تغییراتِ آینده احتمالاً باید با `git add`/`commit`/`push` هم همراه شوند، نه فقط تحویلِ فایل.
