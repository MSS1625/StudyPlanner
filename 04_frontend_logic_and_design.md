# مستند فنی پروژه — بخش ۴ از ۴: منطق فرانت‌اند (`app.js`) و طراحی بصری (`styles.css`)

**پروژه: برنامه‌ریز هوشمند مطالعه (Smart Study Planner)**

> پایانی‌ترین بخشِ مستندسازی. این فایل، دو فایلِ فرانت‌اندیِ باقی‌مانده را پوشش می‌دهد که
> در ۷ صفحه‌ی HTML (بخش ۳) لود می‌شوند: `app.js` (تمامِ منطقِ جاوااسکریپتی) و `styles.css` (تمامِ استایل).

---

## ۴.۱ `static/app.js` — تک‌فایلِ منطقِ فرانت‌اند

این فایل بدونِ هیچ فریم‌ورک یا کتابخانه‌ی جاوااسکریپتی (بدونِ React/Vue، بدونِ فرآیندِ Build/Bundle) نوشته شده و در همه‌ی صفحات به‌صورتِ یکسان لود می‌شود (`<script src="app.js" defer>`). ساختارِ کلیِ فایل به ترتیب:

### ۱) تنظیماتِ پایه

- `API_BASE = "http://127.0.0.1:8000"`: آدرسِ پایه‌ی سرورِ Django. چون فرانت‌اند و بک‌اند دو پروژه‌ی جدا هستند، هر درخواست باید این آدرس را جلوی مسیر بگذارد.
- `endpoints`: یک آبجکت که نامِ خوانا را به مسیرِ واقعیِ API نگاشت می‌کند (مثلاً `endpoints.subjects = "/api/subjects/"`). این‌طوری اگر روزی آدرسِ API عوض شود، فقط همین یک‌جا نیاز به ویرایش دارد.
- `selectors`: سلکتورهای CSS برایِ عناصرِ مشترکِ بینِ همه‌ی صفحات (Toast، نشانگرِ وضعیت، دکمه‌ی خروج و...).
- `storageKeys` و توابعِ کمکیِ `getToken`/`setToken`/`clearToken`/... : لایه‌ی نازکی روی `localStorage` برای نگه‌داریِ توکنِ JWT و نامِ کاربری بینِ بارگذاری‌های صفحه.

### ۲) لایه‌ی ارتباط با API

**`apiRequest(path, options)`** مرکزِ ثقلِ همه‌ی ارتباطات است. کارهایی که یک‌جا انجام می‌دهد:
- اگر توکن وجود دارد، آن را در هدرِ `Authorization: Bearer <token>` می‌گذارد.
- اگر بدنه یک آبجکتِ معمولی است (نه `FormData`)، آن را به JSON تبدیل می‌کند.
- پاسخ را می‌گیرد؛ اگر کدِ وضعیت خطا بود (غیرِ ۲xx)، تلاش می‌کند مناسب‌ترین پیامِ خطا را از میانِ شکل‌های مختلفِ خطاهای DRF (`detail`, `message`, یا خطای اعتبارسنجیِ فیلد‌به‌فیلد) استخراج کند و آن را با `throw new Error(message)` پرتاب کند.

سه تابعِ کوچکِ `apiGet`, `apiPost`, `apiDelete` فقط Wrapperهایی روی `apiRequest` هستند تا کدِ صدازننده خواناتر باشد.

### ۳) توابعِ کمکیِ رابطِ کاربریِ مشترک

- `showToast(message, type)`: نمایشِ پیامِ موقتِ گوشه‌ی صفحه؛ با `requestAnimationFrame` تضمین می‌کند انیمیشنِ CSS واقعاً اجرا شود.
- `updateUserGreeting()`: نمایشِ «سلام، فلانی» بر اساسِ نامِ کاربریِ ذخیره‌شده.
- `updateStatusIndicator(status)`: تغییرِ رنگ/متنِ نقطه‌ی وضعیت (آنلاین/در حال پردازش/خارج از سیستم).
- `requireAuth()`: اگر کاربر توکن نداشته باشد، به‌زور به `login.html` هدایتش می‌کند — این تابع اولین خط در `init...Page()` هر صفحه‌ی محافظت‌شده است.
- `logout()`: پاک‌کردنِ توکن/نامِ کاربری و بازگشت به صفحه‌ی ورود.
- `bindGlobalEvents()`: رویدادهایی که در همه‌ی صفحات مشترک‌اند — دکمه‌ی خروج، باز/بسته‌کردنِ سایدبارِ موبایل با دکمه‌ی همبرگری، و کلیک روی پس‌زمینه‌ی تیره برای بستنِ سایدبار.

### ۴) توابعِ مخصوصِ هر صفحه

این بخش، بزرگ‌ترین قسمتِ فایل است. برای هر صفحه، الگو معمولاً این است: یک تابعِ `render...()` که HTML می‌سازد، یک تابعِ `load...()` که داده را از API می‌گیرد و `render` را صدا می‌زند، و (اگر فرمی در کار باشد) یک تابعِ `handle...Form()` که رویدادِ Submit را می‌گیرد.

- **داشبورد**: `renderDashboard(data)` پنج بخشِ صفحه (کارت‌های آماری، پیشرفتِ درس‌ها، تقویم، هشدارها، نمودار) را جداگانه پر می‌کند؛ هرکدام یک بررسیِ «اگر داده خالی بود، پیامِ `empty-state` نشان بده» دارد. نکته‌ی جالب: برای نوعِ هشدار، به‌جای فرضِ فقط دو حالت (`warning`/`danger`)، یک فهرستِ `validTypes` شاملِ سومین نوعِ `info` هم بررسی می‌شود — این‌طوری اگر بک‌اند نوعِ جدیدی اضافه کند، به‌اشتباه قرمز/خطرناک نشان داده نمی‌شود.
- **درس‌ها**: `sortSubjects()` مرتب‌سازیِ سمتِ‌کلاینت (الفبا/سختی/پیشرفت)، `renderSubjects()` رسمِ جدول، `loadSubjects()` دریافتِ داده.
  - به‌روزرسانیِ 2026-08-25: `handleSubjectForm` دیگر فیلدِ `total_hours` نمی‌فرستد (حذف از فرم) و `notes` واقعاً ذخیره می‌شود؛ `renderSubjects` یادداشتِ درس را به‌صورتِ زیرنویسِ کم‌رنگ (`.text-muted`) زیرِ نامِ درس نمایش می‌دهد. تابعِ `resetExamForm()` هم ابتداً برای زیرساختِ «ویرایشِ امتحان» تعریف شد.
- **امتحان‌ها**: `populateSubjectSelect()` پرکردنِ کشوی درس در فرم؛ `renderExams()` که علاوه بر رسمِ جدول، خودش تعدادِ روزهای باقی‌مانده و «درجه‌ی اهمیت» (🔴/🟡/🟢) را از تاریخِ امتحان محاسبه می‌کند (این محاسبه در بک‌اند تکرار نمی‌شود، سمتِ کلاینت انجام می‌شود). از 2026-08-29 این جدول ستونِ «عملیات» هم دارد: هر ردیف دکمه‌ی «ویرایش» دارد که `startExamEdit(exam)` را صدا می‌زند — فرم با داده‌های امتحان پر می‌شود، `examId` ست می‌شود و ظاهرِ فرم به حالتِ ویرایش می‌رود؛ سپس `handleExamForm` تشخیص می‌دهد که submit باید به `POST /api/exams/` برود یا `PATCH /api/exams/{id}/` (Wrapperِ جدیدِ `apiPatch`). نامِ درس و یادداشتِ امتحان با تابعِ کمکیِ جدیدِ `escapeHtml` رندر می‌شوند (اولین گامِ بستنِ XSS). دکمه‌ی «ویرایش» از کلاسِ `.btn-edit` استفاده می‌کند (قرصیِ آبی با آیکونِ مداد که با CSS mask رندر می‌شود).
- **ثبتِ مطالعه**: `populateExamSelectForLog()`، `renderStudyLogs()` (با دکمه‌ی حذفِ پویا که رویدادش بعد از رندر اضافه می‌شود)، `deleteStudyLog()` (با `confirm()` قبل از حذف — از 2026-09-03 پیامِ تأیید اطلاع می‌دهد که ساعتِ کسرشده به امتحان برمی‌گردد و بعد از حذفِ موفق، کشویِ امتحان‌ها هم برای به‌روزشدنِ متنِ «X ساعت باقی‌مانده» تازه می‌شود)، و `handleStudyLogForm()` که بعد از ثبتِ موفق، هم لیستِ گزارش‌ها و هم کشوی امتحان‌ها (که ساعتِ باقی‌مانده‌اش عوض شده) را هم‌زمان با `Promise.all` تازه می‌کند. دکمه‌ی «حذف» هر ردیف از کلاسِ `.btn-danger-sm` استفاده می‌کند (قرصیِ قرمز با آیکونِ سطل).
- **برنامه‌ی مطالعه**: `renderPlan(plan)` بلوک‌های برنامه را می‌سازد؛ اگر بلوکی چند روز را ادغام کرده باشد (`days_covered > 1`)، یک برچسبِ کوچک («۴ روز») هم نشان می‌دهد. `getActiveRange()` تشخیص می‌دهد کدام دکمه (روزانه/هفتگی) الان فعال است. `handleDailyHoursForm()` بعد از تغییرِ ساعتِ روزانه، برنامه را دوباره برای همان بازه‌ای که کاربر در حالِ مشاهده‌اش بود بارگذاری می‌کند (نه همیشه «روزانه»).
- **ورود/ثبت‌نام**: `handleLogin()` و `handleRegister()`.

### ۵) توابعِ راه‌اندازیِ صفحه (`init...Page`)

هرکدام یک ترکیبِ ثابت دارند: `requireAuth()` + `bindGlobalEvents()` + کارِ مخصوصِ همان صفحه (وصل‌کردنِ فرم‌ها، بارگذاریِ اولیه‌ی داده).

### ۶) روترِ سبکِ انتهای فایل

```javascript
document.addEventListener("DOMContentLoaded", () => {
    const page = document.body.dataset.page;
    switch (page) {
        case "login": handleLogin(); break;
        case "dashboard": initDashboardPage(); break;
        // ...
    }
});
```

این جایگزینِ یک کتابخانه‌ی Routing واقعی است که برای این پروژه (چند صفحه‌ی جدا، نه یک Single Page Application) اضافی بود. با خواندنِ `data-page` از تگِ `<body>` (همان‌که در بخشِ ۳ توضیح داده شد)، دقیقاً می‌داند کدام تابعِ راه‌اندازی را صدا بزند.

---

## ۴.۲ `static/styles.css` — سیستمِ طراحیِ بصری

استایلِ کاملاً دستی، بدونِ Bootstrap/Tailwind یا هر فریم‌ورکِ CSS دیگر. ساختارِ کلی:

### متغیرهای سراسری (`:root`)

در ابتدای فایل، حدودِ ۱۵ متغیرِ CSS (`--primary`, `--danger`, `--radius-lg`, `--shadow`, ...) تعریف شده‌اند. این یعنی رنگ یا شعاعِ گردیِ کلِ سایت را می‌شود با تغییرِ چند مقدار در یک‌جا عوض کرد، بدونِ گشتن در تک‌تکِ کلاس‌ها — یک الگوی استانداردِ نگه‌داری‌پذیر (Maintainable) در CSS مدرن.

### چیدمانِ کلی

`.app-shell` با `display: grid; grid-template-columns: 280px 1fr;` صفحه را به دو ستون تقسیم می‌کند: سایدبارِ ثابت (۲۸۰ پیکسل) و محتوای اصلی (باقیِ فضا). چون صفحه `dir="rtl"` است، این ستونِ اول به‌صورتِ خودکار در سمتِ **راست** قرار می‌گیرد.

### بخش‌های اصلیِ استایل (به ترتیبِ فایل)

1. **سایدبار**: گرادیانِ آبیِ تیره، لوگو، لینک‌های ناوبری (با حالتِ `is-active` برای صفحه‌ی جاری)، نشانگرِ وضعیتِ اتصال.
2. **نوار بالا (Topbar)**: `position: sticky; top: 0;` به‌همراه یک `background` و `box-shadow` مشخص — این دو مقدار عمداً اضافه شده‌اند تا هنگامِ اسکرول، محتوای زیرِ نوار از پشتش دیده نشود (اگر فقط `sticky` باشد بدونِ پس‌زمینه‌ی مات، محتوای اسکرول‌شونده از زیرش رد می‌شود).
3. **کارت‌های آماری، پنل‌های عمومی**: کلاس‌های مشترکِ `.panel`, `.panel__header`, `.metric-card` که در همه‌ی صفحات تکرار می‌شوند.
4. **پیشرفت/تقویم/هشدار/نمودار**: استایلِ اختصاصیِ هر پنلِ داشبورد؛ از‌جمله سه رنگِ متفاوت برای `.alert--danger` (قرمز)، `.alert--warning` (زرد) و `.alert--info` (آبی).
5. **نمودارِ میله‌ای (`.mini-chart`, `.mini-bar`)**: یک نمودارِ میله‌ایِ ساده که کاملاً با CSS خالص ساخته شده (بدونِ Chart.js یا کتابخانه‌ی مشابه) — ارتفاعِ هر میله با `style="height: X%"` از جاوااسکریپت تنظیم می‌شود و برچسبِ داخلش با `transform: rotate(-90deg)` عمودی چرخانده شده تا در عرضِ باریکِ میله جا شود.
6. **فرم‌ها و جدول‌ها**: کلاس‌های عمومیِ `.form-grid`, `.form-field`, `.primary-button`, `.data-table` که در صفحاتِ درس‌ها/امتحان‌ها/ثبتِ مطالعه استفاده می‌شوند. از 2026-08-29 دکمه‌های عملیاتِ روی سطحِ سفید هم کلاس‌های اختصاصی دارند: `.btn-edit` (ویرایش)، `.btn-cancel-edit` (انصراف از ویرایش)، `.btn-danger-sm` (حذف) و نسخه‌ی سطحِ روشنِ `.panel .ghost-button` برای دکمه‌های ghostِ داخلِ پنل‌ها (آیکون‌های همه‌ی این‌ها SVG درون‌خطی با CSS mask هستند).
7. **بلوک‌های برنامه‌ی مطالعه (`.plan-block`, `.plan-days-chip`)**: استایلِ اختصاصیِ صفحه‌ی برنامه، شاملِ برچسبِ کوچکِ «N روز» برای بلوک‌های ادغام‌شده.
8. **صفحاتِ ورود/ثبت‌نام (`.auth-page`, `.auth-card`)**: چیدمانِ وسط‌چینِ مستقل از `app-shell` (چون این دو صفحه سایدبار ندارند).

### واکنش‌گرایی (Media Queries)

دو نقطه‌ی شکست (Breakpoint):

- **زیرِ ۱۱۰۰ پیکسل**: `.app-shell` از حالتِ Grid دوستونه به یک‌ستونه تغییر می‌کند؛ سایدبار از حالتِ ثابتِ درونِ Grid خارج و به یک پنلِ شناور (`position: fixed`) با `inset: 0 0 0 auto` (یعنی چسبیده به لبه‌ی **راست**، چون RTL است) تبدیل می‌شود. با کلاسِ `sidebar-open` روی `<body>` (که `bindGlobalEvents` در `app.js` با کلیکِ دکمه‌ی همبرگری اضافه/حذف می‌کند)، با `transform: translateX(0/100%)` باز و بسته می‌شود.
- **زیرِ ۶۴۰ پیکسل**: گریدهای دوستونه (مثلِ کارت‌های آماری) به چیدمانِ فشرده‌ترِ موبایل تغییر می‌کنند.

### انیمیشن

یک `@keyframes fadeUp` تعریف شده که پنل‌ها و کارت‌های `auth-card` هنگامِ بارگذاریِ صفحه با آن، به‌آرامی از پایین محو و ظاهر می‌شوند (حسِ نرم‌تر به رابط کاربری می‌دهد).

---

## جمع‌بندیِ نهایی

با کنارِ‌هم‌گذاشتنِ هر ۴ بخشِ این مستند، تصویرِ کاملی از پروژه به دست می‌آید:

- **بخش ۱** نشان داد چطور Django پیکربندی شده و داده‌ها (درس، امتحان، لاگ، تنظیمات) چه ساختاری دارند.
- **بخش ۲** نشان داد چطور این داده‌ها از طریقِ یک REST API امن (با JWT و فیلترِ سمتِ کاربر) در دسترسِ فرانت‌اند قرار می‌گیرند، و چطور الگوریتمِ ریاضیِ ساده‌ی وزن‌دهی، بدونِ هیچ سرویسِ خارجی، برنامه‌ی مطالعه می‌سازد.
- **بخش ۳** نشان داد این داده‌ها در ۷ صفحه‌ی HTML چطور به کاربر نمایش داده می‌شوند.
- **بخش ۴** نشان داد چه منطقِ جاوااسکریپتی (بدونِ فریم‌ورک) و چه سیستمِ طراحیِ CSSیی (بدونِ کتابخانه) این صفحات را زنده و تعاملی می‌کند.

هر تصمیمِ طراحیِ مهم (چرا SQLite، چرا JWT، چرا الگوریتمِ قانون‌محور به‌جای ML، چرا فرانت‌اندِ بدونِ فریم‌ورک) هم در همین مستند و هم با جزئیاتِ تکمیلی در `README.md` پروژه توضیح داده شده است.

---

*پایانِ مستندِ فنیِ پروژه (۴ بخش).*
